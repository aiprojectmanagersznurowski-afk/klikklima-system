---
name: repo-drift-traps
description: Miejsca w KlikKlima, gdzie kod i kontrakt rozjeżdżają się po cichu — sprawdzaj je przy każdym Work Orderze z domeny funnel/crm
metadata:
  type: project
---

Kod panelu B2B ma **własną, równoległą maszynę stanów** (`ALLOWED_TRANSITIONS` w `apps/b2b-web/src/app/(dashboard)/leads/actions.ts`), niezależną od `contracts/funnel.contract.mjs`. (Errata 2026-09-01: `@klikklima/contracts` jest już importowany w 17 plikach B2B, ale WYŁĄCZNIE warstwa RBAC/`can()`. `TRANSITIONS.effects`, `NOTIFICATIONS` i `QUEUE_POLICY` nadal mają zero konsumentów w `apps/` — `grep -rn "effects" apps/` = 0.)

**Why:** kontrakt jest źródłem prawdy (zasada zerowa), ale nie ma dziś żadnego konsumenta po stronie B2B — więc „przejście istnieje w kontrakcie" nie znaczy „działa w aplikacji". Odkryte przy WO `CRM-SAFE-RECORD-ACTIONS` (2026-08-20): kontrakt ma T15 `QUOTE_REJECTED → AUDIT_COMPLETED`, a UI oferuje `QUOTE_REJECTED → NEW_LEAD`.

**How to apply:** pisząc WO dotyczący przejścia lejka, ZAWSZE porównaj kontrakt z `ALLOWED_TRANSITIONS` i etykietami w `leads-client.tsx`. Rozjazd wypisz jako osobny punkt — implementer inaczej „naprawi" jedną stronę i zostawi drugą.

Stałe pułapki tego repo, weryfikowane wielokrotnie:
- Prisma `enum LeadStatus` jest **uboższy** niż stany w kontrakcie (brakowało `ARCHIVED_LOST`). Sprawdzaj enum, zanim zaplanujesz przejście.
- Nie ma tabel `quotes` ani `audit_log`, mimo że kontrakt (`DELETE_POLICIES`, `AUDIT_REQUIREMENTS.mustLog`) na nie liczy.
- Server Actions w `(dashboard)/*/actions.ts` nie sprawdzają roli, a Prisma omija RLS. Każdy WO dotykający DELETE musi to wywołać jawnie.
- `apps/` zawiera tylko `b2b-web` i `b2c-web` — **Field App nie istnieje**, więc kryteria „nie loguje się do Field App" nie są testowalne end-to-end.
- `tools/kk-precommit-scan.mjs` raportuje **pierwsze** dopasowanie na plik — wcześniejsza reguła (np. `as-any`) maskuje kolejne (`service-key`). „Skaner zielony" po naprawie jednego naruszenia nie znaczy „plik czysty"; przeskanuj ponownie.
- Reguła `service-key` obejmuje wyłącznie `.tsx` pod `app|components|hooks`. Przeniesienie kodu z kluczem serwisowym do `.ts` ucisza bramkę, nie zwiększając bezpieczeństwa — w WO żądaj dowodu izolacji (`server-only`), nie samego exit 0.
- Pole `leady.lost_reason` jest przeciążone: trzyma zarówno powody ze słownika `LOST_REASONS`, jak i techniczny znacznik `AUTO_REJECT_14_DAYS` używany do filtrowania bucketu.

Powiązane: [[contract-sources-of-truth]]

Dopisane 2026-08-21 (planowanie fazy 0 Field App):
- **`tools/kk-codegen.mjs` po cichu gubi wartość progu SLA o nieznanym kształcie.** Lista skalarów (`['days','count','hourOfDay']`) jest osobna od `MEASURES` w `kk-validate.mjs`. Próg z polem spoza tej listy przechodzi walidację, a do `generated/sla.ts` trafia sam `scope`, bez liczby i bez błędu. Rozszerzając `MEASURES`, zawsze sprawdź obie listy.
- **`kk-trace.mjs` traktuje `BLOCKED` inaczej tylko w jednym miejscu**: wyłącza je z ostrzeżenia „HIGH RISK bez testu". W `violations` liczy się status `DONE`/`IMPLEMENTING` bez testu. To jedyna semantyka `BLOCKED` w repo — poza tym status nic nie znaczy.
- **`apps/b2c-web/app/actions/leads.ts` wstawia do `adresy` kolumny `lat`/`lng`, które nie istnieją** (migracja z `75da8c5` dodała `latitude`/`longitude`). `field_app_requirements.md#6.3` twierdzi, że ta ścieżka „zapisuje współrzędne" — opisuje intencję, nie skutek. Jedyne wywołanie zapisu leada z UI to `saveLead` w `Step8Booking.tsx:195`.
- **`kk-trace.mjs` dopasowuje ID wymagania w komentarzu testu, nie zasób.** Wymaganie o zakresie zbiorczym („we WSZYSTKICH 7 widokach CRM", `CRM-DELETE-ADMIN-ONLY`) pokazuje się jako ✓ pokryte po naprawieniu dwóch widoków z siedmiu. Przy każdym wymaganiu mówiącym „wszystkie/każdy", policz ścieżki w kodzie ręcznie — zielony trace nic tu nie znaczy.
- **Ta sama encja bywa kasowana z dwóch widoków.** `deleteLogisticsOrderAction` (`logistics/actions.ts`) robi `prisma.leady.delete` — obchodzi bramkę w `deleteLeadAction`. Szukając „czy DELETE jest zabezpieczony", grepuj po TABELI (`prisma.<tabela>.delete`), nie po nazwie widoku.
- **`DELETE_POLICIES.clients = ANONYMIZE_OR_SET_NULL`, a kod robi twardy `prisma.klienci.delete`**; dokument (`b2b_crm_specifications.md:32`) pisze „twarde usunięcie / usunięcie zgodne z RODO" i sam nie rozstrzyga. Stała sprzeczność — zgłaszaj jako WYMAGA DECYZJI, nie wybieraj.
- **Asymetria flag aktywności:** `audytorzy.is_active` kontra `zespoly_monterskie.aktywny`. Bramka logowania czyta tylko pierwszą; drugiej nie czyta nic w celach autoryzacji.
- **`contracts/rbac.contract.mjs` nie zna kolumn, tylko zasoby.**
 Każde `audytor:own` w `update` na `auditors` daje pracownikowi dostęp również do `is_active`. Planując pole edytowalne przez pracownika, pytaj, czy nie musi mieszkać w osobnej encji.

Dopisane 2026-08-26 (WO `SEC-AUTHZ-B2B-MUTATIONS`, po zbudowaniu `tools/kk-authz-gate.mjs`):
- **`tools/kk-authz-gate.mjs` to jedyne narzędzie odpowiadające na pytanie „czy Server Action w ogóle pyta o uprawnienia".** Uruchamiaj je na starcie każdego WO z domeny security/CRM. Ale: skanuje wyłącznie pliki o nazwie `actions.ts` pod `apps/b2b-web/src/app`, więc mutacja w Route Handlerze albo w `lib/` nie zostanie wykryta — liczba znalezisk to dolna granica. Nie dowodzi też POPRAWNOŚCI pary zasób/zdolność. Świadomie niepodpięte do `verify.sh`, dopóki dług otwarty.
- **Przegląd „po nazwach akcji z wymagania" systematycznie gubi dziury.** `leads/actions.ts` był naprawiany trzy razy i po każdej naprawie wyglądał na domknięty; skaner znalazł w nim jeszcze `updateLeadStatus` i `advanceLeadStatus`. Zawsze idź po MUTACJACH w pliku, nie po liście nazw z acceptance.
- **Pole `actor` w `contracts/funnel.contract.mjs` NIE jest źródłem autoryzacji.** To pojedyncza wartość z `ACTORS` (`CLIENT|DISPATCHER|ADMIN|AUDITOR|INSTALLER|SYSTEM`), opisuje typowego wykonawcę przejścia. Nie istnieje żadne odwzorowanie `ACTORS` → `ROLES` ani w kontraktach, ani w kodzie (zero użyć `.actor` w `apps/`). Wnioskowanie „T06 ma DISPATCHER, więc admin nie może" jest błędem — zestawy ról bierz wyłącznie z `MATRIX`.
- **`RESOURCES` nie pokrywa wszystkich tabel, do których pisze kod.** Brakuje m.in. `addresses` (tabela `adresy`), mimo że `NAMING.md:30` ma dla niej nazwę. Zanim zaplanujesz bramkę, sprawdź, czy zasób w ogóle istnieje w macierzy — inaczej implementer wymyśli mapowanie sam.
- **Kolizja `installations.update` (`monter:own`) z `leads.update` (bez montera) blokuje T09 `completeInstallation`.** `updateInstallationStatus` pisze do obu tabel w jednej akcji. Żadne miejsce w kodzie nie implementuje dziś wariantu `:own` — `can()` zwraca `'own'`, a sprawdzenia własności rekordu nie ma nigdzie. Każdy WO opierający się na `:own` musi to wywołać jako osobną pracę.
- **`installations/actions.ts:assignCrew` to homonim akcji kontraktowej T05 `assignCrew`**, którą realizuje `assignCrewToLead` w `leads/actions.ts`. Martwy kod bez wywołań. Grepując „assignCrew", rozróżniaj te dwa.
- **Widok „Serwisy Gwarancyjne" prawdopodobnie kasuje niewłaściwą encję:** `getUpcomingServices` zwraca wiersze z `instalacje` z `id: inst.id`, a `deleteServiceAction` robi `prisma.serwisy.delete({ where: { id } })` — inna tabela. Do weryfikacji.
- **Liczby wystąpień podane w treści wymagania bywają artefaktem substringu.** `SEC-LEADS-LIST-SCALARS` twierdzi, że `as Lead` występuje w `leads-client.tsx` trzykrotnie — `grep -n "as Lead"` daje trzy trafienia, ale trzecie to `as LeadStatus` (legalne, ma zostać). Każdą liczbę „występuje N razy" z acceptance weryfikuj greperem i wypisuj w WO faktyczne numery linii, inaczej implementer usunie coś potrzebnego, szukając brakującego wystąpienia.
- **Test kształtu przy mockowanej Prismie dowodzi mapowania, nie `select`-a.** Mock `findMany` zwraca to, co mu wpisano, niezależnie od `select` — więc WO na minimalizację odczytu ZAWSZE musi żądać dwóch osobnych asercji: na argument przekazany do `findMany` (co weszło do pamięci serwera) i na zwrócony obiekt (co wyszło do klienta). Dodatkowo: dopóki wynik budowany jest spreadem `...lead`, „pobrane == wysłane" i mapowanie nie jest żadną drugą linią obrony.
Dopisane 2026-08-26 (WO `SEC-RLS-AUDITOR-SCOPE`):
- **Cała autoryzacja w panelu B2B dotyczy WYŁĄCZNIE mutacji.** Na 21 plików `page.tsx` pod `(dashboard)` dokładnie jeden (`settings/page.tsx`) sprawdza `can(..., 'read')`. `getLeads()`, `getInstallations()`, `getCustomers()`, `getLogisticsLeads()`, `getIncidents()`, `getUpcomingServices()`, `getAuditors()`, `getCrews()` — zero bramek odczytu. Planując cokolwiek z domeny security, nie zakładaj, że „odczyt jest już ograniczony rolą"; trzy wymagania (`SEC-*-MINIMIZE`, `SEC-LEADS-LIST-SCALARS`) mają tę fałszywą przesłankę wpisaną w `source`.
- **`getCurrentActorRole()` zwraca samą rolę, nigdy tożsamości.** Każde wymaganie `:own` wymaga osobnego `prisma.audytorzy/zespoly_monterskie.findUnique({ where: { email: user.email } })` — wzorzec skopiowany już w `auditors/actions.ts:121` i `crews/actions.ts:111`. `audytorzy.email` jest NULLABLE, więc „nie znaleziono" to realny stan produkcyjny i każde AC musi być fail-closed (Prisma ignoruje `where` z wartością `undefined` i zwraca WSZYSTKO).
- **`leady`, `klienci`, `adresy` mają RLS włączone, ale tylko polityki INSERT dla anon** (`20260824185845_security_enable_rls_baseline.sql`). Odczyt przez `supabase-js` jest więc deny-by-default — acceptance typu „test próbuje eskalacji przez supabase-js" nie wymaga nowej pracy w kodzie, tylko testu zamrażającego ten stan.
- **Nic nie ogranicza roli przy wejściu do panelu.** `middleware.ts:110` przekierowuje KAŻDEGO authorized usera na `/leads`, a `navItems` w `layout.tsx:22` jest statyczne (zero warunków roli). Jedyny hamulec przed kontami nie-admin: `SettingsClient.tsx:31` woła `addAuthorizedUser(email, "admin")` na sztywno — ale sama Server Action przyjmuje dowolną rolę z `ROLES`. „Nie da się założyć konta audytora" to własność klienta, nie kontraktu akcji.

Dopisane 2026-08-27 (WO `CRM-KARTOTEKI-CREATE-AND-CREW-ASSIGN`):
- **Placeholder „Dodawanie w Fazie 2" nie znaczy „nic nie ma".** `auditors/components/AddAuditorModal.tsx` i `AddCrewModal.tsx` to kompletne, gotowe formularze — po prostu **nikt ich nie importuje** (martwy kod, jak `AuditorsTable.tsx`, `CrewsTable.tsx`, `TopBar.tsx` w tym samym katalogu). `alert()` siedzi w `auditors-client.tsx:90` / `crews-client.tsx:101`. Zanim napiszesz „brakuje UI", zgrepuj nazwę komponentu — w tym repo „brakuje" zwykle znaczy „nie jest podpięte". Uwaga na katalog: modal EKIPY leży pod `auditors/components/`.
- **Oba te modale łamią ADR-001** (`useState` na pola zamiast `react-hook-form` + `zodResolver`), a `react-hook-form` i `zod` **nie są zależnościami `apps/b2b-web`**. Każdy WO dotykający formularza w B2B musi to rozstrzygnąć jako decyzję, nie założyć.
- **Ekipa NIE mieszka na leadzie.** `leady` ma `audytor_id` wprost, ale ekipa żyje na `instalacje.zespol_id` (`instalacje.lead_id` jest NOT NULL). Konsekwencja, którą trzeba wypisywać w każdym WO o przypisaniu ekipy: przypisanie przed E4 **musi utworzyć wiersz `instalacje`** (status `PLANNED`), a ten natychmiast wchodzi do widoku Instalacji, licznika `installationsCount` w `crews/actions.ts` i blokady `deleteCrewAction`. „Instalacja duch" to realny efekt uboczny, nie kosmetyka.
- **`assignCrewToLead` (`leads/actions.ts:159`) jest twardo zawężone do T05** — odrzuca każdy status ≠ `AWAITING_CREW_ASSIGNMENT`, wymaga `data_rezerwacji`, przesuwa lead do E5. Nie da się go użyć jako ogólnego „ustaw pole". `getCrews()` z tego pliku wymaga daty montażu jako argumentu OBOWIĄZKOWEGO — pula wspólna dla listy leadów o N różnych datach wymagałaby innej sygnatury.
- **`updateLeadAuditor` (`leads/[id]/actions.ts:97-102`) też zmienia status** (NEW_LEAD↔AWAITING_AUDIT) i robi to **z pominięciem `canTransition`/`findTransition`**, inaczej niż `assignCrewToLead`/`returnToFunnel`/`archiveLost` w sąsiednim pliku. „Przypisanie audytora jest neutralne statusowo" to fałszywa przesłanka — nie powielaj jej w WO.
- **`getLeadDetail` robi `include: { klient, adres }` — bez `instalacje`.** Strona szczegółów leada nie pokazuje ekipy w ogóle; każde „dodaj widok ekipy na szczegółach" to też rozszerzenie tego zapytania, nie sam komponent.
- **`CrewsClient` nie przyjmuje `actorRole`** (`AuditorsClient` przyjmuje). Element „Usuń (Tylko Admin)" jest tam pokazywany wszystkim — serwer odrzuca, UI kłamie. Asymetria par audytorzy/ekipy jest w tym repo regułą, nie wyjątkiem (por. `is_active` vs `aktywny`).

Dopisane 2026-08-31 (errata A-2 do WO `CRM-KARTOTEKI-CREATE-AND-CREW-ASSIGN`):
- **Kryteria akceptacji, które LICZĄ pola („komplet 12 pól kartoteki"), zamieniają każde dodanie pola w zmianę kontraktu.** Przy rejestracji `CRM-*-KARTOTEKA` pominięto `fgaz_valid_until`/`sep_valid_until` — implementer zbudował dokładnie zarejestrowany zakres. Wyliczając pola w acceptance, przejdź kolumna po kolumnie po `schema.prisma`, nie po istniejącym modalu.
- **Bramka certyfikatów istnieje TYLKO dla ekipy.** `isCertValidForDate` (`leads/actions.ts`) traktuje `NULL` jako NIEWAŻNY (fail-closed) i jest wołana w `getCrews()` oraz w `assignCrewToLead`. `getAuditors()` nie pobiera i nie sprawdza żadnej daty ważności — `fgaz_valid_until` audytora karmi wyłącznie plakietkę w `auditors-client.tsx`, a `sep_valid_until` audytora nie jest czytane NIGDZIE. Zgłoszenia typu „audytor nie przejdzie sprawdzenia certyfikatu" są w połowie fałszywe; nie domykaj tej asymetrii „dla symetrii" — to nowa reguła biznesowa bez źródła.
- **W `apps/b2b-web` nie ma ani jednego `<input type="date">`.** Pierwsze pole daty ustanawia wzorzec mapowania `FormData` → `DateTime? @db.Date`: `'' → null`, inaczej północ **UTC** (`new Date('YYYY-MM-DD')`), bo `dateOnlyIso` porównuje w UTC, a dzień montażu liczy w `Europe/Warsaw`.

Dopisane 2026-08-31 (batch MEDIUM/LOW z `openFindings`):
- **Część znalezisk „bezpieczeństwa" opisuje stan ŻYWEJ BAZY, którego nie ma w repo.** Buckety Storage `bazawiedzy`/`urzadzenia` i GRANT-y na `public.AuthorizedUser` nie mają ANI JEDNEJ wzmianki w `supabase/migrations/*.sql`. Planując ich naprawę, nie da się potwierdzić stanu wyjściowego greperem — WO musi żądać sprawdzenia na żywo przed migracją.
- **Widok „Serwisy Gwarancyjne" to projekcja `instalacje`, nie tabeli `serwisy`.** `getUpcomingServices` zwraca `id: inst.id`, a `deleteServiceAction` kasuje `prisma.serwisy` — akcja jest martwa (zawsze P2025). Potwierdzone 2026-08-31, nadal niepoprawione.
- **Zod jest w monorepo tylko po stronie klienta.** Importują go wyłącznie `AddCrewModal.tsx` i `AddAuditorModal.tsx`; 0 z 10 plików `actions.ts` w `apps/b2b-web`. „ADR-001 wymaga walidacji Zod" nie znaczy, że gdziekolwiek na serwerze ona jest.
- **`shipments` ISTNIEJE w `RESOURCES`/`MATRIX`** (`rbac.contract.mjs:13,37`), ale `logistics/actions.ts` sprawdza wyłącznie `leads.update`. Zestawy ról są dziś identyczne, więc to dług utajony, nie dziura — nie zgłaszaj jako podatności.

Dopisane 2026-09-01 (WO `SRV-SOURCE-OF-TRUTH-SERVICES-VIEW`):
- **`CHANGES-ADR-010.md` opisuje schemat, którego NIE MA.** Nie istnieje tabela `services` ani `bookings`, nie ma `booking_id`, `scheduled_date`, `reminder_sent_at`, a `enum ServiceStatus` to nadal `PLANNED SCHEDULED COMPLETED CANCELLED` — bez `AWAITING_CONTACT`/`IGNORED`. Fizyczną realizacją „serwisu" jest legacy `serwisy` (relacja `instalacje.serwisy[]`). Dokument pisze „sprawdzone w obie strony" (linia 52), co dotyczy tylko hooka `adr010-derived-write`, nie migracji. Każde wymaganie z `source: ADR-010` planuj na `serwisy`, nie na `services`.
- **`serwisy` miesza przegląd roczny ze zgłoszeniem usterki** (`opis_usterki`, `data_zgloszenia`), mimo osobnej tabeli `usterki_incidents`. Nierozstrzygnięte — nie filtruj tej tabeli po zgadywanym przeznaczeniu.
- Potwierdzone dwie diagnozy z 2026-08-31: `deleteServiceAction` nadal kasuje `serwisy` po ID z `instalacje`, a pozycja „Usuń" w `services-client.tsx` renderuje się bezwarunkowo.

Dopisane 2026-09-01 (WO `CLIENT-ANONYMIZATION-RODO`):
- **Wzorzec „druga ścieżka kasowania” NIE występuje dla `klienci`.** Sprawdzone greperem: jedyne `prisma.klienci.delete` jest w `customers/actions.ts:67`. Analogia do `deleteLogisticsOrderAction`→`prisma.leady.delete` jest prawdziwa tylko dla leadów. Nie powtarzaj podejrzenia jako faktu — grepuj za każdym razem.
- **`EmployeeConsent` (`schema.prisma:567`) to gotowy, udokumentowany wzorzec tabeli append-only** (RBAC `update: []` + brak polityk RLS + wyzwalacz `*_append_only_trg`, bo Prisma omija RLS). Projektując `audit_log` albo dowolny rejestr-dowód, kopiuj ten profil zamiast wymyślać.
- **`AUDIT_REQUIREMENTS.mustLog` to słownik OPERACJI audytowych, nie zdolności RBAC.** `'anonymize'` tam obecne nie znaczy, że `PERMISSIONS` w `rbac.contract.mjs` potrzebuje zdolności `anonymize`. Te osie się nie pokrywają; `DELETE_POLICIES` już definiuje, czym „delete” jest dla danej encji.

- **Akcje zwracające `void` maskują odmowę.** Klienty CRM (`customers-client`, `incidents-client`, `services-client`, `installations-client`, `logistics-client`) pokazują komunikat sukcesu i przeładowują stronę niezależnie od wyniku. Zmiana sygnatury na `{ success, error }` jest częścią naprawy uprawnień, nie kosmetyką — bez niej AC „odmowa odróżnialna od sukcesu" nie da się spełnić.

Dopisane 2026-09-01 (WO `LOGISTICS-SHIPPING-EFFECTS`):
- **Cała warstwa powiadomień to dziś czysty kontrakt bez implementacji.** `notification_queue` występuje w repo dokładnie 4 razy i wyłącznie w `contracts/` + `packages/contracts/src/generated/`. Zero w `schema.prisma`, zero w `supabase/migrations/`, zero producentów, zero nadawców (`twilio|resend|nodemailer|sendSms|sendEmail` = 0 trafień w monorepo). Wymagania `NTF-*` zakładają istnienie kolejki, ale ŻADNE nie odpowiada za jej powstanie — planując cokolwiek z powiadomień, najpierw zarejestruj brakujące wymaganie na samą tabelę.
- **„Wstrzymanie SLA" nie ma czego zatrzymać.** `SLA_POLICIES.LOGISTICS_INSTALL` to metryka WYLICZANA (`daysUntilInstallation` z `leady.data_rezerwacji`), nie zapisany zegar. Każdy efekt `do:suspend*Sla` wymaga najpierw ZDEFINIOWANIA reprezentacji w schemacie, a nie odnalezienia jej.
- **W B2C nie ma wzorca atomowej rezerwacji slotu do skopiowania.** `getFomoSlots.ts` robi `count(*)` przez `supabase-js` i porównuje z limitem z `system_config`, a `saveLead.ts` zapisuje `data_rezerwacji` bez ŻADNEGO sprawdzenia pojemności. To jest dokładnie antywzorzec z CLAUDE.md #4. Jedyny precedens kontroli izolacji w całym repo: `auditors/actions.ts:432` (`isolationLevel: 'Serializable'`); `FOR UPDATE`/`$queryRaw`/advisory locks — zero wystąpień.
- **Slot ekipy nie jest jedną rzeczą.** Żyje na `instalacje.zespol_id` + `instalacje.data_planowana` ORAZ `leady.data_rezerwacji`. Sygnatura `releaseCrewSlot(crewId)` jest nierozstrzygalna (ekipa ma wiele slotów) — zwolnienie musi iść po `leadId`.
Dopisane 2026-09-02 (WO `SEC-READ-GATES`):
- **`monter:own` JEST wykonalny dzisiejszym schematem.** `zespoly_monterskie.email String? @unique` istnieje (`schema.prisma:218`, migracja `20260822120000`), a `zespol_id` siedzi na `instalacje`, `serwisy` i `usterki_incidents` (wszystkie nullable, wszystkie z indeksem). Wzorzec `findUnique({where:{email:user.email}})` działa już w `crews/actions.ts:122,166`. Nie powtarzaj hipotezy „brak powiązania montera z ekipą" — została obalona pomiarem.
- **Pułapka przy `:own` na `serwisy`/`usterki_incidents`:** ich własne `zespol_id` jest w praktyce puste, bo przypisanie ekipy żyje na `instalacje.zespol_id` (`assignCrewToLead`). Filtr wyłącznie po `zespol_id` daje monterowi pustą listę także dla jego montaży — czyli „naprawę", po której widok przestaje działać. Zawsze rozważ dziedziczenie z instalacji zawężone do rekordów bez własnego przypisania.
- **Rozszerzenie `kk-authz-gate.mjs` o odczyty daje ZERO fałszywych alarmów** (skan AST 2026-09-02: 34 eksportowane funkcje czytające Prismę, 27 z `can()`, 7 bez — dokładnie znane znaleziska; zero naruszeń kolejności wśród zabramkowanych). Etapowanie „najpierw raport ostrzegawczy" jest tu zbędne. Blind spot `export const … = async () => …` jest dziś pusty w `apps/b2b-web/src/app/**/actions.ts`.
- **`tools/` NIE jest ścieżką chronioną kontraktowo** (`contractProtectedPaths` w `tools/kk.config.mjs:14-19` to tylko `contracts/`, `packages/contracts/src/generated/`, `schema.prisma`, `supabase/migrations/`). Zmiana narzędzia wymaga roli `contract-steward`, ale NIE okna. Planując fazy, nie wrzucaj `tools/` do okna „na wszelki wypadek".
- **Precedens bramki strony to `notFound()`**, w obu istniejących miejscach (`leads/page.tsx`, `settings/page.tsx`). `redirect('/leads')` jest dodatkowo bez sensu dla montera — `/leads` też mu zwraca 404.

Dopisane 2026-09-10 (WO `FLD-AVAIL-WEEKLY-RULES`):
- **Kolumny GENERATED nie istnieją dla Prismy, a to na nich wisi unikalność kalendarza.** `availability_rules`, `absences` i `bookings` mają `resource_id UUID GENERATED ALWAYS AS (COALESCE(auditor_id, crew_id)) STORED`, a unikalny indeks to `(resource_id, weekday)`. Modele Prisma tego pola NIE mają i nie mają `@@unique` — więc `prisma.availabilityRule.upsert` jest niedostępny. Każdy WO na te tabele musi wskazać drogę zapisu (`$executeRaw` z `ON CONFLICT (resource_id, weekday)` albo `findFirst` + obsługa `P2002`), inaczej implementer napisze „sprawdź, potem zapisz" i przy współbieżności dostanie 500.
- **`setSelfAvailabilityAction` (audytorzy i crews) nie ma ANI JEDNEGO wywołania w `.tsx`.** FLD-AVAIL-SELF jest zaimplementowane wyłącznie serwerowo. Nie ma w panelu B2B żadnego ekranu „moje dane"/samoobsługi pracownika — każdy WO z UI dla roli terenowej zakłada nową trasę, nie rozszerzenie istniejącej.
- **`system_config` nie jest czytane z `apps/b2b-web` w ogóle** (zero trafień w `src`). Wiersz `scheduling_config` (`travel_buffer_minutes`, `default_workday_start/end`, `default_weekdays`) istnieje na żywej bazie od migracji `20260910100000`, ale pierwszy jego konsument dopiero powstanie. Nie zakładaj, że jest helper do odczytu konfiguracji.

Dopisane 2026-09-10 (WO `CAL-SLOT-ENGINE`):
- **`getEffectiveAvailability` czyta `system_config` przy KAŻDYM wywołaniu** (`findUnique` na `scheduling_config` + `findMany` reguł = 2 zapytania na pracownika). Wywołanie go w pętli po puli to N+1 ukryty w funkcji, która sama w sobie spełnia swoje AC „bez zapytania per dzień". Planując cokolwiek nad tą funkcją, żądaj wyniesienia odczytu konfiguracji.
- **`FIELD-APP-PLAN.md` R5 (granulacja slotów i horyzont) jest do dziś NIEROZSTRZYGNIĘTY** — „Dziś: 2-godzinne okna, 60 dni. Do potwierdzenia jako świadoma decyzja, nie zastana." Ani kontrakt, ani `scheduling_config` nie niosą kroku siatki. Każdy WO na kalendarz musi to wypisać jako decyzję człowieka: wariant „siatka wyprowadzona z duration+buffer" nie rusza kontraktu, wariant „stała granulacja z konfiguracji" wymaga migracji `system_config`.
- **`SLA` ma tylko `AUDITOR_DAILY_CAP`, nie ma odpowiednika dla ekip.** Scope stałej mówi wprost „audytów / audytorowi", więc użycie jej dla puli `CREW` byłoby cichą zmianą znaczenia progu. Limit dzienny dla ekip = nowa stała SLA = okno kontraktowe.

Dopisane 2026-09-10 (WO `FLD-BOOKING-ATOMIC-ASSIGN`):
- **W repo NIE MA żadnego mechanizmu testu z żywym Postgresem.** Zmierzone: jedyny workflow to `.github/workflows/playwright.yml` (build + Playwright dla `apps/b2c-web`, bez `services: postgres`, bez migracji). `vitest.config.ts` nie ma setupu bazy. `supabase/config.toml` istnieje, ale nikt nie startuje lokalnego stacku ani w CI, ani w `scripts/verify.sh` (`:80-82` sprawdza `DATABASE_URL` tylko po to, by POMINĄĆ porównanie schematu). Każde kryterium „test na prawdziwej bazie / dwa równoległe żądania" jest dziś nieosiągalne i musi iść jako WYMAGA DECYZJI z osobnym WO infrastrukturalnym.
- **`ON CONFLICT` nie działa z ograniczeniem wykluczającym** (`bookings_no_overlap_per_resource`) — Postgres wspiera je tylko dla indeksów unikalnych. Jedyna droga to przechwycenie `23P01`.
- **Nieudany INSERT zatruwa otwartą transakcję** (`25P02`), a Prisma `$transaction(async tx => …)` nie daje savepointów. Każdy algorytm „spróbuj kolejnego kandydata po kolizji" musi działać POZA transakcją. To bezpośrednio uderza w `B2C-LEAD-ATOMIC` (cztery zapisy w jednej transakcji) — kandydata trzeba ustalić przed jej otwarciem.
- **`bookings.create` w RBAC to `['admin','dyspozytor']` — nie ma roli klienta.** `booked_by = 'CLIENT'` istnieje w schemacie, ale `ROLES` nie zna klienta. Ścieżka B2C rezerwacji będzie wymagała albo zmiany kontraktu, albo jawnego zapisu „publiczny link na token, poza `can()`".

Dopisane 2026-09-14 (WO `CAL-POOL-AGGREGATE`):
- **Rejestr kłamie o źródle terminów w B2C.** `B2C-BOOKING-SLOT` wskazuje `apps/b2c-web/app/api/calendar/slots/route.ts` jako listę slotów — ten route **nie ma ani jednego konsumenta** (grep `api/calendar/slots` = 0). Realnym źródłem jest Server Action `apps/b2c-web/app/actions/calendar.ts` → `getAvailableSlots()`, odpytująca **Google Calendar** (`freebusy.query`, siatka `TIME_SLOTS` zahardkodowana, horyzont 60 dni), konsumowana przez `Step8Booking.tsx` i `getFomoSlots.ts`. Każde wymaganie „podepnij prawdziwe terminy w B2C" to wymiana źródła prawdy, nie podmiana funkcji.
- **Nie istnieje żaden wzorzec komunikacji `apps/b2c-web` → `apps/b2b-web`** (grep `B2B_URL|NEXT_PUBLIC_B2B|localhost:3001` = 0). `@apps/b2b-web` jest `private` bez `exports`, a `tsconfig` b2c ma tylko `@/*` — import kodu z b2b jest niemożliwy. Każdy WO chcący użyć w B2C logiki z `apps/b2b-web/src/lib/**` musi postawić to jako decyzję (wyniesienie do `packages/`, Route Handler, albo duplikacja), nie zgadnąć.
- **`apps/b2c-web` ma `@repo/database` w `dependencies`, ale nie importuje go nigdzie.** Prisma jest tam technicznie dostępna — argument „B2C nie ma Prismy" jest fałszywy, prawdziwy argument to ADR-001.
- **`B2C-RLS-PUBLIC` AC3 rozstrzyga za nas część decyzji architektonicznych:** rola `anon` nie ma prawa czytać `bookings`. Każdy publiczny odczyt terminów MUSI iść serwerowo; wariant „supabase-js z kluczem anonimowym" jest odrzucony na poziomie rejestru.
- **`MAX_ALTERNATIVES = 5` w `create-booking.ts` to literał prezentacyjny, NIE `SLA.AUDITOR_DAILY_CAP.count` (też 5).** Zbieżność przypadkowa — nie „naprawiaj" jej podstawieniem stałej z kontraktu.

Dopisane 2026-09-15 (WO `CAL-SCHEDULING-CONFIG-UI`):
- **„Zakładki" w `settings/SettingsClient.tsx` to trzy martwe przyciski** (`["Ogólne","Zarządzanie Dostępem","Integracje (Stripe)"]`, podświetlenie przez `i === 1`) — zero stanu, zero routingu. Prawdziwy wzorzec zakładek ustawień to POD-TRASY (`/settings/exit-intent`, `/settings/notifications`) spięte przez `subItems` w `(dashboard)/layout.tsx`. Prośba „dodaj zakładkę w ustawieniach" zawsze znaczy nową trasę, nie nowy napis w tablicy.
- **`system_config` NIE jest zasobem RBAC**, a `can()` dla nieznanego zasobu zwraca `'no'` dla KAŻDEJ roli (fail-closed, `generated/rbac.ts:102-107`). Każda Server Action pisząca do `system_config` wymaga więc zmiany kontraktu (jedna linia w `RESOURCES` + `MATRIX`), mimo że tabela istnieje od dawna — sama obecność tabeli i modelu nie znaczy „bez okna kontraktowego". Obejście przez `can()` jest niemożliwe: `tools/kk-authz-gate.mjs` łapie też `$executeRaw`.
- **`system_config.scheduling_config` to JEDEN blob JSONB z dwoma niezależnymi konsumentami**: `travel_buffer_minutes` (`available-slots.ts`, `create-booking.ts`) i `default_workday_start/end` + `default_weekdays` (`effective-availability.ts`). Każdy WO edytujący jedną wartość musi wymusić SCALENIE (`jsonb_set`), nie podmianę `konfiguracja` — naiwny `update` kasuje domyślne okno pracy i wywraca grafik wszystkim bez własnych reguł. Tabela nie ma `updated_at`.
- **Trigger `bookings_pool_matches_basket_trg` pilnuje REZERWACJI, nie koszyka** — zmiana `visit_duration_baskets.pool` nie jest blokowana przez bazę i po cichu rozjeżdża rezerwacje już zawarte. Jedyną obroną jest zamknięta lista pól w Server Action.
- **`bookings.visit_basket_id` przyjmuje wartość tylko w `bookings/actions.ts:31`, a ŻADEN komponent `.tsx` jej nie ustawia** (`grep "basket" apps/b2b-web/src --include=*.tsx` = 0). AC „audytor wybiera koszyk przy wycenie" pozostaje niespełnione niezależnie od prac nad administracją słownika.

Dopisane 2026-09-16 (WO `FNL-2PHASE-ROLLBACK-RELEASE`):
- **Rollback lejka (T10–T13) nie zwalnia ŻADNEJ rezerwacji w `bookings`.** `releaseCrewSlot` operuje wyłącznie na legacy `instalacje` (`status PLANNED -> CANCELLED`, `zespol_id`/`data_planowana` na null) i nie zna ani `bookings`, ani `installation_phases`. Aktywna rezerwacja zostaje i blokuje podmiot przez `bookings_one_active_per_subject` — to jest klasa błędu szersza niż montaż dwuetapowy. Dwa równoległe światy rezerwacji (`instalacje` vs `bookings`) trzeba wypisywać osobno w każdym WO o slotach.
- **Nie ma funkcji `releaseBooking`.** Konwencja zwalniania to wprost `status = 'RELEASED'` na wierszu `bookings`, w tej samej transakcji, przed wstawieniem nowej rezerwacji. Brak kolumny `released_at` — nie wymyślać jej.
- **`assignCrewToLead` reużywa wiersz `instalacje` bez filtra statusu** (`findFirst({ where: { lead_id } })`) i ustawia tylko `zespol_id`. Po rollbacku instalacja zostaje `CANCELLED` na zawsze, mimo powrotu leada do lejka.

- **`bind.transition` w `notifications.contract.mjs` bywa stringiem z pipe'ami** (`'T10|T11|T12|T13'` dla `N_ROLLBACK` i `I4`), nie tablicą. Nie ma w repo helpera, który by to parsował — to ukryta praca w każdej wycenie „podepnij powiadomienia do przejścia".
