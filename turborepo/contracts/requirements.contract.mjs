/**
 * KONTRAKT: Rejestr wymagań (Requirements Registry).
 *
 * Każde wymaganie ma STAŁE ID. To ID pojawia się w:
 *   - tagu testu:      // @REQ: FNL-E5-BYPASS
 *   - nazwie brancha:  feat/FNL-E5-BYPASS-bypass-action
 *   - tytule PR-a i w Work Order agenta
 *
 * Dzięki temu `node tools/kk-trace.mjs` odpowiada na pytanie, na które nie odpowie żaden agent:
 * „czy TO wymaganie faktycznie ma pokrycie w teście, który przechodzi?"
 *
 * status: TODO | IMPLEMENTING | DONE | BLOCKED
 * risk:   LOW | MEDIUM | HIGH   (HIGH => obowiązkowy przegląd człowieka przed merge)
 */

const R = (id, o) => ({ id, status: 'TODO', risk: 'MEDIUM', ...o });

export const REQUIREMENTS = [
  // ───────────────────────── LEJEK ─────────────────────────
  R('FNL-E1-E2', { source: 'b2b_funnel_process.md#etap-1', domain: 'funnel', statement: 'Administrator ręcznie przypisuje audytora, co przenosi leada z E1 do E2.', acceptance: ['Przypisanie zapisuje audytor_id i zmienia status na AWAITING_AUDIT', 'Blokada gdy audytor nieaktywny lub ma nieważne certyfikaty', 'Blokada gdy przekroczony dzienny cap audytora', 'Kolejkowane są N1 (klient) oraz I5 (audytor)'], risk: 'HIGH' }),
  R('FNL-E2-E3', { source: 'b2b_funnel_process.md#etap-2', domain: 'funnel', statement: 'Wysłanie wyceny z Field App automatycznie przenosi leada do E3 (auto-transition).', acceptance: ['Utworzenie quote ustawia status_akceptacji=SENT i wazna_do = now + QUOTE_VALIDITY', 'Status leada zmienia się bez akcji dyspozytora', 'Kolejkowane jest N4 z linkiem akceptacyjnym'], risk: 'HIGH' }),
  R('FNL-E3-E4', { source: 'b2b_funnel_process.md#etap-3', domain: 'funnel', statement: 'Klient akceptuje wycenę i rezerwuje termin montażu, co przenosi leada do E4.', acceptance: ['Wymagana akceptacja regulaminu', 'Rezerwacja tworzy rekord w bookings ze statusem RESERVED', 'Atomowość gwarantuje unikalny indeks częściowy w bazie, nie sprawdzenie w kodzie — test uruchamia dwa równoległe żądania na ten sam slot i oczekuje dokładnie jednego sukcesu', 'Kolejkowane jest I2 do dyspozytora'], risk: 'HIGH' }),
  R('FNL-E3-BUCKET', { source: 'b2b_funnel_process.md#bucket-wyceny-odrzucone', domain: 'funnel', statement: 'Brak akceptacji wyceny po 14 dniach automatycznie przenosi leada do bucketu QUOTE_REJECTED.', acceptance: ['pg_cron uruchamiany codziennie', 'Ustawiane jest bucket_entered_at', 'Kolejkowane jest N_REJECT', 'Job jest idempotentny — dwukrotne uruchomienie nie duplikuje powiadomień'], risk: 'HIGH' }),
  R('FNL-E4-E5', { source: 'b2b_funnel_process.md#etap-4', domain: 'funnel', statement: 'Administrator przypisuje ekipę (Crew_ID), lead przechodzi do E5.', acceptance: ['Ekipa z nieważnym certyfikatem nie pojawia się na liście wyboru', 'Kolejkowane jest I3 do ekipy', 'Tworzony jest rekord wysyłki'], risk: 'HIGH' }),
  R('FNL-E5-E6', { source: 'b2b_funnel_process.md#etap-5', domain: 'logistics', statement: 'Akcja „Wysłano kurierem" z Tracking ID przenosi leada do E6.', acceptance: ['Brak tracking_id blokuje akcję', 'Kolejkowane jest N5 z numerem przesyłki'] }),
  R('FNL-E5-BYPASS', { source: 'b2b_funnel_process.md#etap-5', domain: 'logistics', statement: 'Akcja „Dostawa z ekipą" pomija E6 i przenosi leada bezpośrednio do E7.', acceptance: ['E6 zostaje pominięte', 'Nie powstaje rekord przesyłki kurierskiej', 'Nie wysyła się N5'] }),
  R('FNL-E6-E7', { source: 'b2b_funnel_process.md#etap-6', domain: 'logistics', statement: 'Webhook kuriera „Doręczono" lub ręczna akcja dyspozytora przenosi leada do E7.', acceptance: ['Webhook jest idempotentny po tracking_id', 'Nieznany tracking_id trafia do dead-letter, nie rzuca 500', 'Ręczna akcja daje identyczny efekt jak webhook'], risk: 'HIGH' }),
  R('FNL-E7-E8', { source: 'b2b_funnel_process.md#etap-7', domain: 'funnel', statement: 'Monter zamyka montaż w Field App, lead przechodzi do E8.', acceptance: ['Wyliczane jest next_service_date = data_zakonczenia + 1 rok', 'Kolejkowane jest N8 z 3 załącznikami', 'Widok Instalacji w B2B odświeża status'], risk: 'HIGH' }),
  R('FNL-ROLLBACK', { source: 'b2b_funnel_process.md#rollback-engine', domain: 'logistics', statement: 'Lead z etapów E4–E7 może trafić do bucketu ROLLBACK_RESCHEDULING.', acceptance: ['Slot kalendarza ekipy zostaje zwolniony', 'SLA logistyczne zostaje wstrzymane', 'Kolejkowane są N_ROLLBACK (klient) i I4 (dyspozytor)'], risk: 'HIGH' }),
  R('FNL-ROLLBACK-EXIT', { source: 'b2b_funnel_process.md#rollback-engine', domain: 'logistics', statement: 'Wybór nowego terminu przez klienta zwraca leada do E4.', acceptance: ['Nowy slot jest rezerwowany atomowo', 'Lead wraca dokładnie do AWAITING_CREW_ASSIGNMENT, nie do etapu źródłowego'] }),
  R('FNL-NO-ILLEGAL-TRANSITIONS', { source: 'contracts/funnel.contract.mjs', domain: 'funnel', statement: 'Każde przejście stanu nieujęte w kontrakcie musi zostać odrzucone przez warstwę serwerową ORAZ przez bazę.', acceptance: ['Test model-based przechodzi całą macierz stan × akcja', 'Nielegalne przejście zwraca błąd domenowy, nie wyjątek 500', 'Bezpośredni UPDATE w bazie omijający Server Action jest blokowany triggerem'], risk: 'HIGH' }),

  // ───────────────────────── CRM ─────────────────────────
  R('CRM-KLI-AC1', { source: 'b2b_crm_specifications.md#1', domain: 'crm', statement: 'Globalna wyszukiwarka znajduje klienta po imieniu, nazwisku, telefonie lub e-mailu.', acceptance: ['Wyszukiwanie działa na częściowych dopasowaniach', 'Wyniki respektują RLS roli'] }),
  R('CRM-KLI-AC2', { source: 'b2b_crm_specifications.md#1', domain: 'crm', statement: 'Zmiana danych kontaktowych na Karcie 360 propaguje się do aktywnych leadów.', acceptance: ['Leady w stanach terminalnych (E8, buckety) nie są modyfikowane'] }),
  R('CRM-KLI-AC3', { source: 'b2b_crm_specifications.md#1', domain: 'crm', statement: 'Karta 360 ładuje historię i pliki asynchronicznie (lazy loading).', acceptance: ['Pierwszy paint bez oczekiwania na historię', 'Brak N+1 zapytań przy 200 zdarzeniach historii'], risk: 'LOW' }),
  R('CRM-INST-AC1', { source: 'b2b_crm_specifications.md#2', domain: 'crm', statement: 'Widok Instalacji odświeża status, gdy ekipa oznaczy montaż jako zakończony w aplikacji mobilnej.', acceptance: ['Zmiana widoczna bez pełnego przeładowania strony'] }),
  R('CRM-INST-AC2', { source: 'b2b_crm_specifications.md#2', domain: 'crm', statement: 'Dzisiejsze instalacje bez statusu Zakończona po godzinie 16:00 podświetlają się na pomarańczowo.', acceptance: ['Próg godzinowy pochodzi z SLA.INSTALL_DAY_ALERT, nie z literału w komponencie'] }),
  R('CRM-SRV-TRIGGER', { source: 'b2b_crm_specifications.md#3', domain: 'crm', statement: 'Nocny job aktualizuje statusy zbliżających się serwisów i generuje N10 na 30 dni przed terminem.', acceptance: ['Job idempotentny', 'Zaległe serwisy na czerwono, nadchodzące w 30 dni na żółto/pomarańczowo'] }),
  R('CRM-UST-AC1', { source: 'b2b_crm_specifications.md#4', domain: 'crm', statement: 'Usterka o priorytecie Krytyczny natychmiast wysyła PUSH do dyspozytora.', acceptance: ['Powiadomienie omija okno godzinowe kolejki (priorytet krytyczny)'], risk: 'HIGH' }),
  R('CRM-UST-AC2', { source: 'b2b_crm_specifications.md#4', domain: 'crm', statement: 'Formularz usterki wymusza opis problemu i umożliwia załączenie zdjęć/wideo.', acceptance: ['Walidacja Zod po stronie serwera, nie tylko klienta', 'Limit rozmiaru i typu pliku egzekwowany w Storage'] }),
  R('CRM-UST-AC3', { source: 'b2b_crm_specifications.md#4', domain: 'crm', statement: 'Przekroczenie 48h od zgłoszenia bez akcji koloruje wiersz na czerwono.', acceptance: ['Zawieszenie usterki („oczekiwanie na części") wstrzymuje licznik SLA'] }),
  // Zwężone 2026-08-21 (decyzja człowieka z 2026-08-20). Pierwotne kryterium „Zablokowane konto nie loguje się
  // do Field App" opisywało aplikację, która nie istnieje — zbudowana i przetestowana bramka chroni panel B2B.
  // Blokada logowania do aplikacji terenowej żyje odtąd we FLD-AUTH-BLOCKED (status BLOCKED, faza 3).
  R('CRM-AUDYT-AC1', { status: 'DONE', source: 'b2b_crm_specifications.md#5', domain: 'crm', statement: 'Administrator może zablokować lub usunąć konto audytora w panelu B2B; usunięcie wymaga wcześniejszego przepięcia leadów.', acceptance: ['Zablokowane konto audytora nie przechodzi bramki autoryzacyjnej panelu B2B — odmowa mimo poprawnych danych logowania', 'Sprawdzenie blokady dotyczy wyłącznie kont o roli audytor; konto administratora lub dyspozytora o e-mailu pasującym do zablokowanego rekordu audytora przechodzi bramkę', 'Błąd zapytania o blokadę kończy się odmową dostępu (fail-closed), nie cichym przepuszczeniem', 'Zablokowane konto nie występuje w puli wyboru przy przypisywaniu audytora do leada', 'Usunięcie z wiszącymi leadami jest odrzucone z listą blokujących rekordów', 'Blokada jest odwracalna: odblokowanie przywraca dostęp i obecność w puli', 'Bramka logowania do aplikacji terenowej nie należy do tego wymagania — realizuje ją FLD-AUTH-BLOCKED'], risk: 'HIGH' }),
  R('CRM-AUDYT-AC2', { source: 'b2b_crm_specifications.md#5', domain: 'crm', statement: 'Dzienny limit audytów (domyślnie 5) blokuje kolejne przypisania w tym samym dniu.' , acceptance: ['Limit konfigurowalny per audytor', 'Blokada działa też przy równoległych przypisaniach']}),
  R('CRM-AUDYT-AC3', { source: 'b2b_crm_specifications.md#5', domain: 'crm', statement: 'Wygasające uprawnienia F-Gaz i SEP audytora generują alert do administratora.', acceptance: ['Alert I6 na SLA.CERT_EXPIRY_WARNING dni przed datą ważności', 'Ten sam mechanizm obsługuje audytorów i przedstawicieli zespołów — jedna ścieżka, dwa źródła dat'] }),
  // CRM-AUDYT-KARTOTEKA / CRM-ZESP-KARTOTEKA (2026-08-28, WO CRM-KARTOTEKI-CREATE-AND-CREW-ASSIGN, część A).
  // DWA ID, nie cztery — decyzja człowieka z rozmowy 2026-08-28. Tworzenie i edycja współdzielą
  // modal, schemat Zod, mapowanie FormData → Prisma i ścieżkę uploadu, więc rozdzielenie ich na
  // osobne ID rozbiłoby jedno pokrycie na dwa nierozłączne. Warunek postawiony w WO (linia 134)
  // jest spełniony: kryteria akceptacji wymieniają tryb edycji JAWNIE, więc kk-trace nie policzy
  // pokrycia, którego nie ma. Podział przebiega po ENCJI (audytor / zespół), nie po operacji —
  // bo to encje różnią się polami: audytor ma adres z autouzupełnianiem i preferowane_marki,
  // zespół ma liczba_brygad NOT NULL @default(1) i posiada_wiertnice, a żadnego z tych pól
  // nie ma po drugiej stronie.
  // RYZYKO MEDIUM, nie HIGH — świadomie. Za HIGH przemawiają dane finansowe w formularzu
  // (iban, nip) i cicha utrata danych przy okrojonym initialData. Przeciw: operacja jest
  // admin-only w już obowiązującej macierzy (auditors/crews create+update = ['admin'], bez zmiany
  // kontraktu), nie dotyka danych klienta, nie dotyka lejka ani kolejki powiadomień, a najgorszy
  // realny skutek błędu to nadpisanie kartoteki pracownika — odwracalne ręcznie, w przeciwieństwie
  // do SMS-a wysłanego klientowi albo wycieku danych osobowych klienta.
  R('CRM-AUDYT-KARTOTEKA', { status: 'TODO', domain: 'crm', risk: 'MEDIUM', source: 'docs/workorders/CRM-KARTOTEKI-CREATE-AND-CREW-ASSIGN.md (część A + rozszerzenie „edycja", AC-A1..AC-A24) + decyzje człowieka z rozmowy 2026-08-28: D-A1 (react-hook-form + zod + @hookform/resolvers wchodzą do apps/b2b-web), D-A2 (zdjęcie przez Supabase Storage, nie base64), D-A4 (edycja wchodzi do zakresu), R-A3 wariant (b) (osobna akcja getAuditorForEdit(id) z bramką update, zamiast poszerzania listy). Brak źródła w dokumentach architektury — b2b_crm_specifications.md#5 opisuje blokadę i usunięcie audytora (CRM-AUDYT-AC1), nie zakładanie ani edycję kartoteki. R-A1 POTWIERDZONE 2026-08-28 zapytaniem SELECT * FROM storage.buckets: buckety audytorzy i zespoly ISTNIEJĄ. R-A2 POTWIERDZONE tym samym sposobem: storage.objects ma RLS włączone i ZERO polityk, więc upload z przeglądarki dziś nie działa dla nikogo poza service_role — także ten „działający" dla ekipy; odblokowuje go migracja kartoteki_storage_policies (admin-only). KONTEKST NA PRZYSZŁOŚĆ, POZA TYM WYMAGANIEM: człowiek zadeklarował, że w fazie Field App pracownik będzie sam wgrywał własne zdjęcie do TEGO SAMEGO bucketu — rozszerzenie polityki storage.objects o ścieżkę samoobsługową będzie miało własne ID i własną migrację. ERRATA A-2 (2026-08-31, sekcja „ERRATA A-2" tego samego WO): pierwotna rejestracja POMINĘŁA kolumny fgaz_valid_until i sep_valid_until — BŁĄD REJESTRACJI WYMAGANIA, nie regresja implementacji. Po stronie audytora NIE MA regresji funkcjonalnej, jest luka danych: getAuditors() nie filtruje po żadnej z tych kolumn, fgaz_valid_until zasila wyłącznie plakietkę „wygasa za N dni" w auditors-client.tsx, a sep_valid_until audytora nie jest dziś czytane NIGDZIE. Decyzje człowieka z 2026-08-31 identyczne jak przy CRM-ZESP-KARTOTEKA: data w przeszłości dozwolona, brak ograniczenia górnej granicy roku. Kolumny już istnieją (DateTime? @db.Date) — bez zmiany schematu, migracji i macierzy uprawnień.', statement: 'Administrator zakłada i edytuje kartotekę audytora z panelu B2B: jeden formularz w dwóch rozłącznych trybach, komplet 14 pól kartoteki (w tym fgaz_valid_until i sep_valid_until — daty ważności certyfikatów), bez pól administracyjnych is_active i leave_status.', acceptance: [
    'Przycisk „Dodaj Audytora" otwiera modal formularza — w całej ścieżce tego przycisku nie pada alert („Dodawanie w Fazie 2"), a modal przestaje być martwym kodem bez importu (AC-A1)',
    'Zapis poprawnie wypełnionego formularza tworzy audytora widocznego na liście /auditors po odświeżeniu, bez ręcznego wejścia do bazy (AC-A3)',
    'Nowo utworzony aktywny audytor pojawia się w puli przypisania do leada (getAuditors() z leads/actions.ts) bez restartu aplikacji; pula pozostaje zawężona zgodnie z SEC-ASSIGNMENT-POOL-MINIMIZE — nowy konsument nie rozszerza zestawu pól (AC-A8)',
    'Kliknięcie „Edytuj Audytora" otwiera ten sam modal z WSZYSTKIMI 14 polami wypełnionymi wartościami z bazy; w tej ścieżce nie pada alert („Wkrótce w Fazie 2") (AC-A11)',
    'Dane do formularza edycji pochodzą z NOWEJ akcji getAuditorForEdit(id) z bramką can(rola, auditors, update), pobierającej JEDEN rekord — nie z AuditorSummary używanego przez listę, któremu brakuje co najmniej 7 z 14 pól formularza (adres, nazwa_firmy, nip, doswiadczenie_hvac_lata, kod_pocztowy_bazowy, iban, zdjecie_url) — a po erracie A-2 także sep_valid_until, którego AuditorSummary nie pobiera. Otwarcie edycji na obiekcie listy pokazałoby te pola puste i zapisało pustkę do bazy',
    'Zapis zmienionego formularza zmienia rekord w miejscu: po odświeżeniu widać nową wartość, a id rekordu jest to samo — edycja, nie „usuń i utwórz" (AC-A12)',
    'Otwarcie formularza edycji i zapis BEZ ZMIANY czegokolwiek zostawia wszystkie 14 kolumn nietknięte; w szczególności iban, nip, kod_pocztowy_bazowy oraz fgaz_valid_until i sep_valid_until mają po zapisie tę samą wartość co przed (AC-A13 rozszerzone erratą A-2 o AC-E3 — to jest test na okrojony initialData, który przechodzi AC-A12 i wywraca się dopiero tutaj)',
    'Payload listy audytorów dostarczony do przeglądarki nie zawiera iban ani nip żadnego pracownika, pod żadną nazwą pola — zawężenie stoi w select zapytania, nie w map po nim (AC-A16)',
    'Wywołanie akcji tworzącej lub edytującej z rolą dyspozytor, audytor albo monter kończy się odmową: zero wiersza przy tworzeniu, zero zmienionych kolumn przy edycji. Rola pochodzi z getCurrentActorRole() (sesja serwera), podanie roli w argumencie wywołania nie zmienia wyniku. Dyspozytor jest osobnym przypadkiem testowym, bo ma read na tym zasobie i widzi listę (AC-A5, AC-A14)',
    'Akcje zwracają rozróżnialny wynik { success, error }: odmowa nie zamyka modala i nie pokazuje komunikatu sukcesu (AC-A6)',
    'UI nie pokazuje pozycji „Dodaj" ani „Edytuj" roli bez odpowiedniej zdolności — serwer i tak odrzuca, chodzi o to, żeby interfejs nie kłamał (AC-A15)',
    'Formularz bez imie_i_nazwisko nie tworzy i nie zmienia wiersza — walidacja Zod odrzuca PO STRONIE SERWERA, niezależnie od atrybutu required w HTML (AC-A7)',
    'E-mail zajęty przez INNY rekord kończy się czytelnym błędem domenowym, nie surowym wyjątkiem Prismy P2002; przy edycji rekord pozostaje niezmieniony w całości, nie tylko w kolumnie email (AC-A9, AC-A17)',
    'Zapis formularza edycji z NIEZMIENIONYM WŁASNYM e-mailem kończy się sukcesem — naiwne sprawdzenie wstępne findUnique({ where: { email } }) znajduje własny rekord i fałszywie odrzuca każdą edycję; jeśli sprawdzenie wstępne w ogóle istnieje, musi wykluczać id edytowanego rekordu (AC-A18)',
    'Pusty e-mail zapisuje się jako NULL, nigdy jako pusty łańcuch: dwóch audytorów bez e-maila współistnieje przy tworzeniu, a wyczyszczenie e-maila w edycji daje ten sam wynik (kolumna jest NULL-owalna i @unique jednocześnie, więc drugi łańcuch pusty wysadza zapis) (AC-A10, AC-A19)',
    'Puste pole liczbowe z FormData daje NULL, nigdy NaN (Prisma odrzuci) i nigdy 0 (cicha zmiana danych): dotyczy doswiadczenie_hvac_lata i max_promien_dojazdu_km, w obu trybach',
    'Checkbox przychodzi z FormData jako łańcuch true/false, a łańcuch false jest w JS prawdziwy: odznaczone uprawnienia_sep zapisuje wartość logiczną false. Test odznaczenia wcześniej ZAZNACZONEGO pola jest obowiązkowy, bo przy tworzeniu domyślna wartość kolumny (false) maskuje ten błąd',
    'preferowane_marki jedzie z formularza jako JSON w polu tekstowym, a kolumna to String[]: wartość „[]" czyści listę (nie jest ignorowana), a niepoprawny JSON ODRZUCA zapis zamiast zapisać pustą tablicę po cichu — przy edycji to byłaby cicha utrata danych',
    'Zdjęcie audytora idzie przez Supabase Storage do bucketu audytorzy (bucket istnieje, potwierdzone zapytaniem 2026-08-28), a do kolumny zdjecie_url trafia ŚCIEŻKA obiektu; base64 nie występuje w żadnym trybie, bo odczyt w leads/[id]/page.tsx podaje tę wartość do signStoragePaths("audytorzy", …) i base64 by go rozwalił (D-A2)',
    'Upload jest ADMIN-ONLY, egzekwowany politykami INSERT/UPDATE na storage.objects (migracja kartoteki_storage_policies), a nie samym ukryciem kontrolki: rola bez admin nie wgra pliku nawet z pominięciem UI. Samoobsługowe wgranie własnego zdjęcia przez pracownika z aplikacji terenowej NIE należy do tego wymagania i nie jest w nim projektowane',
    'Zapis formularza edycji BEZ dotykania zdjęcia zostawia zdjecie_url bez zmian — w szczególności nie ustawia NULL dlatego, że pole pliku było puste (AC-A21). Nieudany upload nie zapisuje rekordu z pustą ani błędną ścieżką; wariant „undefined przemycone do update" jest niedopuszczalny w obu trybach (AC-A22). Podmiana zdjęcia NIE kasuje starego obiektu w buckecie — test to STWIERDZA, nie naprawia (AC-A20)',
    'is_active i leave_status NIE należą do 14 pól formularza i nie mogą znaleźć się w danych akcji edycji: zapis formularza nie cofa blokady ustawionej równolegle przez toggleAuditorActiveAction. toggleAuditorActiveAction pozostaje jednokolumnowa i nie jest rozszerzana',
    'Edycja nieistniejącego id (rekord usunięty w innej karcie) zwraca czytelny błąd, nie tworzy nowego rekordu i nie kończy się błędem 500 (AC-A23)',
    'Tryb tworzenia i tryb edycji są rozłączne: submit bez initialData wywołuje wyłącznie akcję create, submit z initialData wyłącznie akcję update; zapis w trybie edycji nie tworzy drugiego rekordu (AC-A24)',
    'Podwójne kliknięcie „Zapisz" nie tworzy dwóch rekordów ani nie wykonuje dwóch zapisów — w obu trybach',
    'ERRATA A-2: formularz audytora ma pole daty ważności F-Gaz (fgaz_valid_until) i pole daty ważności SEP (sep_valid_until), oba w trybie tworzenia I edycji, obok istniejących pól certyfikat_fgaz i uprawnienia_sep — dla kompletności kartoteki i spójności z kartoteką ekipy',
    'ERRATA A-2 (AC-E6), KRYTERIUM ZABEZPIECZAJĄCE: dodanie tych pól NIE tworzy żadnej nowej bramki walidacyjnej przy przypisywaniu audytora. Audytor utworzony z pustymi datami ważności NADAL trafia do puli getAuditors(), która filtruje wyłącznie po is_active i availability_declaration. Nie wolno dopisywać funkcji invalidAuditorCerts ani analogicznego filtra „dla symetrii z ekipą" — takiej reguły biznesowej dziś nie ma i nie ma dla niej źródła w dokumentach. Brak mechanizmu ważności certyfikatu dla audytora jest ŚWIADOMIE ODRĘBNĄ luką, zarejestrowaną osobno poza tym wymaganiem; jej naprawa wymaga decyzji człowieka i własnego ID',
    'ERRATA A-2 (AC-E7): ustawienie audytorowi fgaz_valid_until z formularza zmienia plakietkę „wygasa za N dni" na liście /auditors — pole jest realnie konsumowane, nie tylko zapisywane. sep_valid_until audytora jest po erracie WYŁĄCZNIE zapisywane; wyświetlanie go gdziekolwiek w UI pozostaje poza zakresem',
    'ERRATA A-2 (AC-E4/AC-E5), identycznie jak przy CRM-ZESP-KARTOTEKA: pusty łańcuch z <input type="date"> → NULL (nigdy Invalid Date, nigdy 1970), poprawna data → DateTime w północ UTC dnia kalendarzowego (new Date("YYYY-MM-DD"), nie czas lokalny przeglądarki), wyczyszczenie daty w edycji faktycznie ustawia NULL',
    'ERRATA A-2, DECYZJE CZŁOWIEKA 2026-08-31: data w przeszłości w polu ważności certyfikatu jest DOZWOLONA i zapisuje się bez błędu walidacji (rekord dokumentuje stan faktyczny; nikt nie ma tego „naprawiać" na twardą walidację), oraz BRAK dodatkowego ograniczenia górnej granicy roku poza naturalną walidacją formatu daty. Format spoza YYYY-MM-DD przesłany z pominięciem kontrolki jest odrzucany po stronie serwera z zerem zmienionych kolumn',
    'Adres audytora z autouzupełniania zapisuje wyłącznie tekst do kolumny adres: współrzędne NIE są zapisywane, bo tabela audytorzy nie ma na nie kolumn. FLD-GEO-COORDS dotyczy adresów klientów i nie jest tu domykany przy okazji (D-A3 pozostaje nierozstrzygnięte i poza zakresem)',
  ] }),
  R('CRM-ZESP-KARTOTEKA', { status: 'TODO', domain: 'crm', risk: 'MEDIUM', source: 'docs/workorders/CRM-KARTOTEKI-CREATE-AND-CREW-ASSIGN.md (część A + rozszerzenie „edycja", AC-A1..AC-A24) + te same decyzje człowieka z 2026-08-28 co przy CRM-AUDYT-KARTOTEKA (D-A1, D-A2, D-A4, R-A3 wariant b). Brak źródła w dokumentach architektury — b2b_crm_specifications.md#6 opisuje certyfikaty i pulę w E4 (CRM-ZESP-AC1..AC3), nie zakładanie ani edycję kartoteki. Bucket zespoly ISTNIEJE (potwierdzone zapytaniem SELECT * FROM storage.buckets, 2026-08-28), ale upload z przeglądarki dziś fizycznie nie działa: storage.objects ma RLS włączone i zero polityk — dotyczy to także istniejącego, uchodzącego za działający wzorca crews-client.tsx. Odblokowuje go migracja kartoteki_storage_policies, admin-only. Przyszła samoobsługa pracownika z Field App do tego samego bucketu: osobne ID, osobna migracja. CZĘŚĆ B tego WO (przypisywanie ekipy do leada poza E4) jest ŚWIADOMIE ODŁOŻONA — nie ma i nie ma mieć ID. ERRATA A-2 (2026-08-31, ta sama sekcja WO): pierwotna rejestracja tego wymagania POMINĘŁA kolumny fgaz_valid_until i sep_valid_until — jest to BŁĄD REJESTRACJI WYMAGANIA, nie regresja implementacji; implementer zbudował dokładnie to, co było zapisane. Skutek potwierdzony na żywej bazie 2026-08-31: jedyna istniejąca ekipa („Ekipa Eweliny") ma obie kolumny NULL i jest przez to trwale wykluczona z puli przypisania, bo isCertValidForDate traktuje NULL jako nieważny (fail-closed, D6). Decyzje człowieka z 2026-08-31: data w przeszłości w polu ważności certyfikatu jest DOZWOLONA (rekord dokumentuje stan faktyczny), brak dodatkowego ograniczenia górnej granicy roku poza naturalną walidacją formatu daty. Kolumny już istnieją w schemacie (DateTime? @db.Date), więc errata NIE pociąga zmiany schema.prisma, migracji ani macierzy uprawnień.', statement: 'Administrator zakłada i edytuje kartotekę zespołu montażowego z panelu B2B: jeden formularz w dwóch rozłącznych trybach, komplet 14 pól kartoteki (w tym fgaz_valid_until i sep_valid_until — daty ważności certyfikatów), bez pól administracyjnych aktywny i leave_status.', acceptance: [
    'Przycisk „Dodaj Zespół" w widoku /crews otwiera modal formularza ekipy — woła komponent modala, a nie alert („Dodawanie w Fazie 2"); dziś przycisk i modal leżą w różnych katalogach i nigdy się nie widziały (AC-A2)',
    'Zapis poprawnie wypełnionego formularza tworzy ekipę widoczną na liście /crews po odświeżeniu (AC-A4)',
    'Kliknięcie „Edytuj Zespół" otwiera ten sam modal z WSZYSTKIMI 14 polami wypełnionymi wartościami z bazy; w tej ścieżce nie pada alert („Wkrótce w Fazie 2") (AC-A11)',
    'Dane do formularza edycji pochodzą z NOWEJ akcji getCrewForEdit(id) z bramką can(rola, crews, update), pobierającej JEDEN rekord — nie z CrewSummary używanego przez listę, któremu brakuje co najmniej 5 z 14 pól formularza (email, nip, kod_pocztowy_bazowy, posiada_wiertnice, iban) — a po erracie A-2 także obu dat ważności, jeśli CrewSummary ich nie pobiera. Otwarcie edycji na obiekcie listy pokazałoby te pola puste i zapisało pustkę do bazy',
    'Zapis zmienionego formularza zmienia rekord w miejscu: po odświeżeniu widać nową wartość, a id rekordu jest to samo (AC-A12)',
    'Otwarcie formularza edycji i zapis BEZ ZMIANY czegokolwiek zostawia wszystkie 14 kolumn nietknięte; w szczególności iban, nip, kod_pocztowy_bazowy oraz fgaz_valid_until i sep_valid_until mają po zapisie tę samą wartość co przed (AC-A13 rozszerzone erratą A-2 o AC-E3)',
    'Payload listy ekip dostarczony do przeglądarki nie zawiera iban ani nip żadnego pracownika, pod żadną nazwą pola — zawężenie stoi w select zapytania, nie w map po nim (AC-A16)',
    'Wywołanie akcji tworzącej lub edytującej z rolą dyspozytor, audytor albo monter kończy się odmową: zero wiersza przy tworzeniu, zero zmienionych kolumn przy edycji. Rola z getCurrentActorRole(), nigdy z argumentu. Dyspozytor osobnym przypadkiem, bo ma read i widzi listę (AC-A5, AC-A14)',
    'Akcje zwracają rozróżnialny wynik { success, error }: odmowa nie zamyka modala i nie pokazuje komunikatu sukcesu (AC-A6)',
    'CrewsClient otrzymuje actorRole z crews/page.tsx — dziś dostaje wyłącznie initialCrews — i warunkuje nim pozycje „Dodaj Zespół" oraz „Edytuj Zespół" (AC-A15). Naprawa nieuwarunkowanej pozycji „Usuń (Tylko Admin)" nie należy do tego wymagania, ale prop, którego jej brakuje, powstaje właśnie tutaj',
    'Formularz bez nazwy ekipy nie tworzy i nie zmienia wiersza — walidacja Zod odrzuca PO STRONIE SERWERA, niezależnie od atrybutu required w HTML (AC-A7)',
    'E-mail zajęty przez INNY rekord kończy się czytelnym błędem domenowym, nie surowym wyjątkiem Prismy P2002; przy edycji rekord pozostaje niezmieniony w całości (AC-A9, AC-A17)',
    'Zapis formularza edycji z NIEZMIENIONYM WŁASNYM e-mailem kończy się sukcesem; sprawdzenie wstępne, jeśli istnieje, musi wykluczać id edytowanego rekordu (AC-A18)',
    'Pusty e-mail zapisuje się jako NULL, nigdy jako pusty łańcuch — dwie ekipy bez e-maila współistnieją, a wyczyszczenie e-maila w edycji daje ten sam wynik (AC-A10, AC-A19)',
    'Puste pole liczbowe z FormData daje NULL dla promien_dzialania_km, nigdy NaN i nigdy 0 (promień 0 km znaczy „ekipa nigdzie nie dojedzie" i wypada z filtrów). WYJĄTEK, który odróżnia zespół od audytora: liczba_brygad jest NOT NULL z @default(1), więc puste pole musi dać 1, nie NULL',
    'Checkbox przychodzi z FormData jako łańcuch true/false, a łańcuch false jest w JS prawdziwy: odznaczone uprawnienia_sep i posiada_wiertnice zapisują wartość logiczną false. Test odznaczenia wcześniej ZAZNACZONEGO pola jest obowiązkowy w trybie edycji',
    'Formularz zespołu NIE ma pola adresu z autouzupełnianiem ani pola preferowanych marek — tabela zespoly_monterskie nie ma odpowiedników tych kolumn (są wyłącznie po stronie audytora). Kryteria adresowe i markowe z CRM-AUDYT-KARTOTEKA nie mają tu zastosowania i nie wolno ich dopisywać „dla symetrii"',
    'Zdjęcie ekipy idzie przez Supabase Storage do bucketu zespoly, a do kolumny zdjecie_url trafia ŚCIEŻKA obiektu, nie base64 (D-A2). Nazwa pliku pozostaje unikalna (zawiera znacznik czasu), więc obiekt nigdy nie jest nadpisywany',
    'Upload jest ADMIN-ONLY, egzekwowany politykami INSERT/UPDATE na storage.objects (migracja kartoteki_storage_policies), nie samym ukryciem kontrolki. Samoobsługowe wgranie własnego zdjęcia przez pracownika z aplikacji terenowej NIE należy do tego wymagania',
    'Zapis formularza edycji BEZ dotykania zdjęcia zostawia zdjecie_url bez zmian i nie ustawia NULL dlatego, że pole pliku było puste (AC-A21). Nieudany upload nie zapisuje pustej ani błędnej ścieżki (AC-A22). Stary obiekt w buckecie nie jest kasowany — test to STWIERDZA, nie naprawia (AC-A20)',
    'updateCrewAvatar pozostaje jednokolumnowa i nie jest rozszerzana o pozostałe pola: jest wołana z menu „Wgraj zdjęcie" bez otwierania formularza i nie zna reszty kartoteki',
    'aktywny i leave_status NIE należą do 14 pól formularza i nie mogą znaleźć się w danych akcji edycji — dostępność i status kadrowy mają własne, wąskie ścieżki i mają je zachować',
    'Edycja nieistniejącego id (ekipa usunięta w innej karcie) zwraca czytelny błąd, nie tworzy nowego rekordu i nie kończy się błędem 500 (AC-A23)',
    'Tryb tworzenia i tryb edycji są rozłączne: submit bez initialData wywołuje wyłącznie create, submit z initialData wyłącznie update; zapis w edycji nie tworzy drugiego rekordu (AC-A24)',
    'Podwójne kliknięcie „Zapisz" nie tworzy dwóch rekordów ani nie wykonuje dwóch zapisów — w obu trybach',
    'ERRATA A-2: formularz ekipy ma pole daty ważności F-Gaz (fgaz_valid_until) i pole daty ważności SEP (sep_valid_until), oba w trybie tworzenia I edycji, umieszczone obok odpowiadających im pól certyfikat_fgaz i uprawnienia_sep (numer/flaga + data to logiczna para). Bez nich każda nowo założona ekipa ma obie kolumny NULL i jest trwale wykluczona z puli przypisania',
    'ERRATA A-2 (AC-E1/AC-E2): ekipa utworzona z formularza z obiema datami w przyszłości pojawia się w puli getCrews(installationDate) i daje się przypisać BEZ ręcznego UPDATE w bazie; ekipa z pustymi polami dat nie pojawia się w puli, a próba przypisania zwraca błąd wymieniający oba certyfikaty. Semantyka NULL = nieważny (fail-closed) pozostaje NIEZMIENIONA — errata ją dokumentuje, nie odwraca',
    'ERRATA A-2 (AC-E4): pusty łańcuch z <input type="date"> zapisuje się jako NULL — nigdy new Date("") (Invalid Date, odrzucane przez Prismę runtime\'owo) ani new Date(0) (1970, czyli po cichu certyfikat wygasły ponad pół wieku temu). Dotyczy obu kolumn i obu trybów; wyczyszczenie daty w edycji ma faktycznie ustawić NULL, a nie zostawić poprzednią wartość',
    'ERRATA A-2 (AC-E5): poprawna data zapisuje się jako DateTime w PÓŁNOC UTC danego dnia kalendarzowego, nie w czasie lokalnym przeglądarki ani serwera — new Date("2027-01-01") daje północ UTC i jest poprawne, new Date("2027-01-01T00:00:00") (bez Z) przy dodatnim offsecie cofa datę o dzień. Powód: isCertValidForDate porównuje dateOnlyIso(validUntil) liczone po UTC. Data 2027-01-01 wpisana przy serwerze w Europe/Warsaw odczytuje się z bazy jako 2027-01-01, nie 2026-12-31',
    'ERRATA A-2, DECYZJA CZŁOWIEKA 2026-08-31 — data w przeszłości w polu ważności certyfikatu jest DOZWOLONA i zapisuje się bez błędu walidacji. Uzasadnienie: rekord dokumentuje stan faktyczny, a wygasły certyfikat jest stanem faktycznym; skutek (wypadnięcie z puli) realizuje invalidCrewCerts, nie walidator formularza. Zablokowanie zapisu uniemożliwiłoby wprowadzenie zaległych danych i wymusiło obejście przez bazę. Zapisane jawnie, żeby nikt nie „naprawił" tego na twardą walidację. Ostrzeżenie NIEBLOKUJĄCE w UI jest dopuszczalne',
    'ERRATA A-2, DECYZJA CZŁOWIEKA 2026-08-31 — BRAK dodatkowego ograniczenia górnej granicy roku dla dat ważności (żadnego min/max poza naturalną walidacją formatu daty YYYY-MM-DD). Format spoza YYYY-MM-DD, przesłany z pominięciem kontrolki, ma być odrzucony po stronie serwera z zerem zmienionych kolumn',
    'Istniejąca ścieżka przypisania ekipy w E4 (assignCrewToLead, T05) pozostaje nietknięta: to wymaganie dotyczy wyłącznie kartoteki. Przypisywanie ekipy do leada poza E4 (część B tego WO) jest świadomie odłożone i nie ma zarejestrowanego ID',
  ] }),
  R('CRM-ZESP-AC1', { source: 'b2b_crm_specifications.md#6', domain: 'crm', statement: 'Powiadomienie do administratora na 30 dni przed wygaśnięciem certyfikatu przedstawiciela zespołu.', acceptance: ['Alert I6 na SLA.CERT_EXPIRY_WARNING dni przed fgaz_valid_until lub sep_valid_until', 'Zmienna assignee w szablonie zawiera nazwę zespołu i imię przedstawiciela', 'Dwa certyfikaty wygasające w tym samym oknie generują dwa osobne alerty, nie jeden zbiorczy', 'Alert nie powtarza się przy każdym przebiegu crona'], risk: 'MEDIUM' }),
  R('CRM-ZESP-AC2', { status: 'DONE', source: 'b2b_crm_specifications.md#6', domain: 'crm', statement: 'Zespół z nieważnym certyfikatem jest automatycznie ukrywany z puli brygad przy przypisywaniu w E4.', acceptance: ['Ukrycie w UI ORAZ odrzucenie po stronie serwera przy próbie ręcznego wymuszenia'], risk: 'HIGH' }),
  R('CRM-ZESP-AC3', { source: 'b2b_crm_specifications.md#6', domain: 'crm', statement: 'Profil zespołu jest zintegrowany z kalendarzem rezerwacyjnym — dostępne terminy klienta zależą od dostępności ekipy.', acceptance: ['Wpis w absences nakładający się na slot usuwa go z widoku klienta', 'Blokada z reason VEHICLE_FAILURE działa tak samo jak urlop', 'Istniejące bookings w oknie absencji są raportowane dyspozytorowi, a nie kasowane po cichu'], risk: 'HIGH' }),
  R('CRM-ZIMNE-AC1', { source: 'b2b_crm_specifications.md#7', domain: 'crm', statement: 'Tabela zimnych leadów sortuje domyślnie od najświeższych wejść do bucketu.' , risk: 'LOW', acceptance: ['Sortowanie po bucket_entered_at DESC']}),
  R('CRM-ZIMNE-AC2', { source: 'b2b_crm_specifications.md#7', domain: 'crm', statement: 'Akcja „Zwróć do obiegu" wymaga potwierdzenia lub odświeżenia ceny po 30 dniach w bucketcie.', acceptance: ['Przejście T15 istnieje w kontrakcie i jest egzekwowane', 'Wiek wyceny liczony od quoted_at (D3), nie od bucket_entered_at, względem SLA.COLD_LEAD_REPRICE_DAYS', 'Wycena świeższa niż próg przechodzi bez dialogu i bez dodatkowego potwierdzenia', 'Wycena starsza niż próg NIE jest odrzucana twardo — guard quoteRefreshedIfStale wymaga jednej z dwóch jawnych decyzji: (a) potwierdzenie powrotu ze starą ceną (acknowledgeStaleQuote), (b) odświeżenie ceny i kontynuacja (do:refreshQuoteValidity)', 'Brak którejkolwiek z tych dwóch decyzji oznacza odmowę — lead zostaje w QUOTE_REJECTED (samo pominięcie dialogu nie przepuszcza)', 'Guard żyje na serwerze: żądanie do Server Action z pominięciem UI jest oceniane tak samo', 'Test sprawdza trzy ścieżki: cena świeża, cena przeterminowana z potwierdzeniem, cena przeterminowana bez żadnej decyzji'], status: 'DONE', risk: 'HIGH' }),
  R('CRM-ZIMNE-AC3', { source: 'b2b_crm_specifications.md#7', domain: 'crm', statement: 'Trwała archiwizacja wymaga powodu utraty, który zasila moduł analityczny.', acceptance: ['lost_reason ze słownika zamkniętego', 'Stan ARCHIVED_LOST istnieje w enumie', 'Guard lostReasonProvided odrzuca archiwizację bez powodu', 'ARCHIVED_LOST jest terminalny — brak przejścia wychodzącego w maszynie stanów'], status: 'DONE', risk: 'HIGH' }),
  R('CRM-BOOK-HISTORY', { source: 'ADR-012', domain: 'crm', statement: 'Zmiana terminu zachowuje historię — poprzednia rezerwacja nie jest nadpisywana.', acceptance: ['Przełożenie tworzy nowy rekord bookings z wypełnionym reschedule_of', 'Poprzedni rekord dostaje status RELEASED', 'Karta 360 pokazuje pełny łańcuch przekładań'], risk: 'MEDIUM' }),
  // AC8 (FLD-AVAILABILITY-SPLIT, 2026-08-21): poprzednia treść odwoływała się do dwóch pól,
  // których nie było w schemacie — daily_audit_cap i availability_status. Po decyzji D-A
  // pole zgrubnej dostępności istnieje, ale nazywa się inaczej i leży w innej tabeli, a limit
  // dzienny pozostaje progiem globalnym. Kryteria wskazują odtąd wyłącznie byty istniejące.
  R('CRM-REGION-AUTO', { source: 'b2b_crm_specifications.md#5', domain: 'crm', statement: 'Audytor jest auto-przypisywany na podstawie kodu pocztowego adresu.', acceptance: ['Kod pocztowy należy do dokładnie jednego regionu (ograniczenie UNIQUE)', 'Brak regionu dla kodu nie blokuje leada, tylko wymaga przypisania ręcznego', 'Dzienny limit audytów pochodzi z globalnego progu SLA.AUDITOR_DAILY_CAP — pole daily_audit_cap per audytor NIE istnieje i jego wprowadzenie jest osobną zmianą schematu (WO FLD-AVAILABILITY-SPLIT, „Poza zakresem")', 'Auto-przypisanie pomija audytora z trzech rozłącznych powodów, sprawdzanych osobno: is_active = false (blokada administracyjna), leave_status inny niż ACTIVE (urlop lub zwolnienie wpisane przez administratora), availability_declarations.is_available = false (własna deklaracja pracownika)', 'Brak wiersza w availability_declarations znaczy „audytor dostępny" — inaczej audytor dodany po migracji nigdy nie trafiłby do puli', 'Komunikat odmowy wskazuje, który z trzech powodów zadziałał — bez tego pytanie „dlaczego on się nie wyświetla" ma trzy miejsca do sprawdzenia i żadnej podpowiedzi (R1 w WO FLD-AVAILABILITY-SPLIT)'], risk: 'MEDIUM' }),
  R('FNL-2PHASE', { source: 'ADR-005', domain: 'funnel', statement: 'Mieszkanie w stanie deweloperskim realizowane jest w dwóch etapach: przygotowanie instalacji przed wykończeniem, montaż jednostek po wykończeniu.', acceptance: ['Deklaracja klienta w Triage ustawia leads.declared_property_condition, ale NIE decyduje o trybie montażu', 'Audytor na miejscu ustawia quotes.installation_type — to jest wartość wiążąca', 'TWO_PHASE tworzy dwa rekordy installation_phases (1 i 2)', 'Guard installationIsTwoPhase blokuje T17 dla montażu jednoetapowego', 'Guard phaseOneNotCompleted uniemożliwia dwukrotne zamknięcie etapu I'], risk: 'HIGH' }),
  R('FNL-2PHASE-BOOKING', { source: 'ADR-005', domain: 'funnel', statement: 'Każdy etap montażu ma własną rezerwację terminu — klient rezerwuje etap II dopiero po zakończeniu etapu I.', acceptance: ['installation_phases.booking_id wskazuje osobną rezerwację dla każdego etapu', 'Link do rezerwacji etapu II jest generowany dopiero przez efekt do:openPhaseTwoBooking', 'Rezerwacja etapu II nie jest możliwa przed zamknięciem etapu I'], risk: 'HIGH' }),
  R('FNL-2PHASE-INVOICE', { source: 'ADR-005', domain: 'funnel', statement: 'Po zakończeniu etapu I klient otrzymuje fakturę za ten etap.', acceptance: ['Efekt do:issuePhaseOneInvoice tworzy rekord w invoices powiązany z installation_phases', 'N8a ma załącznik invoice_phase_1', 'Montaż jednoetapowy nie generuje faktury częściowej'], risk: 'MEDIUM' }),
  R('NTF-PUSH-TOKEN', { source: 'ADR-006', domain: 'notifications', statement: 'Kanał PUSH dostarcza wiadomości na zarejestrowane urządzenia pracowników.', acceptance: ['Push rozsyłany na wszystkie aktywne tokeny użytkownika, nie tylko ostatni', 'Odrzucenie tokena przez dostawcę ustawia is_active = false i nie liczy się jako nieudana próba wysyłki', 'Brak aktywnego tokena kończy wiadomość statusem CANCELLED, nie ERROR — nie ma czego ponawiać', 'PUSH nie jest kierowany do odbiorcy CLIENT'], risk: 'MEDIUM' }),
  R('NTF-I7-SLA', { source: 'b2b_crm_specifications.md#4', domain: 'notifications', statement: 'Usterka krytyczna wyzwala natychmiastowy push do dyspozytora i uruchamia zegar SLA 48h.', acceptance: ['I7 kolejkowane w tej samej transakcji co utworzenie usterki', 'Zegar liczony od created_at usterki, nie od wysyłki powiadomienia', 'Brak reakcji przez SLA.INCIDENT_RESPONSE godzin oznacza wiersz na czerwono w CRM'], risk: 'HIGH' }),
  R('SRV-SOURCE-OF-TRUTH', { source: 'ADR-010', domain: 'service', statement: 'Dopóki klient nie zarezerwuje terminu, obowiązuje wyliczona data należności; po rezerwacji obowiązuje rekord serwisu.', acceptance: ['Cron czyta installations.next_service_date i wysyła N10 na SLA.SERVICE_REMINDER_LEAD dni przed', 'Rezerwacja z linku w N10 tworzy bookings i ustawia services.booking_id, scheduled_date oraz status SCHEDULED', 'Przełożenie wizyty zmienia services i bookings, a NIE next_service_date', 'Zamknięcie serwisu wyznacza nową datę należności na kolejny rok', 'Widok CRM czyta termin z services, gdy rekord istnieje, i z next_service_date, gdy nie istnieje'], risk: 'HIGH' }),
  R('SRV-REMINDER-ONCE', { source: 'ADR-010', domain: 'service', statement: 'Przypomnienie o zbliżającym się serwisie wysyłane jest raz na cykl.', acceptance: ['reminder_sent_at chroni przed powtórną wysyłką w kolejnym przebiegu crona', 'Ponowne uruchomienie crona tego samego dnia nie generuje drugiego N10', 'Zamknięcie serwisu zeruje reminder_sent_at na potrzeby następnego cyklu'], risk: 'MEDIUM' }),
  R('CRM-ZESP-REP', { source: 'ADR-009', domain: 'crm', statement: 'Zespół reprezentuje jedna osoba, która posiada uprawnienia i odpowiada za montaż.', acceptance: ['crews.representative_user_id wskazuje konto w authorized_users z rolą monter', 'Guard crewCertsValid sprawdza certyfikaty przedstawiciela', 'Zespół z wygasłym certyfikatem przedstawiciela jest ukryty w puli na Etapie 4', 'System nie prowadzi ewidencji pozostałych członków brygady — to świadoma decyzja, nie luka'], risk: 'MEDIUM' }),
  // ── Punkt 22 WO BATCH-MEDIUM-LOW-CLEANUP (2026-09-01, contract-steward) ──
  // Wpis zbiorczy ZOSTAJE jako SUPERSEDED — nie usuwamy go, żeby nie zerwać historii ani dopasowań
  // kk-trace do już otagowanych testów (crews-admin-gates.test.ts, leads-delete-admin-only.test.ts).
  // Powód rozbicia: kk-trace dopasowuje ID po komentarzu w teście, nie po zasobie, więc dwa pokryte
  // widoki z siedmiu wyglądały jak pełne pokrycie wymagania o zasięgu „WSZYSTKIE 7 widoków".
  // Liczba 7 POLICZONA RĘCZNIE po ścieżkach `prisma.<tabela>.delete` / `tx.<tabela>.delete`
  // w apps/b2b-web/src/app/(dashboard)/**/actions.ts i zmapowana na RESOURCES z rbac.contract.mjs:
  //   klienci→clients, leady→leads, instalacje→installations, serwisy→services,
  //   usterki_incidents→incidents, audytorzy→auditors, zespoly_monterskie→crews.
  // Ósma ścieżka (settings/actions.ts → authorizedUser → authorized_users) NIE jest widokiem CRM
  // i ma własne wymaganie SEC-AUTHZ-USER-MGMT — świadomie poza tym rozbiciem.
  R('CRM-DELETE-ADMIN-ONLY', { status: 'SUPERSEDED', source: 'b2b_crm_specifications.md#globalne; rozbite 2026-09-01 (WO BATCH-MEDIUM-LOW-CLEANUP, punkt 22) na siedem wymagań per zasób: CRM-DELETE-ADMIN-ONLY-CLIENTS, -LEADS, -INSTALLATIONS, -SERVICES, -INCIDENTS, -AUDITORS, -CREWS. Ten wpis nie jest już samodzielnie egzekwowalny — pokrycie liczy się na wpisach potomnych.', domain: 'security', statement: 'ZASTĄPIONE. Akcja „Usuń" we WSZYSTKICH 7 widokach CRM dostępna wyłącznie dla roli admin. Reguła obowiązuje dalej, ale jej pokrycie jest śledzone per zasób we wpisach potomnych CRM-DELETE-ADMIN-ONLY-<RESOURCE>.', acceptance: ['Wpis nie ma własnych kryteriów — patrz wymagania potomne CRM-DELETE-ADMIN-ONLY-<RESOURCE>'], risk: 'HIGH' }),

  // Siedem wpisów potomnych. Każdy ma kryteria dla TRZECH warstw osobno (UI / Server Action / RLS).
  // Status per wpis odzwierciedla FAKTYCZNE pokrycie testem, nie intencję: dziś żaden z siedmiu
  // zasobów nie ma testu warstwy RLS (DELETE dla roli nie-admin odrzucone przez bazę), bo w tym
  // środowisku nie ma Postgresa — więc żaden nie może być DONE, nawet tam, gdzie Server Action
  // i UI są pokryte. To jest właśnie różnica, którą wpis zbiorczy ukrywał.
  // CLIENTS zastąpione 2026-09-03 (okno SEC-RODO-DELETE-RECONCILE) — jako jedyny z siedmiu.
  // Powód jest wyłącznie ten, że dla zasobu clients operacja „usuń" już nie istnieje: została
  // zastąpiona anonimizacją, a wszystkie trzy warstwy mają dowód pod tagiem następcy.
  // Pozostałe sześć wpisów potomnych ZOSTAJE — dla nich odpowiednika anonimizacji nie ma.
  R('CRM-DELETE-ADMIN-ONLY-CLIENTS', { status: 'SUPERSEDED', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/customers/actions.ts. ERRATA 2026-09-01 (WO CLIENT-ANONYMIZATION-RODO, Faza A punkt A5): kryterium warstwy Server Action mówiło o metodzie delete Prismy na tabeli clients; po Fazie B ta metoda nie jest wywoływana NIGDY (delete zastąpiony anonimizacją przez updateMany), więc kryterium przechodziłoby trywialnie dla każdej roli. ZASTĄPIONE 2026-09-03 decyzją człowieka w oknie kontraktowym SEC-RODO-DELETE-RECONCILE: WO docs/workorders/SEC-AUDIT-COVERAGE-RETAG.md odradzał ten krok wyłącznie z powodu brakującego dowodu warstwy RLS (Blok A3), a dowód powstał — apps/b2b-web/tests/rls-deny-by-default-freeze.test.ts, blok „AC-A3”. Pokrycie liczy się teraz na CRM-CLIENT-ANONYMIZE-RODO.', domain: 'security', statement: 'ZASTĄPIONE przez CRM-CLIENT-ANONYMIZE-RODO. Reguła (usunięcie klienta wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS) obowiązuje dalej i ma dowód w trzech warstwach, ale jej kryteria żyją we wpisie następcy, bo dla zasobu clients samo usunięcie zostało zastąpione anonimizacją. Dowód warstwy RLS jest testem statycznym nad tekstem migracji 20260824185845_security_enable_rls_baseline.sql (RLS włączone, zero polityk DELETE/ALL na tabeli clients), nie próbą na żywej bazie — to świadomie przyjęty poziom dowodu, wynikający z braku Postgresa w tym środowisku.', acceptance: ['Wpis nie ma własnych kryteriów — patrz CRM-CLIENT-ANONYMIZE-RODO'], risk: 'HIGH' }),
  // ── WO CLIENT-ANONYMIZATION-RODO, Faza A (2026-09-01) ──
  // Dwa wpisy, nie jeden, bo to dwa różne profile ryzyka i dwa różne zestawy testów:
  // CRM-CLIENT-ANONYMIZE-RODO jest o TREŚCI operacji (co znika, co zostaje, kto może),
  // SEC-AUDIT-LOG-APPEND-ONLY o NIEZMIENNOŚCI dowodu (i obowiązuje wszystkie sześć operacji
  // z AUDIT_REQUIREMENTS.mustLog, nie tylko anonimizację klienta). Sklejenie ich dawałoby wpis,
  // który przechodzi na wpół.
  R('CRM-CLIENT-ANONYMIZE-RODO', { status: 'TODO', source: 'docs/workorders/CLIENT-ANONYMIZATION-RODO.md (2026-09-01); rbac.contract.mjs DELETE_POLICIES.clients = ANONYMIZE_OR_SET_NULL; schemat: klienci.anonymized_at + audit_log (migracja 20260901220000 — ZASTOSOWANA na żywej bazie; SPROSTOWANIE 2026-09-03: wcześniejsze zdanie „NIE uruchomiona na żywej bazie" było nieaktualne. Podstawą jest bezpośrednie zapytanie do bazy z 2026-09-03, nie ewidencja pliku migracji ani supabase_migrations.schema_migrations — to rozróżnienie jest w tym repozytorium istotne, bo ewidencja myliła w obie strony, patrz SEC-EMAIL-UNIQUE. Odczyt pokazał tabelę audit_log z 9 kolumnami (id, actor_email, actor_role, operation, resource, record_id, justification, legal_basis, created_at; kolumny before_snapshot NIE ma), 4 ograniczenia CHECK w tym audit_log_operation_check dopuszczające wartość delete, wyzwalacz audit_log_append_only_trg oraz 0 wierszy); ścieżka kodu (Faza B): apps/b2b-web/src/app/(dashboard)/customers/actions.ts → anonymizeClientAction', domain: 'security', statement: 'Usunięcie klienta jest realizowane jako ANONIMIZACJA danych osobowych z zachowaniem rekordu i całej historii powiązanej, w jednej transakcji z wpisem w audit_log. Twarde DELETE na tabeli klienci nie istnieje w kodzie aplikacji.', acceptance: ['Po operacji klient ma imie_i_nazwisko = „Klient usunięty", email = null, telefon = null, anonymized_at ustawione; WSZYSTKIE powiązane adresy mają ulica_miasto = „Adres usunięty", latitude = null, longitude = null', 'Identyfikator klienta i liczba wierszy w leady, serwisy, usterki_incidents, adresy wskazujących na tego klienta są niezmienione — żadne klient_id nie zostało wyzerowane', 'Idempotencja: drugie wywołanie zwraca sukces, nie rzuca, nie przesuwa anonymized_at i nie tworzy drugiego wpisu w audit_log; liczba wpisów dla record_id po N wywołaniach wynosi 1', 'Współbieżność rozstrzygnięta w bazie: bramką jest count z updateMany z warunkiem anonymized_at = null, nie odczyt findUnique poprzedzający zapis', 'Atomowość: gdy zapis do audit_log zawiedzie, klient NIE jest zanonimizowany (rollback całej transakcji); nie istnieje ścieżka kodu zmieniająca klienci poza transakcją zawierającą wpis audytowy', 'Bramka roli: wywołanie przez dyspozytor, audytor lub monter jest odrzucone ZANIM powstanie zapytanie (zero wywołań prisma.klienci.updateMany, prisma.adresy.updateMany i zapisu audit_log); wyjątek z getCurrentActorRole() daje odmowę uprawnień, nie generyczny błąd zapisu', 'Uzasadnienie obowiązkowe: justification krótsze niż 10 znaków po trim albo legalBasis spoza AUDIT_REQUIREMENTS.legalBases jest odrzucone bez żadnego zapisu', 'Wpis audytowy ma operation = anonymize, resource = clients, record_id anonimizowanego klienta, actor_email zalogowanego użytkownika (nie null, nie stała) i actor_role z chwili operacji', 'Test statyczny nad apps/: literał klienci.delete nie występuje w żadnym pliku .ts/.tsx poza testami, a nazwa deleteCustomerAction nie jest nigdzie eksportowana ani importowana', 'Test statyczny: żadna funkcja w repozytorium nie przechowuje kopii danych osobowych klienta sprzed anonimizacji'], risk: 'HIGH' }),
  R('SEC-AUDIT-LOG-APPEND-ONLY', { status: 'TODO', source: 'rbac.contract.mjs AUDIT_REQUIREMENTS.appendOnly = true oraz MATRIX audit_log (update: [], delete: []); ADR-008; docs/workorders/CLIENT-ANONYMIZATION-RODO.md, Faza A punkt A1; schemat: model AuditLog, migracja 20260901220000 — ZASTOSOWANA na żywej bazie. SPROSTOWANIE 2026-09-03: wcześniejsze zdanie „NIE uruchomiona na żywej bazie" było nieaktualne. Podstawa: bezpośrednie zapytanie do bazy z 2026-09-03, nie ewidencja pliku migracji (tabela audit_log z 9 kolumnami, 4 ograniczenia CHECK w tym audit_log_operation_check dopuszczające delete, wyzwalacz audit_log_append_only_trg, 0 wierszy)', domain: 'security', statement: 'Wpisu w audit_log nie da się zmienić ani usunąć ŻADNĄ ścieżką dostępną aplikacji — łącznie z rolą admin i łącznie z zapisem przez Prismę, która omija RLS. Sprostowanie błędnego wpisu jest nowym wierszem.', acceptance: ['UPDATE na audit_log wykonany połączeniem omijającym RLS (ścieżka Prismy) jest odrzucony przez wyzwalacz audit_log_append_only_trg', 'DELETE na audit_log wykonany połączeniem omijającym RLS jest odrzucony przez ten sam wyzwalacz — samo RLS nie wystarcza, bo Prisma go omija', 'RLS jest włączone na tabeli audit_log i nie istnieje polityka UPDATE ani DELETE dla żadnej roli, łącznie z admin', 'MATRIX dla zasobu audit_log ma update: [] i delete: [] — test kontraktowy wykrywa nadanie tych zdolności komukolwiek', 'Baza odrzuca wpis niekompletny: operation spoza AUDIT_REQUIREMENTS.mustLog, resource spoza RESOURCES, legal_basis spoza legalBases oraz justification krótsze niż 10 znaków po btrim', 'Poziom dowodu warstwy bazy (wyzwalacz, RLS, CHECK) — SPROSTOWANIE 2026-09-03. Nieprawdą jest, że „tabela nie istnieje": migracja 20260901220000 jest zastosowana, a audit_log, cztery CHECK-i i wyzwalacz audit_log_append_only_trg zostały potwierdzone bezpośrednim zapytaniem do żywej bazy. Prawdą pozostaje co innego i to jest rzeczywiste ograniczenie: zestaw testów (vitest) NIE ma połączenia z żywym Postgresem w trakcie uruchomienia, więc kryteria warstwy bazy są dowodzone STATYCZNIE nad tekstem migracji (obecność wyzwalacza, jego przypięcie do UPDATE i DELETE, treść CHECK-ów), a nie ZACHOWANIOWO próbą zapisu. Dowód statyczny wykrywa usunięcie ochrony z migracji, ale nie wykryje rozjazdu między plikiem migracji a stanem serwera — dlatego wpis zostaje TODO do czasu, aż suite zyska dostęp do bazy i te same kryteria zostaną wykonane zachowaniowo. Testu przechodzącego przez pominięcie (it.skip, warunkowy return) nie wolno uznać za dowód żadnej z tych dwóch ścieżek'], risk: 'HIGH' }),

  R('CRM-DELETE-ADMIN-ONLY-LEADS', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/leads/actions.ts → prisma.leady.delete; warstwa Server Action pokryta przez apps/b2b-web/tests/leads-delete-admin-only.test.ts', domain: 'security', statement: 'Usunięcie leada (zasób leads) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Leadów', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM powstanie zapytanie kasujące — test dowodzi, że prisma.leady.delete nie zostało wywołane', 'RLS: bezpośredni DELETE na tabeli leady rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-DELETE-ADMIN-ONLY-INSTALLATIONS', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/installations/actions.ts → prisma.instalacje.delete', domain: 'security', statement: 'Usunięcie instalacji (zasób installations) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Instalacji', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM powstanie zapytanie kasujące — test dowodzi, że prisma.instalacje.delete nie zostało wywołane', 'RLS: bezpośredni DELETE na tabeli instalacje rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-DELETE-ADMIN-ONLY-SERVICES', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/services/actions.ts → prisma.serwisy.delete. UWAGA: ta ścieżka jest dziś martwa (kasuje encję z innej tabeli niż ta, którą pokazuje widok) — patrz punkt 4 WO, przeniesiony do Grupy C.', domain: 'security', statement: 'Usunięcie serwisu (zasób services) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Serwisów', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM powstanie zapytanie kasujące', 'RLS: bezpośredni DELETE na tabeli serwisy rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-DELETE-ADMIN-ONLY-INCIDENTS', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/incidents/actions.ts → prisma.usterki_incidents.delete', domain: 'security', statement: 'Usunięcie usterki (zasób incidents) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Usterek', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM powstanie zapytanie kasujące — test dowodzi, że prisma.usterki_incidents.delete nie zostało wywołane', 'RLS: bezpośredni DELETE na tabeli usterki_incidents rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-DELETE-ADMIN-ONLY-AUDITORS', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/auditors/actions.ts → tx.audytorzy.delete (wewnątrz transakcji)', domain: 'security', statement: 'Usunięcie audytora (zasób auditors) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Audytorów', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM otworzy się transakcja — test dowodzi, że tx.audytorzy.delete nie zostało wywołane', 'RLS: bezpośredni DELETE na tabeli audytorzy rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-DELETE-ADMIN-ONLY-CREWS', { status: 'TODO', source: 'b2b_crm_specifications.md#globalne (rozbicie CRM-DELETE-ADMIN-ONLY, WO BATCH-MEDIUM-LOW-CLEANUP punkt 22, 2026-09-01); ścieżka kodu: apps/b2b-web/src/app/(dashboard)/crews/actions.ts → tx.zespoly_monterskie.delete (wewnątrz transakcji); warstwa Server Action pokryta przez apps/b2b-web/tests/crews-admin-gates.test.ts', domain: 'security', statement: 'Usunięcie ekipy montażowej (zasób crews) jest dostępne wyłącznie dla roli admin, egzekwowane niezależnie w interfejsie, w Server Action i w RLS.', acceptance: ['UI: akcja „Usuń" jest ukryta dla ról dyspozytor, audytor i monter w widoku Ekip', 'Server Action: wywołanie z pominięciem interfejsu przez rolę nie-admin jest odrzucone ZANIM otworzy się transakcja — test dowodzi, że tx.zespoly_monterskie.delete nie zostało wywołane', 'RLS: bezpośredni DELETE na tabeli zespoly_monterskie rolą nie-admin jest odrzucony przez bazę', 'Każda z trzech warstw ma osobny test — pokrycie jednej nie zalicza pozostałych'], risk: 'HIGH' }),
  R('CRM-CREW-UPDATE-ADMIN-ONLY', { status: 'TODO', source: 'review 2026-08-25 (luka znaleziona przy przeglądzie apps/b2b-web/src/app/(dashboard)/crews/actions.ts) — brak źródła w dokumentach architektury; regułę niesie wyłącznie macierz w contracts/rbac.contract.mjs (crews.update = [admin]), która była poprawna, zanim powstało to wymaganie, bo kod akcji nigdy do niej nie zajrzał — ten sam wzorzec błędu co SEC-AUTHZ-USER-MGMT, inny zasób', domain: 'security', statement: 'Zmiana rekordu ekipy jest dostępna wyłącznie dla roli admin, egzekwowana po stronie serwera przed jakimkolwiek zapytaniem zapisującym do bazy. Dziś jedyną taką ścieżką jest ustawienie zdjęcia ekipy przez updateCrewAvatar, które nie ma ŻADNEGO sprawdzenia roli — dowolne zalogowane konto może podstawić dowolną ścieżkę Storage jako zdjęcie dowolnej ekipy. Reguła obowiązuje każdą kolejną Server Action zapisującą do tabeli ekip (crews.update = [admin]), nie tylko tę jedną.', acceptance: ['Wywołanie updateCrewAvatar przez konto o roli dyspozytor, audytor albo monter jest odrzucone po stronie serwera ZANIM powstanie zapytanie zapisujące — test dowodzi, że update na rekordzie ekipy nie zostało w ogóle wywołane, a nie tylko że wartość zdjęcia nie zmieniła się; test wywołuje Server Action bezpośrednio, z pominięciem interfejsu, bo interfejs nie jest granicą uprawnień: Prisma omija RLS, więc jedyną granicą jest kod akcji', 'Decyzję podejmuje can(actorRole, "crews", "update") z wygenerowanego kontraktu, a actorRole pochodzi z sesji serwera (getCurrentActorRole()) — nigdy z parametru akcji ani z jakiejkolwiek wartości przesłanej przez klienta; literał role === "admin" w kodzie akcji jest naruszeniem, bo dubluje macierz i przy jej zmianie zostanie w tyle', 'Przepuszczony jest wyłącznie wynik "yes". Warunek can(...) !== "no" jest naruszeniem, bo przepuszcza wariant ":own", a ta akcja bierze identyfikator ekipy z parametru i nigdy nie sprawdza, czyj to rekord — dopisanie kiedyś wariantu ":own" do crews.update zamieniłoby taki warunek w prawo do zmiany zdjęcia DOWOLNEJ ekipy. Że can() nie weryfikuje właścicielstwa rekordu, odnotowano już przy setSelfAvailabilityAction w tym samym pliku', 'Brak roli oznacza odmowę (fail-closed): brak sesji, e-mail spoza authorized_users oraz wartość roli nierozpoznana przez ROLES dają odmowę — getCurrentActorRole() zwraca w tych trzech przypadkach null, a null nie może trafić do can() jako rola przepuszczająca. Błąd samego zapytania o rolę (wyjątek z bazy) również kończy się odmową, nie zapisem. Test wykonuje te cztery próby realnie, sprawdzenie konfiguracji nie wystarcza (wzorem CRM-AUDYT-AC1)', 'Kontrola pozytywna: wywołanie przez konto o roli admin kończy się zapisem, a rekord ekipy ma po operacji przekazaną wartość zdjęcia. Bez tego przypadku zestaw testów przechodzi także dla akcji zepsutej tak, że odrzuca wszystkich', 'Odmowa jest odróżnialna dla wywołującego: updateCrewAvatar zwraca dziś void, więc odmowa nie ma jak się objawić inaczej niż wyjątkiem 500 albo cichym brakiem efektu — jedno i drugie jest wykluczone. Akcja zwraca wynik z jawną informacją o odmowie (wzorem SetSelfAvailabilityResult w tym samym pliku), a komponent wywołujący nie może traktować braku wyjątku jako sukcesu, bo dziś dokładnie tak robi', 'Ukrycie kontrolki wgrywania zdjęcia dla ról nie-admin jest wymagane, ale stanowi warstwę osobną od bramki serwerowej i jest testowane osobno (wzorem CRM-DELETE-ADMIN-ONLY) — samo ukrycie nie zamyka luki, bo Server Action pozostaje wywoływalna z pominięciem interfejsu', 'Bramka obowiązuje każdą ścieżkę zapisującą do rekordu ekipy, nie tylko updateCrewAvatar — akcja edycji ekipy dodana później bez tego sprawdzenia będzie tą samą luką pod inną nazwą. setSelfAvailabilityAction pozostaje poza zakresem, bo zapisuje do availability_declarations (FLD-AVAIL-SELF), a nie do rekordu ekipy — i taka pozostaje granica: decyduje tabela docelowa zapisu, nie plik, w którym akcja mieszka', 'Usunięcie ekipy NIE należy do tego wymagania: deleteCrewAction w tym samym pliku również nie ma dziś żadnego sprawdzenia roli, ale pokrywa je CRM-DELETE-ADMIN-ONLY. Zapisane wprost, żeby żadne z dwóch wymagań nie zostało uznane za spełnione naprawą tego drugiego', 'Odczyt listy ekip jest ŚWIADOMIE poza zakresem: getCrews() w tym samym pliku nie sprawdza roli, a macierz mówi crews.read = [admin, dyspozytor]. To inna zdolność i wymaga osobnego ID (wzorem rozszerzenia SEC-AUTHZ-USER-MGMT o read). Zapisane wprost, bo naprawa samego update zostawia plik w stanie, który przy pobieżnym przeglądzie wygląda na domknięty', 'Walidacja parametru path jest ŚWIADOMIE poza zakresem tego wymagania. Dziś path to dowolny string trafiający wprost do kolumny ze zdjęciem, a wgranie pliku wykonuje przeglądarka własną sesją do bucketu zespoly — o tym, kto tworzy tam obiekty, decyduje RLS Storage, nie ta akcja. Bramka roli zawęża krąg wywołujących do adminów, ale nie sprawdza, czy ścieżka wskazuje realny obiekt w oczekiwanym buckecie ani czy z niego nie wychodzi. To inna klasa błędu (walidacja wejścia, nie autoryzacja), dostaje osobne ID i nie wolno jej zamarkować regexem dopisanym przy okazji: sprawdzenie kształtu ścieżki bez sprawdzenia istnienia obiektu wygląda na zabezpieczenie, a nim nie jest'], risk: 'MEDIUM' }),
  R('CRM-CONTEXT-MENU', { source: 'b2b_crm_specifications.md#interfejs-akcji', domain: 'ui', statement: 'Każda tabela w panelu B2B ma akcje kontekstowe pod ikoną trzech kropek (MoreHorizontal) w ostatniej kolumnie.', risk: 'LOW', acceptance: ['Menu dostępne z klawiatury', 'Ikona wyłącznie z lucide-react'] }),

  // ───────────────────────── SLA / UI ─────────────────────────
  R('SLA-QUOTE-14D', { source: 'b2b_app_requirements.md#epic-1', domain: 'funnel', statement: 'Okno ważności wyceny wynosi 14 dni i jest zdefiniowane w jednym miejscu.', acceptance: ['Zero literałów 14 w kodzie aplikacji'] }),
  R('SLA-LOG-COLORS', { source: 'b2b_app_requirements.md#epic-2', domain: 'ui', statement: 'Kolorowanie wierszy logistyki: czerwony < 3 dni, pomarańczowy 3–7 dni do montażu.', acceptance: ['Wyliczenie przez date-fns differenceInDays', 'Progi z SLA.LOGISTICS_INSTALL'] }),
  R('UI-SLA-NO-GREEN', { source: 'ui_ux_guidelines.md#8', domain: 'ui', statement: 'Zakaz zielonych alertów SLA w interfejsie.', acceptance: ['Hook blokuje zapis pliku z zieloną klasą w kontekście SLA'], risk: 'LOW' }),
  R('UI-NO-HARDCODED-COLORS', { source: 'ui_ux_guidelines.md#8', domain: 'ui', statement: 'Zakaz hardkodowanych wartości Hex/RGB w komponentach — wyłącznie tokeny.', acceptance: ['Hook blokuje zapis', 'Lint rule w CI'], risk: 'LOW' }),
  R('UI-ICONS-LUCIDE-ONLY', { source: 'ui_ux_guidelines.md#1', domain: 'ui', statement: 'Wyłącznie ikony z lucide-react.', risk: 'LOW', acceptance: ['Import z innej biblioteki ikon blokowany przy zapisie'] }),
  R('UI-FORMS-RHF-ZOD', { source: 'ui_ux_guidelines.md#8', domain: 'ui', statement: 'Formularze wyłącznie react-hook-form + zodResolver; zakaz useState na pojedyncze pola tekstowe.', acceptance: ['Reviewer zgłasza BLOCKER przy naruszeniu'] }),

  // ───────────────────────── Serwisy / posprzedaż ─────────────────────────
  R('SRV-NEXT-DATE', { source: 'b2b_app_requirements.md#epic-4', domain: 'service', statement: 'Zamknięcie montażu generuje next_service_date dla instalacji.', acceptance: ['Data wyliczana w bazie, nie w kliencie', 'next_service_date jest polem pochodnym — żadna ścieżka w aplikacji go nie zapisuje (ADR-010)', 'Dla montażu TWO_PHASE liczona od zamknięcia etapu II, nie etapu I — sprzęt zaczyna pracować dopiero po drugim etapie', 'Zamknięcie etapu I nie ustawia next_service_date'] }),

  // ───────────────────────── Bezpieczeństwo / RODO ─────────────────────────
  R('SEC-SSO-GUARD', { source: 'b2b_app_requirements.md#epic-5', domain: 'security', statement: 'Logowanie wyłącznie przez Google; e-mail spoza authorized_users jest odrzucany z komunikatem o braku uprawnień.', acceptance: ['Odrzucenie następuje po stronie serwera', 'Brak wycieku informacji, czy konto istnieje'], risk: 'HIGH' }),
  // Dodane 2026-08-24 po znalezisku pobocznym z review FLD-AVAILABILITY-SPLIT (rls-security-auditor).
  // Reguła nie jest nowa — macierz RBAC mówi authorized_users: create/update/delete = ['admin'] od początku.
  // Nowy jest wyłącznie fakt, że nikt tej reguły nie egzekwował w kodzie: settings/actions.ts nie miał
  // żadnego sprawdzenia roli, a `role` przyjmował jako dowolny string. Macierzy NIE zmieniamy.
  R('SEC-AUTHZ-USER-MGMT', { status: 'TODO', source: 'review 2026-08-24 (rls-security-auditor, znalezisko poboczne przy FLD-AVAILABILITY-SPLIT) — brak źródła w dokumentach architektury; regułę niesie wyłącznie macierz w contracts/rbac.contract.mjs, która była poprawna, zanim powstało to wymaganie; zakres rozszerzony 2026-08-25 o zdolność read (ten sam audytor, przy weryfikacji naprawy create/delete) — settings/page.tsx czytał pełną listę kont bez sprawdzenia roli, ten sam wzorzec błędu, inna zdolność', domain: 'security', statement: 'Dodanie konta, usunięcie konta i przypisanie mu roli w authorized_users jest dostępne wyłącznie dla roli admin, a sama rola pochodzi z zamkniętego słownika ROLES — jedno i drugie egzekwowane po stronie serwera. Tej samej regule podlega odczyt listy kont (e-maile i role wszystkich pracowników): authorized_users.read = [admin], egzekwowane po stronie serwera również na ścieżce renderowania widoku, nie tylko w Server Actions.', acceptance: ['Wywołanie addAuthorizedUser i deleteAuthorizedUser przez konto o roli dyspozytor, audytor albo monter jest odrzucone po stronie serwera — test wywołuje Server Action bezpośrednio, z pominięciem interfejsu, bo interfejs nie jest granicą uprawnień: Prisma omija RLS, więc jedyną granicą jest kod akcji', 'Odczyt listy kont przez konto o roli dyspozytor, audytor albo monter jest odrzucony po stronie serwera ZANIM powstanie zapytanie do bazy — test dowodzi, że prisma.authorizedUser.findMany() nie zostało w ogóle wywołane, bo pusty ekran niczego nie dowodzi: rekordy pobrane i odfiltrowane dopiero w komponencie i tak trafiają do przeglądarki w payloadzie RSC. Dzisiejszą ścieżką odczytu jest Server Component apps/b2b-web/src/app/(dashboard)/settings/page.tsx, nie Server Action — bramka należy do niego, nie do middleware, które sprawdza wyłącznie obecność e-maila w authorized_users i przepuszcza każdą rolę; ta sama bramka obowiązuje każdą kolejną ścieżkę czytającą authorized_users (widok, eksport, endpoint), nie tylko ten jeden plik', 'Odmowa odczytu jest fail-closed dokładnie tak samo jak przy create i delete: decyduje can(actorRole, "authorized_users", "read") z actorRole z getCurrentActorRole(), a brak sesji, e-mail spoza authorized_users, wartość roli nierozpoznana przez ROLES oraz błąd samego zapytania o rolę dają odmowę. Odmowa nie może przybrać postaci pustej listy kont — to wynik nieodróżnialny od stanu „nie ma czego pokazać" i przy następnej zmianie kodu zostanie wzięty za poprawny; kształt odmowy na ścieżce widoku (przekierowanie albo strona odmowy) jest wyborem implementacji, wykluczony jest wyjątek 500', 'Decyzję podejmuje can(actorRole, "authorized_users", capability) z wygenerowanego kontraktu, a actorRole pochodzi z sesji serwera (getCurrentActorRole()) — nigdy z parametru akcji ani z jakiejkolwiek wartości przesłanej przez klienta; literał role === "admin" w kodzie akcji jest naruszeniem, bo dubluje macierz i przy jej zmianie zostanie w tyle', 'Brak roli oznacza odmowę (fail-closed), nie przepuszczenie: brak sesji, e-mail spoza authorized_users, wartość roli nierozpoznana przez ROLES oraz błąd samego zapytania o rolę dają ten sam wynik — odmowę. Test wykonuje te próby realnie, sprawdzenie konfiguracji nie wystarcza (wzorem CRM-AUDYT-AC1)', 'Rola przekazywana przy tworzeniu konta jest walidowana wobec ROLES z kontraktu RBAC po stronie serwera (Zod): pusty string, literówka w nazwie istniejącej roli i rola wymyślona (np. "superadmin") są odrzucone, zamiast trafić do kolumny role jako dowolny tekst — dziś parametr ma typ string i ląduje w bazie bez żadnego sprawdzenia', 'Słownik ról ma dokładnie jedno źródło: ROLES w contracts/rbac.contract.mjs. Druga lista ról wpisana w schemacie Zod, w kodzie akcji albo w komponencie formularza jest naruszeniem nawet wtedy, gdy chwilowo zawiera te same cztery wartości', 'Bramka obowiązuje każdą ścieżkę zmieniającą rolę istniejącego konta (authorized_users.update = [admin]), nie tylko dwie dzisiejsze akcje — akcja zmiany roli dodana później bez tego sprawdzenia byłaby tą samą eskalacją pod inną nazwą', 'Odmowa nie ujawnia, czy konto o podanym identyfikatorze istnieje — nieuprawnione wywołanie dostaje ten sam komunikat dla identyfikatora istniejącego i nieistniejącego (spójnie z SEC-SSO-GUARD), a odmowa jest błędem domenowym w zwracanym wyniku, nie wyjątkiem 500', 'Zapis operacji do rejestru NIE należy do tego wymagania: nadanie i zmiana roli to zdarzenie role_change z AUDIT_REQUIREMENTS.mustLog i pokrywa je SEC-AUDIT-LOG. Tutaj sprawdzana jest wyłącznie bramka — z zastrzeżeniem, że bramka bez rejestru zostawia udaną eskalację niewidoczną w historii, więc kolejność wdrożenia ma znaczenie', 'Usunięcie ostatniego konta o roli admin (w tym administratora usuwającego samego siebie) jest ŚWIADOMIE poza zakresem tego wymagania. To inna klasa awarii: nie eskalacja uprawnień, tylko trwała utrata zarządzania kontami, bo po naprawie nikt poza adminem konta nie doda z powrotem. Poprawne wymuszenie wymaga gwarancji w bazie — dwóch administratorów kasujących się równolegle przejdzie każde sprawdzenie „ilu jest adminów" wykonane w JS przed zapisem, tak samo jak przy rezerwacji slotu — czyli zmiany schematu i migracji, których ten wpis nie obejmuje. Luka jest znana i czeka na osobne ID; nie wolno jej „załatwić" liczeniem adminów w Server Action, bo to wygląda na zabezpieczenie, a nim nie jest'], risk: 'HIGH' }),
  R('SEC-RLS-AUDITOR-SCOPE', { status: 'DONE', source: 'ZAMKNIĘTE 2026-09-01 (WO BATCH-MEDIUM-LOW-CLEANUP, punkt 13). Zastrzeżenie proceduralne z ostatniego kryterium jest spełnione: leads-auditor-scope.test.ts dowodzi kryterium dowodowego na argumencie where (audytor dostaje audytor_id z sesji, admin i dyspozytor nie dostają tego klucza wcale) ORAZ czterech wariantów fail-closed (rola null, wyjątek z getCurrentActorRole, brak rekordu w audytorzy, brak e-maila w sesji) — w każdym findMany NIE jest wołane, więc where z undefined nie powstaje. Liczniki i paginacja pokryte osobno (groupBy i count z tym samym filtrem), /logistics zamknięte dla audytora i montera (D4), konto is_active=false odrzucane w samej akcji, a rls-deny-by-default-freeze.test.ts zamraża eskalację bokiem przez supabase-js. b2b_app_requirements.md#epic-5 (linia 139: „Audytor widzi tylko zlecenia przypisane do siebie, a Dyspozytor widzi wszystko") + docs/workorders/SEC-RLS-AUDITOR-SCOPE.md, decyzje człowieka D1-D6 z rozmowy 2026-08-26. ZAKRES DZIAŁANIA (D6, potwierdzone przez człowieka przy bazie produkcyjnej): dziś w tabeli authorized_users NIE ISTNIEJE ani jedno konto o roli innej niż admin — żadnego audytora, żadnego montera. To czyni tę naprawę PREWENCYJNĄ, nie pożarową: nikt dziś tej dziury nie eksploatuje, bo nie ma kim. Ryzyko zostaje mimo to HIGH i wpis nie schodzi z kolejki, bo aktywacja luki nie wymaga żadnej zmiany w kodzie — wystarczy jeden wiersz w authorized_users. Kod jest już na te konta przygotowany (dedykowana gałąź roli audytor w apps/b2b-web/src/utils/supabase/middleware.ts), a Field App — docelowe miejsce pracy audytora i montera — nie istnieje w tym repozytorium (apps/ to wyłącznie b2b-web i b2c-web), więc pierwsze utworzone konto audytora lub montera dostanie panel B2B jako JEDYNĄ aplikację, do której może się zalogować, i trafi po zalogowaniu prosto na /leads. Trzy wcześniejsze wymagania (SEC-ASSIGNMENT-POOL-MINIMIZE, SEC-LEADS-LIST-MINIMIZE, SEC-LEADS-LIST-SCALARS) uzasadniają obniżone ryzyko zdaniem „dostęp jest już ograniczony rolą i zakresem audytora" — ta przesłanka jest FAŁSZYWA do czasu zamknięcia tego ID i jej prawdziwość zależy właśnie od niego', domain: 'security', statement: 'Odczyt leadów w panelu B2B jest bramkowany rolą i zawężony do zakresu aktora. Audytor widzi wyłącznie leady przypisane do siebie; dyspozytor i admin widzą wszystko; monter nie widzi leadów w ogóle. Dziś getLeads() w apps/b2b-web/src/app/(dashboard)/leads/actions.ts buduje where wyłącznie z filtra statusu i kubełka, nie wywołuje ani getCurrentActorRole(), ani can() — ani razu — więc każde zalogowane konto, niezależnie od roli, dostaje komplet leadów wszystkich audytorów wraz z licznikami wolumenu. Ta sama tabela leadów jest czytana drugim wejściem przez getLogisticsLeads() w logistics/actions.ts, również bez bramki; zostawienie tego wejścia otwartego czyni zawężenie na /leads fikcją, bo obejście to jedno kliknięcie w menu. Reguła jest reguła DANYCH, nie interfejsu: Prisma omija RLS (pułapka 1 z CLAUDE.md), więc jedyną granicą jest warunek where w Server Action.', acceptance: ['ZAKRES AUDYTORA (decyzja D1 człowieka, 2026-08-26): dla roli audytor where przekazane do prisma.leady.findMany zawiera warunek audytor_id równy identyfikatorowi ZALOGOWANEGO audytora i nic ponadto nie rozszerza zbioru. Audytor NIE widzi leadów nieprzypisanych (audytor_id IS NULL), także w statusie NEW_LEAD — pula napływowa nie należy do jego zakresu. Konsekwencja jest świadoma i zaakceptowana: audytor wchodzący na /leads z domyślnym filtrem NEW_LEAD zobaczy pustą listę; to poprawny wynik, nie awaria, i test na pustą listę w tym scenariuszu jest testem pozytywnym', 'TOŻSAMOŚĆ AKTORA POCHODZI Z SESJI, NIGDY Z KLIENTA: identyfikator audytora jest dociągany po stronie serwera przez audytorzy.email równe adresowi e-mail z sesji Supabase, tym samym wzorcem co setSelfAvailabilityAction i acceptLegalDocumentVersionAction. Żaden identyfikator przekazany w argumencie Server Action nie może wpłynąć na warunek własności; test wywołuje akcję bezpośrednio, z pominięciem interfejsu, podstawiając cudzy identyfikator w argumencie i dowodzi, że where zawiera identyfikator z sesji, a nie ten z argumentu', 'FAIL-CLOSED PRZY NIEZNANEJ TOŻSAMOŚCI: gdy dla zalogowanego audytora nie ma rekordu w audytorzy (albo audytorzy.email jest NULL), akcja ODMAWIA. Zabronione jest zbudowanie where z wartością undefined — Prisma taki warunek IGNORUJE i oddaje komplet leadów, czyli dokładnie to, przed czym to wymaganie chroni. Test na ten przypadek jest obowiązkowy i musi dowodzić, że findMany albo nie zostało wywołane, albo dostało warunek niespełnialny — nie że wynik był akurat pusty. Ta sama reguła obowiązuje, gdy getCurrentActorRole() zwraca null albo rolę spoza ROLES', 'MONTER — ODMOWA, NIE PUSTA LISTA (decyzja D2 człowieka, 2026-08-26): rola monter nie ma leads.read w macierzy w żadnym wariancie, więc can("monter","leads","read") zwraca już dziś "no" i brakuje wyłącznie egzekucji. getLeads() odmawia dostępu jawnie — kształtem analogicznym do bramek can(...) !== "yes" wprowadzonych w SEC-AUTHZ-B2B-MUTATIONS — zamiast przepuszczać zapytanie i oddawać pustą listę. Cicha pusta lista jest gorsza od odmowy z dwóch powodów: wygląda jak stan danych, a nie jak brak uprawnienia, i zostawia zapytanie w kodzie, więc pierwsza zmiana warunku filtrującego przywraca przeciek bez śladu w diffie', '/LOGISTICS ZAMKNIĘTE DLA AUDYTORA I MONTERA (decyzja D4 człowieka, 2026-08-26): getLogisticsLeads() czyta tę samą tabelę leadów i podlega tej samej bramce — dla roli audytor ORAZ dla roli monter jest to ODMOWA, nie zawężenie i nie pusta lista. Audytor nie dostaje tam własnego zakresu: widok logistyczny nie jest jego narzędziem pracy, a jego udostępnienie w wariancie :own byłoby drugim, równoległym wejściem do tych samych danych, które trzeba by utrzymywać w zgodzie z pierwszym. Dyspozytor i admin zachowują pełny dostęp bez zmian', 'DOWODEM JEST ARGUMENT PRZEKAZANY DO ZAPYTANIA, NIE KSZTAŁT WYNIKU: test odczytuje obiekt where faktycznie przekazany do prisma.leady.findMany i sprawdza (a) że dla roli audytor zawiera warunek własności po audytor_id z identyfikatorem z sesji, (b) że dla admina i dyspozytora tego warunku NIE zawiera. Asercja „wynik nie zawiera cudzych leadów" nie zamyka tego kryterium i nie jest wystarczająca: przy zamockowanej Prismie mock oddaje to, co mu wpisano, niezależnie od where, więc taki test przechodzi także dla kodu bez żadnego filtra. Ten sam standard dowodowy co przy select w SEC-LEADS-LIST-MINIMIZE i SEC-LEADS-LIST-SCALARS i z tego samego powodu', 'LICZNIKI I PAGINACJA SĄ CZĘŚCIĄ ZAKRESU: stageCounts (osobne groupBy, które dziś nie przyjmuje where w ogóle) oraz totalCount i totalPages liczą dla audytora WYŁĄCZNIE jego leady. Licznik „NEW_LEAD: 47" pokazany audytorowi mającemu trzy rekordy jest wyciekiem informacji o wolumenie biznesu i musi być traktowany jak naruszenie zakresu, nie jak kosmetyka. Suma stageCounts["ALL"] musi zgadzać się z totalCount, a strona druga dla audytora z trzema leadami ma być pusta, nie ma „przeciekać" na cudze rekordy', 'KONTROLA POZYTYWNA DLA RÓL UPRZYWILEJOWANYCH: dla admina i dyspozytora zbiór zwróconych identyfikatorów przed zmianą i po zmianie jest identyczny, a where nie zyskuje żadnego warunku własności. Bez tego przypadku cały zestaw przechodzi także dla naprawy, która zawęża zakres wszystkim i zabiera dyspozytorowi narzędzie pracy. Analogicznie audytor otwierający WŁASNY lead widzi go bez zmian względem dzisiejszego zachowania', 'PRZEPIĘCIE LEADA (decyzja D3 człowieka, 2026-08-26): po zmianie audytor_id na innego audytora poprzedni traci dostęp NATYCHMIAST, przy następnym odczycie — także do leadów, które faktycznie audytował. To naturalna konsekwencja filtra po bieżącym audytor_id i rozstrzygnięcie świadome: żadna tabela historii przypisań NIE powstaje i nie jest do tego wymagania potrzebna. Zapisane wprost, żeby nikt nie potraktował tego jako braku do uzupełnienia', 'ESKALACJA BOKIEM: bezpośrednie zapytanie supabase-js kluczem anon na sesji audytora (from("leady").select("*")) nie zwraca żadnych wierszy. Test zamraża dzisiejszy stan deny-by-default z migracji 20260824185845_security_enable_rls_baseline.sql (tabela leadów ma RLS włączone i jedyną politykę INSERT dla anon, brak polityki SELECT), tak żeby przyszłe dodanie permisywnej polityki SELECT wywaliło bramkę zamiast po cichu otworzyć drugie wejście', 'KONTO NIEAKTYWNE: sprawdzenie w Server Action nie może zakładać, że middleware już zadziałał — akcja jest osiągalna wywołaniem POST z pominięciem middleware. Audytor z is_active = false podlega odmowie w samej akcji, niezależnie od tego, że middleware go wylogowuje', 'POZA ZAKRESEM, ŚWIADOMIE: systemowy brak bramki read w pozostałych widokach panelu B2B (decyzja D5 człowieka, 2026-08-26 — 20 z 21 widoków (dashboard)/**/page.tsx nie sprawdza uprawnienia do odczytu; zostaje jako finding LOW, NIE jest rejestrowane jako osobne wymaganie i nie wolno go doklejać tutaj), wariant monter:own dla installations, services i incidents (wymaga własnego ID), polityki RLS SELECT na tabeli leadów (panel chodzi po Prismie, która RLS omija — polityka niczego tu nie naprawi), filtrowanie navItems po roli w layout.tsx (ukrycie linku nie jest kontrolą dostępu), twardnienie addAuthorizedUser (należy do SEC-AUTHZ-USER-MGMT) oraz Field App, który nie istnieje w repozytorium i na który żadne kryterium nie może się powoływać', 'ZASTRZEŻENIE PROCEDURALNE: status tego wymagania nie przechodzi na DONE bez testu pokrywającego kryterium dowodowe na argumencie where ORAZ przypadku fail-closed przy braku rekordu w audytorzy. Zamknięcie na podstawie samego testu wyniku udokumentowałoby jako spełnione coś, czego kod może nie robić'], risk: 'HIGH' }),
  R('SEC-ASSIGNMENT-POOL-MINIMIZE', { status: 'TODO', source: 'review 2026-08-25 (luka znaleziona przy przeglądzie apps/b2b-web/src/app/(dashboard)/leads/actions.ts) — brak źródła w dokumentach architektury; regułę niesie zasada minimalizacji danych (ta sama, z której żyje SEC-RODO-DELETE), tyle że po stronie odczytu: SEC-RLS-AUDITOR-SCOPE pilnuje, KTÓRE rekordy wolno pobrać, a nikt nie pilnował, KTÓRE KOLUMNY tych rekordów wolno wysłać do przeglądarki. Dostęp do puli jest już poprawnie ograniczony rolą — to nie jest luka RBAC i nie naprawia się jej macierzą uprawnień', domain: 'security', statement: 'Pula wyboru audytora (getAuditors()) i pula wyboru ekipy (getCrews()) w leads/actions.ts przekazują przez granicę serwer/klient wyłącznie te pola, które konsument faktycznie renderuje — nigdy surowego rekordu Prismy. Dziś obie funkcje wołają findMany() bez select, więc do przeglądarki dyspozytora trafia komplet kolumn pracownika, w tym iban, numer NIP, adres, telefon, e-mail i bazowy kod pocztowy, mimo że żaden z trzech konsumentów (leads-client.tsx, assign-auditor.tsx, assign-crew-dialog.tsx) nie renderuje ani jednego z nich. Reguła obowiązuje obie granice: Server Action wywoływaną z komponentu klienckiego ORAZ przekazanie propsów z Server Componentu do komponentu klienckiego, bo payload RSC serializuje cały props, a nie to, co JSX wyświetli.', acceptance: ['Dowodem jest KSZTAŁT zwróconego obiektu: test porównuje Object.keys(element) z zadeklarowanym zestawem pól jako zbiory (posortowana równość, nie toContain i nie toMatchObject). Asercja typu expect(auditor.iban).toBeUndefined() jest niewystarczająca i nie zamyka tego wymagania — przechodzi także dla obiektu z trzydziestoma innymi kolumnami obok, przechodzi dla literówki w nazwie sprawdzanego pola i przechodzi dla rekordu, w którym akurat ta jedna wartość jest pusta w bazie', 'Zestaw pól jest wyprowadzony z listy konsumentów, nie z wygody: pula audytora to identyfikator i nazwa wyświetlana (kolumna o porzuconej nazwie polskiej, przekład w docs/architecture/NAMING.md → full_name); pula ekipy to identyfikator, nazwa ekipy (→ name), imię i nazwisko koordynatora (→ coordinator_full_name), numer certyfikatu F-Gaz (→ fgaz_certificate_no), znacznik uprawnień SEP (→ sep_qualified) oraz promień działania w km. Pole, którego nie renderuje żaden z trzech konsumentów, do zestawu NIE należy, choćby wyglądało niewinnie — kryterium przynależności brzmi „jest wyświetlane", nie „nie jest wrażliwe"', 'Pola wrażliwe (iban, NIP, adres, telefon, e-mail, bazowy kod pocztowy) nie występują w zwróconym obiekcie pod ŻADNĄ nazwą: test iteruje po kluczach elementu i sprawdza, że żaden nie należy do zbioru wrażliwych, zamiast odpytywać o nie po jednej nazwie. Sformułowanie jest celowo niezależne od dzisiejszych nazw kolumn — kolumny tych tabel czekają na przemianowanie wg ADR-002 i przemianowanie NIE może przywrócić przecieku ani unieważnić testu', 'Zawężenie jest zadeklarowane w select samego zapytania, nie dopiero w map po jego wykonaniu. Kształt wyniku to dowód podstawowy, ale test sprawdza dodatkowo, że zapytanie w ogóle nie pobiera pól wrażliwych: rekord z numerem konta wciągnięty do pamięci serwera „na chwilę" trafia potem do logu wyjątku, do zrzutu diagnostycznego i do pierwszego spreada, który ktoś napisze w dobrej wierze. findMany() bez select jest naruszeniem nawet wtedy, gdy zaraz po nim stoi poprawne mapowanie', 'Pole potrzebne WYŁĄCZNIE serwerowo nie przecieka przez spread. Dzisiejszy leads/[id]/page.tsx pobiera ścieżkę zdjęcia audytora (kolumna o porzuconej nazwie polskiej, przekład → photo_url), podpisuje ją przez signStoragePaths i buduje props zapisem {...auditor, avatarUrl} — spread kopiuje wszystko, więc surowa ścieżka Storage jedzie do przeglądarki obok podpisanego adresu i niweczy jakiekolwiek zawężenie zrobione wcześniej w getAuditors(). Wymagane jest jawne wyliczenie pól przy budowie propsa. Test asercjonuje kształt obiektu przekazywanego do komponentu klienckiego, nie tylko wynik Server Action — inaczej sprawdza połowę drogi', 'Typ propa jest wąskim, nazwanym typem wyniku tych dwóch funkcji, nie modelem Prismy. Dziś AssignAuditor przyjmuje pełny model rekordu audytora, przez co avatarUrl trzeba wepchnąć rzutowaniem — rzutowanie jest zakazane regulaminem, ale tutaj jest objawem, nie przyczyną: przyczyną jest prop otypowany szerzej niż dane, które wolno przekazać. Typ definiuje się raz, przy zwrocie z akcji, i importuje w konsumentach; assign-crew-dialog.tsx już dziś czyta go przez Awaited<ReturnType<typeof getCrews>>, więc podąży za zawężeniem bez zmiany', 'Zestaw pól jest ZAMKNIĘTY także dla przyszłych konsumentów: każdy nowy komponent czytający wynik getAuditors()/getCrews() z leads/actions.ts dziedziczy to ograniczenie i nie może dobrać sobie kolejnego pola przy okazji własnej zmiany. Rozszerzenie zestawu wymaga świadomej zmiany tego wymagania, a przy polu wrażliwym — osobnego ID i decyzji człowieka. Sam typ tego nie wymusi (dopisanie pola do typu kompiluje się bez oporu), więc bramką jest test kształtu i ten zapis', 'Zawężenie nie może zdjąć ani osłabić dzisiejszych filtrów puli: aktywność konta audytora, deklaracja własnej dostępności (FLD-AVAIL-SELF), aktywność ekipy oraz ważność certyfikatów na datę montażu (CRM-ZESP-AC2). Pola potrzebne wyłącznie do filtrowania wolno pobrać, ale nie wolno ich wysłać do klienta, jeśli nikt ich nie renderuje. Istniejące testy tych filtrów zostają i muszą przechodzić po zmianie — zawężenie kolumn jest dokładnie tą klasą zmiany, przy której filtr znika niezauważony, bo zniknięcie nie psuje kompilacji', 'Kontrola pozytywna: po zmianie pula nadal pokazuje to, co ma pokazywać — audytora da się wybrać po nazwie, a karta ekipy nadal wyświetla koordynatora, oba certyfikaty i promień działania. Bez tego przypadku cały zestaw testów przechodzi także dla funkcji zwracającej pustą listę albo same identyfikatory, czyli dla naprawy, która zabiera dyspozytorowi możliwość wyboru', 'getAuditors() z auditors/actions.ts i getCrews() z crews/actions.ts są ŚWIADOMIE poza zakresem mimo identycznych nazw. To panele administracyjne kartotek: administrator ma tam widzieć komplet danych pracownika, bo po to te widoki istnieją. Zapisane wprost, bo pomyłka co do funkcji jest przy tych nazwach naturalna, a zawężenie tamtych dwóch skasowałoby działającą funkcjonalność pod pozorem naprawy bezpieczeństwa', 'getLeads() w tym samym pliku jest ŚWIADOMIE poza zakresem, choć ma ten sam defekt: pobiera include całych rekordów klienta, adresu, instalacji z ekipą i audytora, i oddaje je do leads-client.tsx, czyli wysyła do przeglądarki komplet danych kontaktowych klientów całej listy. To inne zapytanie, inny zestaw konsumentów i osobne ID. Zapisane wprost, żeby naprawa dwóch pul nie została wzięta za domknięcie tematu — po niej plik przy pobieżnym czytaniu wygląda na uporządkowany, a największy przeciek w nim zostaje', 'Optymistyczna aktualizacja w leads-client.tsx wstawia element puli w miejsce pełnego rekordu audytora przypisanego do leada. Po zawężeniu ten kod przestanie się typować i to jest jedyne miejsce, w którym zmiana „boli" — a więc jedyne, w którym pojawi się pokusa cofnięcia zawężenia albo rzutowania. Ani jedno, ani drugie nie jest dopuszczalne: poprawną drogą jest zawężenie tego, czego widok potrzebuje od audytora leada, do pól faktycznie wyświetlanych'], risk: 'HIGH' }),
  R('SEC-LEADS-LIST-MINIMIZE', { status: 'DONE', source: 'review 2026-08-25 (przegląd apps/b2b-web/src/app/(dashboard)/leads/actions.ts, funkcja getLeads()) — brak źródła w dokumentach architektury; ta sama zasada minimalizacji danych po stronie odczytu, z której żyje SEC-ASSIGNMENT-POOL-MINIMIZE, tyle że zastosowana do zapytania listy leadów. Poprzednie wymaganie zostawiło ten przeciek świadomie i nazwało go wprost jako osobne ID — to jest to ID. Dostęp do listy jest już poprawnie ograniczony rolą i zakresem audytora (SEC-RLS-AUDITOR-SCOPE), więc nie jest to luka RBAC i nie naprawia się jej macierzą uprawnień', domain: 'security', statement: 'Widok listy leadów (getLeads() w leads/actions.ts) przekazuje przez granicę serwer/klient wyłącznie te pola, które leads-client.tsx faktycznie renderuje — nigdy surowego zagnieżdżonego rekordu Prismy. Dziś zapytanie woła findMany() z include całych relacji: klienta, adresu, instalacji wraz z ekipą, więc do przeglądarki dyspozytora jedzie komplet danych kontaktowych KAŻDEGO klienta na stronie (numer telefonu, e-mail) oraz komplet danych rozliczeniowych ekipy (numer konta bankowego, numer NIP, telefon kontaktowy, e-mail), a także współrzędne geograficzne adresu. Konsument czyta z tych trzech relacji dokładnie trzy wartości: nazwę klienta (kolumna o porzuconej nazwie polskiej, przekład w docs/architecture/NAMING.md → full_name), adres w postaci ulicy z miastem (→ street_city) i nazwę ekipy pierwszej instalacji (→ name). Skala odróżnia ten przypadek od widoku szczegółów: strona listy oddaje do pięćdziesięciu kompletów danych osobowych naraz, komuś, kto otworzył ekran po to, żeby zobaczyć statusy.', acceptance: ['Dowodem jest KSZTAŁT zwróconych obiektów zagnieżdżonych: test porównuje Object.keys(lead.klient), Object.keys(lead.adres) i Object.keys(instalacja.zespol) z zadeklarowanymi zestawami pól jako zbiory (posortowana równość, nie toContain i nie toMatchObject). Asercja typu expect(lead.klient.telefon).toBeUndefined() jest niewystarczająca i nie zamyka tego wymagania — przechodzi także dla obiektu z dwudziestoma innymi kolumnami obok, przechodzi dla literówki w nazwie sprawdzanego pola i przechodzi dla rekordu, w którym akurat ta jedna wartość jest pusta w bazie. Ten sam standard dowodowy co w SEC-ASSIGNMENT-POOL-MINIMIZE i z tego samego powodu', 'Zestaw pól jest wyprowadzony z konsumenta, nie z wygody: relacja klienta to identyfikator i nazwa wyświetlana (→ full_name); relacja adresu to sam adres tekstowy (→ street_city); relacja ekipy zagnieżdżona w instalacji to sama nazwa ekipy (→ name). Identyfikator należy WYŁĄCZNIE do zestawu relacji klienta (klucz rekordu klienta, po którym widok rozpoznaje i odświeża wiersz); leads-client.tsx nie renderuje ani id adresu, ani id ekipy, więc żadne z nich do zestawu nie należy. Pole, którego leads-client.tsx nie renderuje, do zestawu NIE należy, choćby wyglądało niewinnie — kryterium przynależności brzmi „jest wyświetlane", nie „nie jest wrażliwe". Współrzędne geograficzne adresu (szerokość i długość) nie są przez listę renderowane i do klienta nie jadą', 'Pola wrażliwe klienta (numer telefonu, e-mail) i ekipy (numer konta bankowego, numer NIP, telefon kontaktowy, e-mail) nie występują w zwróconych obiektach pod ŻADNĄ nazwą: test iteruje po kluczach obiektów klienta i ekipy i sprawdza, że żaden nie należy do zbioru wrażliwych, zamiast odpytywać o nie po jednej nazwie. Sformułowanie jest celowo niezależne od dzisiejszych nazw kolumn — kolumny tych tabel czekają na przemianowanie wg ADR-002 i przemianowanie NIE może przywrócić przecieku ani unieważnić testu', 'Zawężenie jest zadeklarowane w select samego zapytania, włącznie z zagnieżdżonymi relacjami (klient, adres oraz zespół wewnątrz instalacji), a nie dopiero w map po jego wykonaniu. Zagnieżdżenie jest tu istotą sprawy: select na poziomie leada bez selectu na poziomie relacji nadal wciąga całe rekordy podrzędne, czyli zostawia przeciek nietknięty przy pozorach naprawy. Rekord z numerem konta bankowego wciągnięty do pamięci serwera „na chwilę" trafia potem do logu wyjątku, do zrzutu diagnostycznego i do pierwszego spreada, który ktoś napisze w dobrej wierze. include całych relacji jest naruszeniem nawet wtedy, gdy zaraz po nim stoi poprawne mapowanie', 'Widok szczegółów pojedynczego leada (leads/[id]/page.tsx) jest ŚWIADOMIE poza zakresem. Wykonuje własne, osobne zapytanie findUnique() i nie korzysta z getLeads(); administrator i dyspozytor potrzebują tam pełnych danych kontaktowych klienta, bo po to ten ekran istnieje — żeby zadzwonić albo napisać. Zapisane wprost, żeby nikt nie zawęził tamtego zapytania „przy okazji" tej naprawy i nie zabrał działającej funkcjonalności pod pozorem bezpieczeństwa. Jedno zapytanie oddaje jeden komplet danych na świadome żądanie, drugie oddaje pięćdziesiąt bez żądania — to jest różnica, która dzieli te dwa przypadki', 'Filtry, sortowanie i stronicowanie zostają NIETKNIĘTE: warunki where (w tym zawężenie zakresu audytora), orderBy, skip/take oraz osobne groupBy liczące leady w podziale na statusy. Zawężenie kolumn jest dokładnie tą klasą zmiany, przy której warunek filtrujący znika niezauważony, bo zniknięcie nie psuje kompilacji. Pola potrzebne wyłącznie do filtrowania lub sortowania wolno pobrać, ale nie wolno ich wysłać do klienta, jeśli nikt ich nie renderuje. Istniejące testy filtrów i liczników statusów zostają i muszą przechodzić po zmianie', 'Kontrola pozytywna: po zmianie lista nadal pokazuje to, co ma pokazywać — nazwę klienta, adres z miastem i nazwę przypisanej ekipy, dla leada, który ma te relacje wypełnione, oraz poprawnie pusty stan dla leada bez przypisanej instalacji lub ekipy. Bez tego przypadku cały zestaw testów przechodzi także dla funkcji zwracającej same identyfikatory albo pustą listę, czyli dla naprawy, która zabiera dyspozytorowi widok', 'Zestaw pól jest ZAMKNIĘTY także dla przyszłych konsumentów: każdy nowy komponent czytający wynik getLeads() dziedziczy to ograniczenie i nie może dobrać sobie kolejnego pola przy okazji własnej zmiany. Rozszerzenie zestawu wymaga świadomej zmiany tego wymagania, a przy polu wrażliwym — osobnego ID i decyzji człowieka. Sam typ tego nie wymusi (dopisanie pola do typu kompiluje się bez oporu), więc bramką jest test kształtu i ten zapis', 'Pole audytora w wyniku getLeads() jest JUŻ zawężone przez SEC-ASSIGNMENT-POOL-MINIMIZE (do identyfikatora i nazwy wyświetlanej) i NIE należy do zakresu tego wpisu — odnotowane, bo dopiero oba wymagania razem domykają CAŁY kształt getLeads(). Naprawa realizująca to ID nie może cofnąć ani rozszerzyć tamtego zawężenia, a test kształtu obejmuje wszystkie cztery relacje naraz: dopiero komplet dowodzi, że przez tę granicę nie przechodzi nic ponad to, co widać na ekranie'], risk: 'HIGH' }),
  // Dodane 2026-08-26 po REVIEW zamykającym SEC-LEADS-LIST-MINIMIZE (rls-security-auditor):
  // tamto wymaganie zawęziło WYŁĄCZNIE relacje i zapisało to wprost w kodzie
  // ("Pola samego leada NIE są zawężane"), więc pełny zestaw skalarów leada jedzie do
  // przeglądarki dalej. Rejestracja zatwierdzona przez człowieka w rozmowie 2026-08-26.
  R('SEC-LEADS-LIST-SCALARS', { status: 'DONE', source: 'review 2026-08-26 (rls-security-auditor, znalezisko przy zamknięciu SEC-LEADS-LIST-MINIMIZE — przegląd apps/b2b-web/src/app/(dashboard)/leads/actions.ts, funkcja getLeads()) + decyzja człowieka z rozmowy 2026-08-26, który zatwierdził zarówno rejestrację, jak i naprawę; brak źródła w dokumentach architektury. Ta sama zasada minimalizacji danych po stronie odczytu co w SEC-ASSIGNMENT-POOL-MINIMIZE i SEC-LEADS-LIST-MINIMIZE, tyle że zastosowana do SKALARÓW samego leada, a nie do jego relacji. Poprzednie wymaganie świadomie ograniczyło się do relacji i zapisało to w komentarzu przy select — to jest ID domykające drugą połowę tego samego zapytania. Dostęp do listy jest już poprawnie ograniczony rolą i zakresem audytora (SEC-RLS-AUDITOR-SCOPE), więc nie jest to luka RBAC i nie naprawia się jej macierzą uprawnień', domain: 'security', statement: 'Widok listy leadów (getLeads() w leads/actions.ts) przekazuje przez granicę serwer/klient wyłącznie te SKALARNE pola samego leada, które leads-client.tsx faktycznie zużywa — dokładnie tak, jak SEC-LEADS-LIST-MINIMIZE nakazało to już dla relacji. Dziś select wylicza wszystkie kolumny tabeli leadów, więc do przeglądarki każdego dyspozytora jedzie dla maksymalnie pięćdziesięciu leadów naraz notatka wewnętrzna o kliencie i audycie (pole swobodnego tekstu — może zawierać dowolne dane osobowe wpisane ręcznie przez pracownika) oraz surowe odpowiedzi klienta z formularza triage B2C (adres, dane kontaktowe, preferencje w postaci, w jakiej klient je podał). Żadne z tych dwóch pól nie jest przez listę renderowane w jakiejkolwiek formie. Obok nich jadą bez potrzeby pola operacyjne i handlowe: wybrana konfiguracja, wycena finalna, przewidywany czas montażu, powód utraty wraz z notatką, powód auto-odrzucenia, data ostatniego kontaktu, klucze obce klienta i adresu oraz znaczniki czasu wejścia do kubełka i aktualizacji rekordu.', acceptance: ['Dowodem jest KSZTAŁT zwróconego obiektu leada na najwyższym poziomie: test porównuje Object.keys(lead) z zadeklarowanym zestawem jako zbiory (posortowana równość), a nie toContain, toMatchObject ani expect(lead.notatki_wewnetrzne).toBeUndefined(). Asercja po jednej nazwie przechodzi także dla obiektu z dwudziestoma innymi kolumnami obok, dla literówki w nazwie sprawdzanego pola i dla rekordu, w którym akurat ta jedna wartość jest pusta w bazie. Ten sam standard dowodowy co w SEC-LEADS-LIST-MINIMIZE i z tego samego powodu', 'Zestaw jest wyprowadzony z konsumenta, nie z wygody. leads-client.tsx zużywa dziś: identyfikator (klucz wiersza, skrót #8 znaków w kolumnie, wyszukiwarka), status (filtr etapu, znacznik etapu, menu akcji kontekstowych, aktualizacja optymistyczna), znacznik utworzenia (data wpłynięcia w kolumnie, wyliczenie opóźnienia powyżej 24 h, sortowanie zapasowe), datę rezerwacji audytu (kolumna terminu audytu, sortowanie główne, prop daty montażu dla dialogu przypisania ekipy), estymowaną wycenę (kolumna kwoty) oraz znacznik wystawienia oferty (prop dialogu zwrotu do lejka). Kryterium przynależności brzmi „jest zużywane przez konsumenta", nie „nie jest wrażliwe" — pole zużywane wyłącznie do wyliczenia po stronie klienta (np. próg opóźnienia) należy do zestawu tak samo jak pole wyświetlane wprost', 'Pola RODO-wrażliwe — notatka wewnętrzna i surowe odpowiedzi triage — nie występują w zwróconym obiekcie pod ŻADNĄ nazwą: test iteruje po kluczach leada i sprawdza, że żaden nie należy do zbioru wrażliwych, zamiast odpytywać o nie po jednej nazwie. Sformułowanie jest celowo niezależne od dzisiejszych nazw kolumn — kolumny tej tabeli czekają na przemianowanie wg ADR-002 (docs/architecture/NAMING.md) i przemianowanie NIE może przywrócić przecieku ani unieważnić testu', 'Zawężenie jest zadeklarowane w select samego zapytania, nie dopiero w map po jego wykonaniu. Rekord z notatką wewnętrzną wciągnięty do pamięci serwera „na chwilę" trafia potem do logu wyjątku, do zrzutu diagnostycznego i do pierwszego spreada, który ktoś napisze w dobrej wierze — dzisiejszy kod właśnie spreadem (...lead) buduje wynik, więc każde pole pobrane jest polem wysłanym. Mapowanie na wyjściu wolno zostawić jako drugą linię obrony, ale samo w sobie tego wymagania nie zamyka', 'Zawężenie NIE rusza relacji: klient, adres, zespół w instalacji i audytor pozostają dokładnie w kształcie zamkniętym przez SEC-LEADS-LIST-MINIMIZE i SEC-ASSIGNMENT-POOL-MINIMIZE. Naprawa realizująca to ID nie może tamtych zawężeń cofnąć ani rozszerzyć; test kształtu obejmuje oba poziomy naraz, bo dopiero komplet dowodzi, że przez tę granicę nie przechodzi nic ponad to, co widać na ekranie', 'Filtry, sortowanie, stronicowanie i liczniki zostają NIETKNIĘTE: warunki where (w tym kubełki cold/rejected_auto po powodzie auto-odrzucenia oraz zawężenie zakresu audytora), orderBy po dacie rezerwacji i dacie utworzenia, skip/take oraz osobne groupBy liczące leady w podziale na statusy. Zawężenie kolumn jest dokładnie tą klasą zmiany, przy której warunek filtrujący albo klucz sortowania znika niezauważony, bo zniknięcie nie psuje kompilacji. Pole potrzebne wyłącznie do filtrowania po stronie bazy wolno w zapytaniu wykorzystać, ale nie wolno go wysłać do klienta, jeśli nikt go nie renderuje. Istniejące testy filtrów, sortowania i liczników statusów zostają i muszą przechodzić po zmianie', 'Kontrola pozytywna: po zmianie lista nadal pokazuje to, co ma pokazywać — skrócony identyfikator, datę wpłynięcia, znacznik opóźnienia powyżej 24 h dla nowego leada, znacznik etapu, kwotę estymowaną i termin audytu, a dialog zwrotu do lejka nadal dostaje datę wystawienia oferty, zaś dialog przypisania ekipy datę montażu. Bez tego przypadku cały zestaw testów przechodzi także dla funkcji zwracającej same identyfikatory albo pustą listę, czyli dla naprawy, która zabiera dyspozytorowi widok', 'Klucz obcy audytora (audytor_id) jest przypadkiem granicznym rozstrzygniętym jawnie: NIE jest renderowany — nazwę audytora widok bierze z zawężonej relacji audytor — ale dzisiejsza aktualizacja optymistyczna w leads-client.tsx zapisuje go do stanu przy przypisaniu audytora, mimo że nikt tej wartości potem nie czyta. Domyślnym rozstrzygnięciem jest wyłączenie go z zestawu i usunięcie tego zapisu ze stanu; pozostawienie pola wymaga wskazania miejsca, które faktycznie je czyta. „Kod to zapisuje" nie jest tożsame z „konsument tego potrzebuje", a to jest właśnie ten wyjątek, przy którym zestaw zaczyna puchnąć z rozpędu', 'Typ Lead w leads-client.tsx przestaje być wyprowadzany z Prisma.leadyGetPayload przez Omit i staje się wąskim, nazwanym typem wyniku getLeads(). Dopóki typ deklaruje komplet skalarów modelu, zawężenie zapytania nie skompiluje się albo — gorzej — skompiluje się i zostawi w komponencie pola, których w danych już nie ma. Rzutowanie jest niedopuszczalne (zakaz z CLAUDE.md), a „as Lead" występuje dziś w tym pliku trzykrotnie w aktualizacjach optymistycznych i po zawężeniu musi zniknąć razem z szerokim typem, nie zostać użyte do jego obejścia', 'Zestaw pól jest ZAMKNIĘTY także dla przyszłych konsumentów: każdy nowy komponent czytający wynik getLeads() dziedziczy to ograniczenie i nie może dobrać sobie kolejnej kolumny przy okazji własnej zmiany. Rozszerzenie zestawu wymaga świadomej zmiany tego wymagania, a przy polu wrażliwym — osobnego ID i decyzji człowieka. Sam typ tego nie wymusi (dopisanie pola kompiluje się bez oporu), więc bramką jest test kształtu i ten zapis', 'Widok szczegółów pojedynczego leada (leads/[id]/page.tsx) jest ŚWIADOMIE poza zakresem. Wykonuje własne, osobne zapytanie i nie korzysta z getLeads(); notatka wewnętrzna i odpowiedzi triage są tam operatorowi faktycznie potrzebne — po to ten ekran istnieje. Zapisane wprost, żeby nikt nie zawęził tamtego zapytania „przy okazji" tej naprawy i nie zabrał działającej funkcjonalności pod pozorem bezpieczeństwa. Jedno zapytanie oddaje jeden komplet danych na świadome żądanie, drugie oddaje pięćdziesiąt bez żądania — to jest różnica, która dzieli te dwa przypadki'], risk: 'MEDIUM' }),
  R('CRM-LEAD-UPDATE-ADMIN-DISPATCHER', { status: 'TODO', source: 'review 2026-08-25 (luka znaleziona przy przeglądzie apps/b2b-web/src/app/(dashboard)/leads/[id]/actions.ts) — brak źródła w dokumentach architektury; regułę niesie wyłącznie macierz w contracts/rbac.contract.mjs (leads.update = [admin, dyspozytor]), która była poprawna, zanim powstało to wymaganie, bo kod tego pliku nigdy do niej nie zajrzał — czwarte wystąpienie wzorca SEC-AUTHZ-USER-MGMT / CRM-CREW-UPDATE-ADMIN-ONLY, tym razem w pliku BLIŹNIACZYM wobec już naprawionego: leads/actions.ts sprawdza rolę w trzech akcjach, leads/[id]/actions.ts w żadnej', domain: 'security', statement: 'Zmiana rekordu leada z widoku szczegółów jest dostępna dla ról admin ORAZ dyspozytor — nie jest to bramka admin-only — i jest egzekwowana po stronie serwera przed jakimkolwiek zapytaniem do bazy. Dotyczy dwóch Server Actions, które dziś nie mają ŻADNEGO sprawdzenia roli: updateLeadAuditor (przypisanie/odpięcie audytora, pociągające za sobą zmianę statusu leada) i updateLeadData (nadpisanie danych kontaktowych klienta, adresu i estymowanej wyceny). Dowolne zalogowane konto — w tym monter i audytor spoza sprawy — może dziś przez bezpośrednie wywołanie tych akcji podmienić audytora dowolnego leada, cofnąć jego status albo nadpisać telefon i e-mail klienta. Reguła obowiązuje każdą kolejną akcję zapisującą do rekordu leada w tym pliku, nie tylko te dwie.', acceptance: ['Wywołanie updateLeadAuditor oraz updateLeadData przez konto o roli audytor albo monter jest odrzucone po stronie serwera ZANIM powstanie pierwsze zapytanie do bazy — także zapytanie odczytujące. Test dowodzi, że klient Prismy nie został w ogóle użyty (żadnego findUnique, żadnego update), a nie tylko że wartości w bazie się nie zmieniły. Test wywołuje Server Action bezpośrednio, z pominięciem interfejsu, bo interfejs nie jest granicą uprawnień: Prisma omija RLS, więc jedyną granicą jest kod akcji', 'Decyzję podejmuje can(actorRole, "leads", "update") z wygenerowanego kontraktu, a actorRole pochodzi z sesji serwera (getCurrentActorRole()) — nigdy z parametru akcji ani z żadnej wartości przesłanej przez klienta. Literał role === "admin" albo lista ról wypisana w kodzie akcji jest naruszeniem, bo dubluje macierz i przy jej zmianie zostanie w tyle. Wzorcem jest assignCrewAction z leads/actions.ts, gdzie ten sam zasób i ta sama zdolność są już sprawdzane poprawnie', 'Przepuszczony jest wyłącznie wynik "yes". Warunek can(...) !== "no" jest naruszeniem: obie akcje biorą identyfikator leada z parametru i nigdy nie sprawdzają, czyj to rekord, a wariant "audytor:own" jest już obecny w macierzy przy leads.read — dopisanie go kiedyś do leads.update zamieniłoby taki warunek w prawo audytora do zmiany DOWOLNEGO leada, nie własnego', 'Brak roli oznacza odmowę (fail-closed): brak sesji, e-mail spoza authorized_users oraz wartość roli nierozpoznana przez ROLES dają odmowę — getCurrentActorRole() zwraca w tych trzech przypadkach null, a null nie może trafić do can() jako rola przepuszczająca. Wyjątek z bazy przy samym zapytaniu o rolę również kończy się odmową, nie zapisem: dzisiejsze ciało obu akcji jest opakowane w try/catch zwracający komunikat błędu, więc sprawdzenie roli WEWNĄTRZ tego try zamieni awarię odczytu roli w zwykły błąd zapisu zamiast w twardą odmowę — bramka musi stać przed nim albo mieć własną obsługę. Test wykonuje te cztery próby realnie, sprawdzenie konfiguracji nie wystarcza (wzorem CRM-AUDYT-AC1)', 'Kontrola pozytywna dla OBU dozwolonych ról osobno: wywołanie przez konto admin kończy się zapisem i wywołanie przez konto dyspozytor kończy się zapisem, dla każdej z dwóch akcji. Bez przypadku dyspozytora zestaw testów przechodzi także dla bramki błędnie zawężonej do samego admina, czyli dla „naprawy", która odbiera dyspozytorowi jego podstawowe narzędzie pracy i zostanie odkryta dopiero na produkcji. Bez przypadku admina zestaw przechodzi dla akcji odrzucającej wszystkich', 'Bramka DOKŁADA się przed istniejącą logiką biznesową, nie zastępuje jej ani nie osłabia. W updateLeadAuditor bez zmian zostają: odrzucenie przypisania audytora z wyłączonym znacznikiem aktywności (BLOCKER 4 z WO CRM-SAFE-RECORD-ACTIONS — pula w interfejsie już takich nie pokazuje, ale akcja jest wywoływalna z jej pominięciem) oraz przejścia statusu NEW_LEAD → AWAITING_AUDIT przy przypisaniu i AWAITING_AUDIT → NEW_LEAD przy odpięciu. Istniejące testy tych zachowań zostają i muszą przechodzić po zmianie — sprawdzenie roli nie jest ich substytutem, bo dotyczy innego pytania: kto wywołuje, a nie co wolno zapisać', 'Bramka pokrywa CAŁĄ akcję updateLeadData, łącznie z gałęzią tworzącą nowy rekord klienta i nowy rekord adresu, gdy lead ich jeszcze nie ma. Odrzucone wywołanie nie może zostawić po sobie osieroconego rekordu klienta ani adresu — test sprawdza to wprost dla leada bez przypiętego klienta, bo to jedyna ścieżka, w której akcja tworzy dane od zera i w której bramka postawiona „gdzieś w środku" zdąży już coś zapisać', 'Odmowa jest odróżnialna dla wywołującego i nie da się jej pomylić z sukcesem: obie akcje zwracają już dziś kształt z polem powodzenia i polem błędu, więc odmowa zwraca ten sam kształt z jawnym komunikatem o braku uprawnień (wzorem "Brak uprawnień do przypisania ekipy." z leads/actions.ts), nigdy wyjątku 500 ani cichego zwrotu sukcesu. Komunikat nie zdradza, czy lead o podanym identyfikatorze istnieje', 'Ukrycie kontrolek edycji i wyboru audytora dla ról bez leads.update jest wymagane, ale stanowi warstwę osobną od bramki serwerowej i jest testowane osobno (wzorem CRM-DELETE-ADMIN-ONLY) — samo ukrycie nie zamyka luki, bo Server Action pozostaje wywoływalna z pominięciem interfejsu. Naprawa polegająca wyłącznie na zmianie komponentu nie zamyka tego wymagania', 'Bramka obowiązuje każdą ścieżkę zapisującą do rekordu leada z tego pliku, nie tylko te dwie akcje — akcja dopisana później bez sprawdzenia roli będzie tą samą luką pod inną nazwą. Granicę wyznacza tabela docelowa zapisu, nie plik, w którym akcja mieszka (ta sama zasada co w CRM-CREW-UPDATE-ADMIN-ONLY)', 'deleteLead z tego samego pliku NIE należy do tego wymagania: również nie ma dziś żadnego sprawdzenia roli, ale pokrywa je CRM-DELETE-ADMIN-ONLY (leads.delete = [admin], czyli inny zestaw ról niż tutaj). Zapisane wprost, żeby żadne z dwóch wymagań nie zostało uznane za spełnione naprawą tego drugiego, i żeby bramka delete nie została przez pomyłkę oparta na leads.update, co dałoby dyspozytorowi prawo usuwania leadów', 'Odczyt leada i jego rekordów powiązanych jest ŚWIADOMIE poza zakresem: leads/[id]/page.tsx nie sprawdza roli przed pobraniem karty leada, a macierz mówi leads.read = [admin, dyspozytor, audytor:own]. To inna zdolność, z wariantem :own, którego can() nie rozstrzyga sam z siebie — wymaga osobnego ID (wzorem rozszerzenia SEC-AUTHZ-USER-MGMT o read). Zapisane wprost, bo naprawa samych zapisów zostawia plik w stanie, który przy pobieżnym przeglądzie wygląda na domknięty', 'Zdolność leads.assign = [admin] istniejąca w macierzy jest ŚWIADOMIE nieużywana przez to wymaganie i pozostaje otwartym pytaniem do człowieka. Żaden kod w repozytorium nie woła dziś can() ze zdolnością "assign" dla żadnego zasobu, a przypisanie audytora jest zarazem zapisem do rekordu leada (update) i przypisaniem (assign) — dwie zdolności o RÓŻNYCH zestawach ról. To wymaganie idzie za dzisiejszym precedensem kodu (assignCrewAction używa leads.update), bo alternatywa zawęża prawa dyspozytora, czego nie wolno zrobić przy okazji naprawy luki. Przeklasyfikowanie przypisania audytora na leads.assign jest zmianą łamiącą kompatybilność zachowania panelu, wymaga decyzji człowieka, osobnego ADR i osobnego ID — nie wolno go przemycić podmianą argumentu w can()'], risk: 'HIGH' }),
  R('SEC-AUTHZ-B2B-MUTATIONS', { status: 'DONE', source: 'docs/workorders/SEC-AUTHZ-B2B-MUTATIONS.md (skan tools/kk-authz-gate.mjs, 2026-08-26) — brak źródła w dokumentach architektury; regułę niesie wyłącznie macierz w contracts/rbac.contract.mjs, do której kod akcji nigdy nie zaglądał. Rejestracja RETROAKTYWNA: implementacja i testy zamknięte przed powstaniem tego ID, wpis odzwierciedla stan faktyczny warstwy Server Action, nie pierwotny plan Work Ordera. Ten sam wzorzec błędu co SEC-AUTHZ-USER-MGMT, CRM-CREW-UPDATE-ADMIN-ONLY i CRM-LEAD-UPDATE-ADMIN-DISPATCHER, tyle że wykryty maszynowo i na wszystkich zasobach naraz — trzy poprzednie wymagania zawężone do konkretnych nazw funkcji zostawiły dziurę przy czwartej akcji, dlatego to ID wiąże regułę z TABELĄ DOCELOWĄ ZAPISU, nie z plikiem ani nazwą funkcji', domain: 'security', statement: 'Każda Server Action w apps/b2b-web, która wykonuje mutację przez Prismę (create, update, delete, także wewnątrz $transaction), rozstrzyga uprawnienie przez can(actorRole, <zasób>, <zdolność>) z @klikklima/contracts, zanim powstanie pierwsze zapytanie do bazy. Granicę wyznacza tabela docelowa zapisu, nie plik, w którym akcja mieszka. Skaner tools/kk-authz-gate.mjs znalazł 14 takich akcji BEZ jakiegokolwiek sprawdzenia roli — dowolne zalogowane konto, łącznie z audytorem i monterem, mogło bezpośrednim wywołaniem POST skasować klienta wraz z historią, skasować leada z pominięciem naprawionej wcześniej bramki, przesunąć dowolnego leada na dowolny status, oznaczyć przesyłkę jako wysłaną i dostarczoną albo cofnąć gotowy montaż do rollbacku. Interfejs nie jest granicą uprawnień: Prisma omija RLS (pułapka 1 z CLAUDE.md), więc jedyną granicą jest kod akcji. Zasoby i zdolności pochodzą z istniejących wierszy MATRIX — to wymaganie NIE zmienia macierzy uprawnień, tylko domaga się, żeby kod w ogóle do niej zajrzał.', acceptance: ['Bramka przepuszcza WYŁĄCZNIE wynik "yes" z can(). Warunek !== "no" oblewa ten zestaw: updateInstallationStatus jest wywoływane rolą monter, dla której can("monter","installations","update") zwraca "own" (rbac.contract.mjs:32), i MUSI zostać odrzucone, bo żadne miejsce w repozytorium nie implementuje dziś sprawdzenia własności rekordu. Wariant :own zostaje świadomie odcięty do czasu osobnego wymagania — furtka "own znaczy prawie yes" oddaje prawo do DOWOLNEGO rekordu, nie do własnego', 'Odmowa zachodzi PRZED pierwszym zapytaniem do bazy, także odczytującym. Dotyczy w szczególności advanceLeadStatus, które zaczyna od findUnique: test dowodzi, że po odmowie findUnique nie zostało wywołane ani razu — inaczej akcja jest oraklem istnienia i statusu dowolnego leada dla konta bez uprawnień. Bramka stoi też PRZED otwarciem $transaction (shipLogisticsOrder, markAsDelivered): sprawdzenie w środku transakcji daje rollback zamiast odmowy, czyli w logach jest nieodróżnialne od awarii bazy', 'Dowodem odmowy jest BRAK WYWOŁANIA metody Prismy, nie niezmieniona wartość w bazie: test sprawdza, że klienci.delete, adresy.create, usterki_incidents.delete, instalacje.update, instalacje.delete, leady.update, serwisy.delete nie zostały wywołane ani razu dla roli bez zdolności', 'Kontrola pozytywna wykonana dla KAŻDEJ dozwolonej roli osobno. Dla akcji logistycznych i advanceLeadStatus test przechodzi osobno admin i osobno dyspozytor (założenie Z1 z WO: pole actor w contracts/funnel.contract.mjs opisuje typowego wykonawcę przejścia, nie listę uprawnionych ról, i nie odbiera administratorowi praw, które ma w całej macierzy). Bez tego przypadku zestaw przechodzi także dla bramki błędnie zawężonej do samego admina, czyli dla "naprawy", która zabiera dyspozytorowi narzędzie pracy', 'Zestawy ról dozwolonych i odrzuconych są WYLICZANE dynamicznie z ROLES i can() z wygenerowanego kontraktu, nie wpisane w test jako literały. Przyszła zmiana macierzy ma automatycznie przesunąć rolę do właściwego koszyka, zamiast cicho unieważnić test', 'Fail-closed w trzech realnie wykonanych wariantach: getCurrentActorRole() zwraca null (brak sesji albo e-mail spoza authorized_users), getCurrentActorRole() rzuca wyjątek, rola nierozpoznana przez ROLES. We wszystkich: odmowa, nie zapis i nie nieobsłużony wyjątek. Bramka umieszczona wewnątrz istniejącego try/catch akcji zamieniłaby błąd odczytu roli w zwykły komunikat "nie udało się zmienić statusu" zamiast w twardą odmowę', 'Rola pochodzi wyłącznie z sesji serwera (getCurrentActorRole()), nigdy z argumentu akcji ani z jakiejkolwiek wartości przesłanej przez klienta. Test wywołuje Server Action bezpośrednio, z pominięciem interfejsu', 'Odmowa jest ODRÓŻNIALNA OD SUKCESU: akcje zwracają { success: false, error } zamiast void i zamiast wyjątku. Przed zmianą UI (customers-client.tsx, incidents-client.tsx, services-client.tsx, installations-client.tsx, logistics-client.tsx) pokazywało komunikat sukcesu i przeładowywało stronę niezależnie od wyniku, a każdy wyjątek tłumaczyło na "blokują go klucze obce" — czyli odmowa z powodu roli była nieodróżnialna od konfliktu FK. Zmiana sygnatury jest częścią naprawy, nie kosmetyką', 'Odmowa dla roli bez uprawnień zachodzi NIEZALEŻNIE od istnienia rekordu — usunięcie nieistniejącego identyfikatora nie może być ścieżką, która obchodzi bramkę ani która wypuszcza nieobsłużone P2025 do interfejsu', 'addCustomerAddress jest bramkowane can(role,"clients","update") — decyzja D1(a) człowieka: adres należy do agregatu klienta i NIE dostaje własnego zasobu w RESOURCES. Zapis odnotowany jawnie, bo jest świadomym wyjątkiem od zasady "nazwy tabel objętych uprawnieniami są tożsame z RESOURCES" (CLAUDE.md, ADR-002); dodanie zasobu addresses pozostaje otwartą opcją i wymaga osobnego ID', 'deleteLogisticsOrderAction nie zawiera własnej bramki ani własnego prisma.leady.delete — deleguje do deleteLeadAction z leads/actions.ts i przekazuje dalej jej wynik, zachowując revalidatePath. Test dowodzi, że w całym apps/b2b-web istnieje DOKŁADNIE JEDNA ścieżka usunięcia leada. Druga, choćby zabramkowana, jest długiem, który wraca', 'Dwie martwe funkcje usunięte w całości zamiast zabramkowane (decyzje D3 i D4 człowieka): installations/actions.ts assignCrew — duplikat bez wywołań i homonim akcji kontraktowej T05, którą realizuje assignCrewToLead z walidacją certyfikatów; leads/actions.ts updateLeadStatus — ściśle słabsza wersja advanceLeadStatus BEZ walidacji dozwolonych przejść, pozwalająca cofnąć INSTALLATION_COMPLETED do NEW_LEAD. Eksport z modułu "use server" ma żywy endpoint POST, więc "nikt tego nie woła" nigdy nie było zabezpieczeniem. Dopisanie któregokolwiek z nich z powrotem wymaga świadomej decyzji, nie odruchu', 'Rola uprawniona nadal wykonuje akcję: dla każdej z naprawionych funkcji istnieje przypadek, w którym mutacja faktycznie następuje. Bez tego cały zestaw przechodzi dla bramki odrzucającej wszystkich', 'Poza zakresem tego ID, świadomie i do osobnych wymagań: warstwa UI (ukrycie kontrolek dla ról bez zdolności — CRM-DELETE-ADMIN-ONLY, kryterium 1 i 4), warstwa RLS (polityki FOR DELETE), wpis do audit_log (SEC-AUDIT-LOG, tabela audit_log nie istnieje w schemacie), realizacja wariantu :own, walidacja przejść statusu wg kontraktu lejka zamiast lokalnej mapy ALLOWED_TRANSITIONS, efekty i powiadomienia przejść logistycznych T06/T08/T10-T12 oraz ścieżka webhookowa markDelivered. Zielony kk-authz-gate.mjs NIE jest kryterium akceptacji: skaner odpowiada wyłącznie na pytanie "czy bramka istnieje", nie "czy jest poprawna", i przeszukuje tylko pliki o nazwie actions.ts w apps/b2b-web/src/app — mutacja w Route Handlerze, w lib/ albo w pliku o innej nazwie nie zostanie wykryta. 14 znalezisk to dolna granica, nie liczba ostateczna'], risk: 'HIGH' }),
  // SEC-AUTHZ-B2B-READS: status IMPLEMENTING, nie DONE. Bramki na siedmiu funkcjach odczytowych,
  // sześciu listach i kartotece klienta są zaimplementowane i pokryte (~90 testów w 7 nowych plikach,
  // 822/822 zielone, niezależny audyt rls-security-auditor: werdykt MAJOR, wszystkie znaleziska
  // naprawione). Do DONE brakuje DWÓCH rzeczy, obie wypisane jawnie w acceptance: testu żywotności
  // rozszerzonego kk-authz-gate na katalogu-fikstrze (KK_AUTHZ_SCAN_DIR) oraz tagów @REQ w plikach
  // testowych, bez których kk-trace nie widzi pokrycia. Obie należą do test-author — contract-steward
  // nie ma zakresu zapisu w apps/**/tests/ (tools/kk.config.mjs, agentWriteScopes).
  R('SEC-AUTHZ-B2B-READS', {
    status: 'IMPLEMENTING',
    domain: 'security',
    risk: 'HIGH',
    source: "docs/workorders/SEC-READ-GATES.md (skan AST tools/kk-authz-gate.mjs rozszerzony o odczyty i o pliki inne niż actions.ts, 2026-09-02/03) — brak źródła w dokumentach architektury; regułę niesie wyłącznie MATRIX w contracts/rbac.contract.mjs. Dopełnia SEC-AUTHZ-B2B-MUTATIONS, które jest jawnie zawężone do mutacji, i SEC-RLS-AUDITOR-SCOPE, które objęło JEDEN odczyt (getLeads). Skala znaleziska: siedem eksportowanych funkcji odczytowych (getCustomers, getCrews z crews/actions.ts, getCrews(installationDate) z leads/actions.ts, getAuditors, getInstallations, getUpcomingServices, getIncidents) i siedem stron nie sprawdzało roli WCALE — każde zalogowane konto, w tym monter i audytor, czytało dane osobowe wszystkich klientów wbrew clients.read = [admin, dyspozytor]. Ósmy i najgroźniejszy przypadek — customers/[id]/page.tsx wołająca prisma.klienci.findUnique BEZPOŚREDNIO ze strony — wymykał się skanerowi ograniczonemu do plików o nazwie actions.ts: audytor brał UUID klienta z leada, do którego miał prawo, i przez sam adres URL czytał kartotekę 360 z leadami innych audytorów, omijając SEC-RLS-AUDITOR-SCOPE nie joinem, tylko adresem. Znalezione przez rls-security-auditor przy przeglądzie, nie przez pierwotny skan. Rejestracja RETROAKTYWNA: implementacja i testy powstały przed tym ID (test-author dostał instrukcję pisania bez @REQ, bo wymaganie jeszcze nie istniało), więc wpis odzwierciedla stan FAKTYCZNY kodu po naprawie, nie pierwotny plan Work Ordera.",
    statement: "Każda ścieżka w apps/b2b-web, która ODCZYTUJE dane przez Prismę — Server Action w actions.ts, ale też komponent serwerowy page.tsx wołający klienta wprost — rozstrzyga uprawnienie przez can(actorRole, <zasób>, read) z @klikklima/contracts, ZANIM powstanie pierwsze zapytanie do bazy. Wynik own zawęża zapytanie po właścicielu wyznaczonym z sesji i nigdy nie jest traktowany jak yes. Strona każdego widoku odmawia renderu przez notFound() roli, dla której can() zwraca no. Granicę wyznacza TABELA CZYTANA, nie plik ani nazwa funkcji — Prisma omija RLS (pułapka 1 z CLAUDE.md), więc kod odczytu jest jedyną granicą.",
    acceptance: [
      "getCustomers dla roli, dla której can(role, clients, read) nie zwraca yes (audytor, monter), zwraca { customers: [], totalPages: 0 } ORAZ ani prisma.klienci.findMany, ani prisma.klienci.count nie zostały wywołane. Odcięcie OBU jedną decyzją jest wymagane: odmowa zwracająca totalPages policzone z pełnej tabeli wycieka liczność zbioru PII. Dla admin i dyspozytor osobno lista wraca niepusta.",
      "getCrews (crews/actions.ts) i getAuditors (auditors/actions.ts) — analogicznie dla crews.read i auditors.read; przy odmowie zwracają pustą listę i odpowiadające findMany nie jest wołane. Panel administracyjny nadal pokazuje audytorów z is_active równym false roli uprawnionej (celowa różnica wobec puli przypisań w leads/actions.ts).",
      "getCrews(installationDate) z leads/actions.ts — przy odmowie pusta lista bez zapytania; dla ról uprawnionych istniejący filtr certyfikatów i dostępności działa bez zmian (regresja zamrożona przez crews-cert-availability i availability-pool-filter).",
      "getInstallations — admin i dyspozytor: findMany BEZ klucza zespol_id w where. Monter z dokładnie jednym aktywnym rekordem w zespoly_monterskie: findMany z where.zespol_id równym identyfikatorowi ekipy wyznaczonemu z sesji. Audytor: pusta lista i ZERO wywołań prisma.instalacje.findMany.",
      "getUpcomingServices — monter: OBA zapytania dostają filtr ekipy. prisma.serwisy.findMany dostaje { OR: [ { zespol_id: own }, { AND: [ { zespol_id: null }, { instalacja: { zespol_id: own } } ] } ] }, a prisma.instalacje.findMany (wiersze forecast) dostaje zespol_id równe own obok istniejącego warunku next_service_date. Admin i dyspozytor: żadne z dwóch zapytań nie dostaje filtra ekipy. Audytor: pusta lista i zero wywołań obu.",
      "getIncidents — jak wyżej, na prisma.usterki_incidents, z tym samym wariantem OR.",
      "Dziedziczenie zakresu z instalacji jest ZAWĘŻONE do rekordów bez własnego przypisania: serwis albo usterka jawnie przypisana innej ekipie nigdy nie wpada do zakresu montera, choćby siedziała na jego instalacji. Powód wariantu OR: serwisy.zespol_id i usterki_incidents.zespol_id są nullable i w praktyce puste (przypisanie ekipy żyje na instalacje.zespol_id), więc sam filtr zespol_id równy own pokazałby monterowi listę pustą także dla jego własnych montaży — czyli naprawę, po której funkcja przestaje działać. Serwis osierocony (instalacja_id null i zespol_id null) do zakresu nie trafia.",
      "Identyfikacja zalogowanego montera idzie przez prisma.zespoly_monterskie.findMany({ where: { email }, take: 2 }) i kończy się ODMOWĄ, gdy liczba trafień jest różna od 1. To jest ŚWIADOMA obrona przed brakującym ograniczeniem UNIQUE na kolumnie email w ŻYWEJ bazie: schema.prisma deklaruje @unique i migracja 20260822120000 je tworzy, ale pg_constraint nie zawiera go ani dla zespoly_monterskie, ani dla audytorzy (sprawdzone na bazie 2026-09-02). findUnique po takiej kolumnie zwróciłby wiersz nieokreślony, czyli mógłby wskazać CUDZĄ ekipę. Test na duplikacie (dwa wiersze o tym samym adresie e-mail) dowodzi odmowy, nie wyboru pierwszego wiersza.",
      "Fail-closed montera w pięciu wariantach, każdy osobno i dla wszystkich trzech funkcji z wariantem own: brak sesji, brak adresu e-mail w sesji, getCurrentActorRole() rzuca wyjątek, brak wiersza w zespoly_monterskie o tym adresie, zespoly_monterskie.aktywny równe false. Za każdym razem wynik pusty ORAZ docelowe findMany NIE zostało wywołane. Dowodem jest BRAK WYWOŁANIA, nie pusty wynik: where z zespol_id równym undefined Prisma traktuje jak brak filtra i zwróciłaby WSZYSTKO. Wariant aktywny równy false ma znaczenie osobne, bo middleware.ts sprawdza flagę aktywności wyłącznie dla roli audytor — dla ekipy wyłączonej administracyjnie ta bramka jest JEDYNĄ barierą. Asymetria nazw jest realna i celowo odnotowana: audytorzy.is_active, zespoly_monterskie.aktywny.",
      "Ten sam adres e-mail w audytorzy i w zespoly_monterskie rozstrzyga rola z authorized_users, nie tabela pracownika: konto o roli monter dostaje zakres liczony z ekipy.",
      "Strony customers, crews i auditors wywołują notFound() dla roli, dla której can() nie zwraca yes, i NIE wywołują odpowiadającej funkcji odczytu. Strony installations, services i incidents wywołują notFound() dla wyniku no (audytor, brak roli, wyjątek z getCurrentActorRole), a dla yes i own renderują — monter widzi zakres własnej ekipy. Każde notFound() jest domknięte przez return, żeby kod poniżej nie wykonał się w środowisku, w którym notFound nie przerywa wykonania.",
      "customers/[id]/page.tsx sprawdza can(role, clients, read) równe yes PRZED prisma.klienci.findUnique: dla braku roli i dla wyjątku z getCurrentActorRole findUnique nie jest wołane ani razu. Dla roli uprawnionej i nieistniejącego klienta notFound() nadal pada PO findUnique (zachowanie bez zmian). To jedyne miejsce tej naprawy, w którym strona jest granicą uprawnień z braku warstwy akcji — i dokładnie ten kształt wymusił rozszerzenie skanera poza actions.ts.",
      "Rozróżnienie braku uprawnień od braku danych niesie STRONA (notFound()), a nie typ zwracany akcji: funkcje odczytowe zachowują sygnatury i przy odmowie zwracają wartość pustą. Server Action jest drugą linią obrony przed bezpośrednim POST-em, gdzie nie ma komu wyświetlić komunikatu. Bramka strony jest w tym układzie OBOWIĄZKOWA — bez niej pusta wartość faktycznie gubiłaby odmowę i ta decyzja traci uzasadnienie.",
      "Rola pochodzi wyłącznie z sesji serwera: żadna z siedmiu funkcji nie przyjmuje roli ani identyfikatora ekipy w argumencie. Testy wywołują Server Action bezpośrednio, z pominięciem interfejsu (wektor: bezpośredni POST rolą monter albo audytor). getCurrentActorRole jest memoizowane przez cache(), więc test nie zakłada jednego wywołania na akcję.",
      "Zestawy ról dozwolonych i odrzuconych są WYLICZANE z ROLES i can() z wygenerowanego kontraktu, nie wpisane w test jako literały. Kryteria zakresu są dowodzone na ARGUMENCIE przekazanym do findMany, nie na kształcie zwróconych wierszy — mock zwraca to, co mu wpisano.",
      "Regresja /leads dla audytora: leads/page.tsx woła getAuditors() obok getLeads(), a audytor nie ma auditors.read, więc pula wraca pusta. Strona audytora nadal się renderuje (nie wywala się na pustej puli) i nie pokazuje selektora audytora z cudzymi nazwiskami.",
      "tools/kk-authz-gate.mjs skanuje ODCZYTY (findMany, findUnique, findFirst, findUniqueOrThrow, findFirstOrThrow, count, groupBy, aggregate oraz surowe $queryRaw) obok mutacji ORAZ wszystkie pliki .ts i .tsx pod apps/b2b-web/src/app zamiast samych actions.ts. node tools/kk-authz-gate.mjs kończy się kodem 0 na naprawionym repozytorium i jest wpięty do scripts/verify.sh jako krok blokujący, bez trybu ostrzegawczego. Zielony skaner NIE jest dowodem dla pozostałych kryteriów: odpowiada wyłącznie na pytanie czy bramka istnieje, nie czy para zasób i zdolność jest poprawna.",
      "NIEPOKRYTE (pierwszy z dwóch powodów statusu IMPLEMENTING): nie istnieje test ŻYWOTNOŚCI rozszerzonego skanera. Brakuje przypadku na katalogu-fikstrze przez KK_AUTHZ_SCAN_DIR, dowodzącego, że (a) usunięcie can() z funkcji CZYSTO ODCZYTOWEJ daje kod wyjścia 1 oraz (b) can() postawione PO pierwszym findMany trafia do kategorii ordering. Bez tego rozszerzenie o odczyty jest regułą bramki bez dowodu, że potrafi cokolwiek zablokować — dokładnie ta klasa długu, którą kk-selftest zamyka dla walidatora kontraktu. Do dopisania przez test-author.",
      "NIEPOKRYTE (drugi powód statusu IMPLEMENTING): pliki testowe tej tury — customers-read-authz, crews-read-authz, auditors-read-authz, installations-read-scope, services-read-scope, incidents-read-scope, read-pages-authz — powstały BEZ komentarza @REQ, bo ID jeszcze nie istniało. Dopóki tagi nie zostaną dopisane, kk-trace nie pokaże pokrycia tego wymagania, mimo że testy istnieją i przechodzą. Obecność testów nie jest tym samym co widoczne pokrycie.",
      "Semantyka wariantu own dla montera jest DECYZJĄ tego wymagania, nie cytatem ze specyfikacji: MATRIX mówi monter:own, ale żaden dokument architektury nie definiuje, czym jest własne dla montera. Wywiedziona ze schematu — jedyną kolumną wiążącą rekord z pracownikiem jest zespol_id, a zespoly_monterskie to EKIPA, nie osoba. Jeżeli biznes rozumie własne jako przypisane imiennie monterowi, dzisiejszy model danych tego nie wyraża i wymaga osobnego WO na model danych, nie poprawki tej bramki.",
      "Poza zakresem tego ID, świadomie i do osobnych wymagań: middleware.ts (ograniczenie tras po roli), layout.tsx i navItems (ukrycie pozycji menu dla ról bez read), wariant own dla MUTACJI (nadal odcięty przez SEC-AUTHZ-B2B-MUTATIONS i to rozstrzygnięcie zostaje bez zmian), minimalizacja select w getInstallations i getIncidents (include na pełnych rekordach — realny dług PII, ale inna klasa niż bramka), audit_log dla odczytów (tabela nie istnieje, AUDIT_REQUIREMENTS.mustLog nie zawiera read), apps/b2c-web (inny model ochrony: RLS i supabase-js), Route Handlery oraz lib/ (poza zasięgiem skanera), blind spot skanera dla zapisu export const x = async () => (dziś pod app/ pusty, ale nic nie broni przed dopisaniem takiej akcji jutro), deduplikacja wiersza forecast w getUpcomingServices pod zawężeniem (rozstrzygnięta na korzyść zachowania wiersza forecast — zachowanie widoku, nie granica uprawnień)."
    ]
  }),
  // SEC-EMAIL-UNIQUE: status DONE od 2026-09-03. Obie przyczyny wcześniejszego IMPLEMENTING zamknięte:
  //  (1) Faza B — migracja 20260903061000_security_employee_email_unique_reassert.sql została
  //      napisana, URUCHOMIONA na żywej bazie i ZWERYFIKOWANA odczytem z pg_indexes (oba indeksy
  //      istnieją) oraz próbą zapisu duplikatu (baza odrzuca: 23505 unique_violation). Dodatkowo
  //      prisma migrate diff nie zgłasza już rozbieżności na kolumnie email w żadnej z tabel.
  //      Wektor eskalacji jest zamknięty DWUWARSTWOWO: w kodzie (Faza A) i u źródła (baza).
  //  (2) Tagi @REQ dopisane przez test-author (commit 8bd0c8f) — kk-trace widzi pokrycie
  //      w sec-email-unique-identity-gate.test.ts i sec-email-unique-findunique-freeze.test.ts.
  // Trzeci, mniejszy dług (normalizacja wielkości liter e-maila przy zapisie) NIE blokuje DONE
  // i został wyniesiony do osobnego ID SEC-EMAIL-CASE-NORMALIZE — uzasadnienie w kryterium
  // „Poza zakresem tego ID". Skrótowo: dopasowanie tożsamości jest dokładne co do wielkości liter
  // i fail-closed, więc adres różniący się wielkością liter daje ODMOWĘ (uciążliwość), a nie
  // trafienie w cudzy wiersz. To dług jakości danych i UX, nie luka w tym wymaganiu.
  R('SEC-EMAIL-UNIQUE', {
    status: 'DONE',
    domain: 'security',
    risk: 'HIGH',
    source: "docs/workorders/SEC-EMAIL-UNIQUE.md (Faza A) — rozwinięcie kryterium 8 z SEC-AUTHZ-B2B-READS, które opisało ten brak dla jednej tabeli i jednej funkcji. STAN WYJŚCIOWY (audyt 2026-09-03, przed naprawą): pg_indexes i pg_constraint dla audytorzy i zespoly_monterskie zwracały WYŁĄCZNIE klucz główny — nie było audytorzy_email_key ani zespoly_monterskie_email_key, mimo że schema.prisma deklaruje @unique w obu miejscach, a migracja 20260822120000_fld_availability_split_employee_email_unique.sql zawiera oba CREATE UNIQUE INDEX. Tamta migracja nigdy nie została uruchomiona: supabase_migrations.schema_migrations ma DWA wpisy przy 15 plikach w repozytorium, _prisma_migrations nie istnieje. Dryf był jednokierunkowy (schemat wyprzedzał bazę), więc schema.prisma NIE jest zmieniany — usunięcie @unique zalegalizowałoby podatność zamiast ją zamknąć. STAN DZISIEJSZY (2026-09-03, po naprawie): dryf ZAMKNIĘTY. Migracja 20260903061000_security_employee_email_unique_reassert.sql uruchomiona na żywej bazie i zweryfikowana trzema niezależnymi odczytami — pg_indexes pokazuje audytorzy_email_key i zespoly_monterskie_email_key jako UNIQUE INDEX na (email); próba wstawienia duplikatu kończy się błędem 23505 unique_violation (ograniczenie DZIAŁA, nie tylko istnieje, dane testowe posprzątane); prisma migrate diff nie zgłasza już różnicy na kolumnie email (pozostałe rozbieżności diffa dotyczą kluczy obcych w availability_declarations i employee_consents i są NIEZWIĄZANE z tym ID). Dane bezkolizyjne w chwili audytu: audytorzy 3 wiersze, zespoly_monterskie 2 wiersze, zero duplikatów także po lower(email), zero pustych stringów. Rejestracja RETROAKTYWNA: kod i testy Fazy A powstały przed tym ID, więc wpis opisuje stan FAKTYCZNY repozytorium po naprawie, nie pierwotny szkic Work Ordera — w dwóch miejscach różni się od niego jawnie (patrz kryterium o setSelfAvailabilityAction).",
    statement: "Żadna ścieżka w apps/b2b-web nie wyznacza tożsamości zalogowanego pracownika przez prisma.<tabela>.findUnique({ where: { email } }). Wyznaczenie „czyj to rekord” idzie przez findMany({ where: { email }, take: 2 }) i kończy się ODMOWĄ, gdy liczba trafień jest różna od 1. Powód: findUnique kompiluje się wyłącznie dzięki @unique w schema.prisma, czyli dzięki DEKLARACJI, a nie dzięki sprawdzeniu bazy — przy duplikacie zwróciłby wiersz nieokreślony, czyli mógłby wskazać CUDZĄ osobę lub ekipę. Gdy to wymaganie powstawało, deklaracja faktycznie rozjeżdżała się z żywą bazą. Od 2026-09-03 ograniczenie na bazie ISTNIEJE, ale reguła NIE jest przez to nieaktualna i nie podlega cofnięciu: zależność od obietnicy schematu jest zła sama w sobie, a indeks da się zdjąć jednym DROP INDEX (sekcja ROLLBACK migracji). findMany weryfikuje fakt zamiast ufać deklaracji i to jest trwały powód jego użycia.",
    acceptance: [
      "Sześć wywołań findUnique po kolumnie email zostało zamienionych na findMany z take: 2 i odmową przy matches.length !== 1: getLeads (leads/actions.ts, audytorzy), getLeadDetail (leads/[id]/actions.ts, audytorzy), setSelfAvailabilityAction i acceptLegalDocumentVersionAction w auditors/actions.ts (audytorzy) oraz te same dwie funkcje w crews/actions.ts (zespoly_monterskie). take: 2 jest celowe — wystarcza do stwierdzenia „więcej niż jeden”, nie ciągnie tabeli.",
      "getLeads przy duplikacie zwraca odmowę i NIE buduje scopeWhere — dowodem jest brak wywołania prisma.leady.findMany, nie pusta lista. Where z audytor_id równym undefined Prisma traktuje jak brak filtra i zwróciłaby WSZYSTKIE leady.",
      "getLeadDetail przy duplikacie zwraca odmowę NIEODRÓŻNIALNĄ od odpowiedzi na lead nieistniejący (utrzymanie AC4 z SEC-RLS-AUDITOR-SCOPE) — duplikat tożsamości nie może stać się oraklem istnienia rekordu.",
      "acceptLegalDocumentVersionAction (audytorzy i crews, osobno) przy duplikacie odmawia i NIE wywołuje employeeConsent.create. To był najgroźniejszy z sześciu przypadków: obie funkcje nie porównywały niczego — ani id, ani nic innego — więc zapisywały zgodę na dokument prawny dla wiersza, który akurat wrócił z bazy. Skutek jest nieodwracalny i sięga poza system.",
      "setSelfAvailabilityAction (audytorzy i crews, osobno) przy duplikacie odmawia i NIE wywołuje availabilityDeclaration.upsert. Istniejące porównanie own.id !== id NIE wystarczało: gdy dwie RÓŻNE osoby mają ten sam adres, wywołujący A podający id osoby B trafiał w wiersz B i porównanie przechodziło.",
      "ŚWIADOME ODSTĘPSTWO OD WORK ORDERA (AC-A6), obie kopie setSelfAvailabilityAction: bramka sprawdza WYŁĄCZNIE matches.length !== 1 i NIE sprawdza is_active ani aktywny. Ta akcja jest celowo rozłączna z blokadą administratora — decyzja D-A z FLD-AVAILABILITY-SPLIT, udokumentowana w komentarzu nad funkcją: pracownik zablokowany administracyjnie nadal może zadeklarować własną niedostępność. Dopisanie tam bramki aktywności byłoby cichym cofnięciem wcześniejszego rozstrzygnięcia. Sprawdzenie aktywności zostaje w pozostałych czterech miejscach: acceptLegalDocumentVersionAction (obie kopie), getLeads i getLeadDetail. Asymetria nazw kolumn jest realna i celowa: audytorzy.is_active, zespoly_monterskie.aktywny.",
      "Ścieżka pozytywna nietknięta: przy dokładnie jednym dopasowaniu każda z sześciu funkcji zachowuje się jak przed zmianą. Bez tego kryterium cały zestaw przechodzi dla „naprawy”, która odmawia wszystkim.",
      "Fail-closed bez zmian względem SEC-READ-GATES: brak sesji, brak adresu e-mail w sesji, wyjątek z getCurrentActorRole, zero dopasowań — za każdym razem odmowa, nigdy where zbudowane z undefined.",
      "Test statyczny zamraża regułę: w apps/b2b-web/src nie istnieje findUnique z kluczem email. Bez zamrożenia siódme wywołanie dopisane za miesiąc wraca do punktu wyjścia — wzorzec przejęty z rls-deny-by-default-freeze.test.ts.",
      "Ten sam adres e-mail w audytorzy i w zespoly_monterskie pozostaje legalny (ograniczenie UNIQUE działa per tabela; ai.projectmanager.sznurowski@gmail.com żyje dziś w obu). Rozstrzyga rola z authorized_users, nie tabela pracownika — kryterium 10 z SEC-AUTHZ-B2B-READS, bez zmian.",
      "POKRYTE 2026-09-03: ograniczenie UNIQUE istnieje w ŻYWEJ bazie na audytorzy.email i zespoly_monterskie.email. Postawiła je migracja 20260903061000_security_employee_email_unique_reassert.sql — idempotentna, z blokiem strażniczym DO $$ RAISE EXCEPTION sprawdzającym duplikaty w OBU tabelach PRZED pierwszym CREATE (plik zawiera dwa CREATE UNIQUE INDEX, więc bez tego duplikat w drugiej tabeli zostawiłby bazę w stanie połowicznym), z nazwami audytorzy_email_key i zespoly_monterskie_email_key wg konwencji Prisma dla @unique, bez CONCURRENTLY. Dowód nie jest obecnością pliku ani zielonym testem statycznym, tylko odczytem z bazy: pg_indexes zwraca oba indeksy jako UNIQUE na (email), a próba wstawienia duplikatu jest odrzucana błędem 23505 unique_violation. Migracja niczego nie czyściła automatycznie — dane były bezkolizyjne, a rozstrzygnięcie duplikatu należałoby do człowieka, bo skasowanie e-maila odcina komuś dostęp. Plik 20260822120000 ma komentarz nagłówkowy stwierdzający, że jest martwy i że ograniczenie postawił nowszy plik.",
      "POKRYTE 2026-09-03: pliki sec-email-unique-identity-gate.test.ts i sec-email-unique-findunique-freeze.test.ts mają tag @REQ SEC-EMAIL-UNIQUE (dopisany przez test-author, commit 8bd0c8f) i kk-trace widzi pokrycie. Kryterium zostaje w rejestrze jako zapis powtarzalnej pułapki: ID zarejestrowane RETROAKTYWNIE, po fazie RED, zostawia testy bez tagu, a obecność przechodzącego testu nie jest tym samym co widoczne pokrycie; contract-steward nie ma zakresu zapisu w apps/**/tests/ (tools/kk.config.mjs, agentWriteScopes), więc tagi zawsze dopisuje test-author.",
      "Bramka w kodzie NIE stała się zbędna po uruchomieniu migracji 2026-09-03 i nie podlega usunięciu — to kryterium jest teraz WAŻNIEJSZE niż przed naprawą, bo „baza już tego pilnuje” jest najbardziej prawdopodobnym argumentem za jej skasowaniem. Dwa powody, oba nadal aktualne: sekcja ROLLBACK migracji to dwa DROP INDEX, a ochrona znikająca razem z indeksem nie jest ochroną; findUnique po e-mailu kompiluje się dzięki DEKLARACJI @unique w schema.prisma, a nie dzięki sprawdzeniu bazy — zgodność deklaracji z bazą jest stanem faktycznym z konkretnego dnia, nie gwarancją. Usunięcie bramki wymaga osobnej, jawnej decyzji człowieka; test statyczny sec-email-unique-findunique-freeze.test.ts zamraża regułę.",
      "Poza zakresem tego ID, wyniesione do SEC-EMAIL-CASE-NORMALIZE: normalizacja wielkości liter e-maila przy zapisie (.toLowerCase() w auditors/schema.ts i crews/schema.ts) plus nieautorytatywny pre-check findFirst w czterech akcjach create/update. DLACZEGO TO NIE BLOKUJE DONE: dopasowanie tożsamości w sześciu naprawionych miejscach jest dokładne co do wielkości liter i fail-closed, więc gdy wiersz ma Jan@X.pl, a sesja Supabase podaje jan@x.pl, wynikiem jest ZERO dopasowań i ODMOWA — pracownik traci dostęp do własnych danych, ale NIGDY nie trafia w cudzy wiersz. To dług jakości danych i UX, nie luka w tym wymaganiu, i dlatego przestaje być tu kryterium akceptacji. Ograniczenie stanęło na SUROWEJ kolumnie, nie na lower(email), świadomie: Prisma nie wyraża funkcyjnego indeksu unikalnego jako @unique, więc wariant lower() wyprodukowałby nowy dryf, lecząc stary — konsekwencją jest właśnie to, że Jan@X.pl i jan@x.pl współistnieją mimo UNIQUE.",
      "Poza zakresem tego ID, świadomie: zmiana contracts/rbac.contract.mjs (macierz jest poprawna — problem był w wyznaczaniu own, nie w regule), usunięcie @unique ze schema.prisma, NOT NULL na kolumnie email (odrzucone już przy FLD-AVAILABILITY-SPLIT — adresu nie da się zbackfillować, a zawężenie typu wymaga ADR; konsekwencja: pracownik bez e-maila nie ma ścieżki samoobsługi), powiązanie audytorzy i zespoly_monterskie z authorized_users kluczem obcym zamiast po wartości e-maila (właściwa docelowa naprawa całej klasy problemu, wymaga ADR i migracji danych — odnotowane jako dług), UNIQUE na e-mailu w pozostałych tabelach (authorized_users.email deklaruje @unique w schema.prisma, ale NIE zostało to sprawdzone na żywej bazie — jeśli tam też go brak, ta sama klasa błędu dotyczy getCurrentActorRole, czyli warstwy wyznaczającej ROLĘ), naprawa ewidencji migracji (schema_migrations rozjechane z repozytorium — to przyczyna, a to wymaganie leczy jeden objaw; osobne WO na prisma migrate diff całego schematu wpięty do scripts/verify.sh)."
    ]
  }),
  // SEC-EMAIL-CASE-NORMALIZE: wydzielone z SEC-EMAIL-UNIQUE przy zamykaniu tamtego ID (2026-09-03),
  // żeby zamknięcie wymagania nie skasowało jawnie odłożonego długu. Osobne ID, bo to inna warstwa
  // (walidacja zapisu, nie wyznaczanie tożsamości) i inne ryzyko: skutkiem jest utrata dostępu
  // przez pracownika, nie dostęp do cudzych danych.
  R('SEC-EMAIL-CASE-NORMALIZE', {
    status: 'TODO',
    domain: 'security',
    risk: 'MEDIUM',
    source: 'Wydzielone z SEC-EMAIL-UNIQUE (kryterium „Poza zakresem tego ID"). Stan na 2026-09-03 zweryfikowany w kodzie: ani apps/b2b-web/src/app/(dashboard)/auditors/schema.ts, ani .../crews/schema.ts nie wywołuje .toLowerCase() na polu email — obie definicje kończą się na .trim().email(). Ograniczenie UNIQUE na żywej bazie (SEC-EMAIL-UNIQUE, uruchomione 2026-09-03) stoi na SUROWEJ kolumnie, więc adresy różniące się wyłącznie wielkością liter są dla bazy różnymi wartościami i przechodzą.',
    statement: 'Adres e-mail pracownika jest zapisywany w postaci znormalizowanej (małymi literami), żeby ograniczenie UNIQUE na surowej kolumnie faktycznie odpowiadało tożsamości, a nie zapisowi znaków.',
    acceptance: [
      'Schematy Zod audytorów i ekip normalizują e-mail przez .toLowerCase() w tym samym miejscu co .trim() — normalizacja jest w schemacie, nie w akcji, bo schemat jest współdzielony przez formularz i Server Action (UI-FORMS-RHF-ZOD).',
      'Cztery akcje create/update (audytorzy i ekipy, po dwie) mają nieautorytatywny pre-check findFirst zwracający czytelny komunikat domenowy zamiast surowego błędu 23505 z bazy. Pre-check jest wygodą, nie zabezpieczeniem — jedyną gwarancją pozostaje UNIQUE w bazie, bo między sprawdzeniem a zapisem jest wyścig.',
      'PUŁAPKA, osobny przypadek testowy: pre-check w akcjach EDYCJI musi wykluczać edytowany rekord (id: { not: id }). Bez tego zapis formularza bez zmiany adresu zaczyna zgłaszać kolizję z samym sobą — regresja widoczna dopiero przy edycji, nie przy tworzeniu.',
      'Dane istniejące NIE są normalizowane migracją w ramach tego ID. Backfill lower(email) może skleić dwa wiersze w jeden konflikt z ograniczeniem UNIQUE, a rozstrzygnięcie, który wiersz jest prawdziwym pracownikiem, należy do człowieka — skasowanie e-maila odcina komuś dostęp. Stan na 2026-09-03: zero duplikatów po lower(email) w obu tabelach, więc dług jest wyłącznie prewencyjny.',
      'Poza zakresem: zmiana ograniczenia bazodanowego na UNIQUE(lower(email)). Odrzucone przy SEC-EMAIL-UNIQUE — Prisma nie wyraża funkcyjnego indeksu unikalnego jako @unique, więc taki wariant wyprodukowałby nowy dryf schema.prisma vs baza, lecząc stary.'
    ]
  }),
  R('SEC-SERVICE-KEY-SERVER-ONLY', { status: 'DONE', source: 'system_architecture.md#3', domain: 'security', statement: 'Klucz serwisowy Supabase nigdy nie trafia do bundla klienckiego.', acceptance: ['Hook blokuje zapis pliku klienckiego z odwołaniem do klucza serwisowego', 'Skan bundla w CI'], risk: 'HIGH' }),
  // ── Okno kontraktowe SEC-RODO-DELETE-RECONCILE (2026-09-03, contract-steward) ──
  // Wpis ZOSTAJE jako SUPERSEDED — historia i ewentualne dopasowania kk-trace mają się nie zerwać.
  // Decyzja człowieka rozstrzyga sprzeczność opisaną w docs/workorders/SEC-AUDIT-COVERAGE-RETAG.md,
  // sekcja „Ryzyka i nieznane”: dwóch z trzech dawnych kryteriów nie da się utrzymać obok
  // CRM-CLIENT-ANONYMIZE-RODO, a trzecie jest już przez nie niesione. Nie reanimować bez nowego ADR.
  R('SEC-RODO-DELETE', { status: 'SUPERSEDED', source: 'database_model.md#4 (ślad historyczny — zachowany celowo); zastąpione 2026-09-03 decyzją człowieka w oknie kontraktowym SEC-RODO-DELETE-RECONCILE, na podstawie analizy w docs/workorders/SEC-AUDIT-COVERAGE-RETAG.md (sekcja „Ryzyka i nieznane”). Następcą jest CRM-CLIENT-ANONYMIZE-RODO — tam liczy się pokrycie testami.', domain: 'security', statement: 'ZASTĄPIONE przez CRM-CLIENT-ANONYMIZE-RODO. Zasada (usunięcie klienta na żądanie RODO anonimizuje dane kontaktowe, zachowując wartość zrealizowanego montażu) obowiązuje dalej, ale jej kryteria i pokrycie żyją we wpisie następcy. Z trzech dawnych kryteriów pierwsze (brak kaskadowego kasowania) przeszło do następcy, a dwa pozostałe odpadły: legal_basis nie jest stałą RODO_ERASURE_REQUEST, bo operator świadomie wybiera podstawę prawną z zamkniętej listy pięciu wartości, natomiast migawka before_snapshot jest nierealizowalna — jest wprost sprzeczna z kryterium następcy „żadna funkcja nie przechowuje kopii danych osobowych klienta sprzed anonimizacji”, zamrożonym testem customers-anonymize-rodo.test.ts, a model AuditLog nie ma i nie dostanie takiej kolumny.', acceptance: ['Wpis nie ma własnych kryteriów — patrz CRM-CLIENT-ANONYMIZE-RODO'], risk: 'HIGH' }),
  R('SEC-AUDIT-LOG', { source: 'database_model.md#4', domain: 'security', statement: 'Operacje wrażliwe (delete, anonimizacja, zmiana roli) są rejestrowane w append-only audit_log.', acceptance: ['Każda z sześciu operacji z AUDIT_REQUIREMENTS.mustLog tworzy wpis w tej samej transakcji co sama operacja', 'RLS odrzuca UPDATE i DELETE na audit_log dla wszystkich ról łącznie z admin — test wykonuje obie próby jako admin', 'actor_role zapisuje rolę z chwili operacji, nie odczytaną przez relację', 'legal_basis pochodzi z zamkniętej listy, justification jest wymagane', 'before_snapshot nie zawiera danych osobowych w postaci jawnej', 'Wycofanie transakcji operacji wycofuje też wpis audytowy — brak wpisów o operacjach, które się nie wydarzyły'], status: 'TODO', risk: 'HIGH' }),

  // ── Rozbicie SEC-AUDIT-LOG na wymagania potomne (2026-09-03, okno SEC-AUDIT-LOG-DELETE) ──
  // Wzorem rozbicia CRM-DELETE-ADMIN-ONLY → CRM-DELETE-ADMIN-ONLY-<RESOURCE> (patrz wyżej).
  // Powód rozbicia jest ten sam co tam: kk-trace dopasowuje ID po tagu w teście, nie po operacji,
  // więc jedna pokryta operacja z sześciu w AUDIT_REQUIREMENTS.mustLog wyglądałaby jak pełne
  // pokrycie wymagania o zasięgu „każda z sześciu operacji”.
  // Rodzic SEC-AUDIT-LOG ZOSTAJE 'TODO' i NIE jest oznaczany SUPERSEDED w tym oknie: pokrywają go
  // dopiero cztery dzieci razem (DELETE, ROLE-CHANGE, MANUAL-STATUS, UI-JUSTIFICATION), a trzy
  // pozostałe jeszcze nie istnieją. Oznaczenie rodzica przed ich rejestracją zgubiłoby te operacje.
  // Punktów zapisu jest SIEDEM, nie osiem: deleteLogisticsOrderAction deleguje do deleteLeadAction
  // i nie ma własnego wpisu — dlatego logistics nie występuje w source ani w RESOURCES.
  R('SEC-AUDIT-LOG-DELETE', {
    status: 'TODO',
    domain: 'security',
    risk: 'HIGH',
    source: "docs/workorders/SEC-AUDIT-LOG-DELETE.md (2026-09-03) — część 1 z 4 rozbicia SEC-AUDIT-LOG. Siedem punktów zapisu w apps/b2b-web/src/app/(dashboard)/: leads/actions.ts (deleteLeadAction), installations/actions.ts (deleteInstallationAction), incidents/actions.ts (deleteIncidentAction), services/actions.ts (deleteServiceAction), settings/actions.ts (deleteAuthorizedUser), auditors/actions.ts (deleteAuditorAction), crews/actions.ts (deleteCrewAction). Ósme wejście, logistics/actions.ts (deleteLogisticsOrderAction), NIE ma własnego wpisu w tej liście, bo deleguje do deleteLeadAction — jest drugim wywołującym ten sam punkt zapisu, a nie punktem zapisu. Wzorzec referencyjny: anonymizeClientAction w customers/actions.ts. STAN BAZY: migracja 20260901220000 jest ZASTOSOWANA na żywej bazie — potwierdzone bezpośrednim zapytaniem 2026-09-03 (nie ewidencją pliku migracji): tabela audit_log z 9 kolumnami, 4 ograniczenia CHECK w tym audit_log_operation_check dopuszczające wartość 'delete', wyzwalacz audit_log_append_only_trg, 0 wierszy. To sprostowanie nieaktualnych zdań w source wpisów CRM-CLIENT-ANONYMIZE-RODO i SEC-AUDIT-LOG-APPEND-ONLY, poprawionych w tym samym oknie. Zmiana schematu ani migracja NIE są potrzebne. AUDIT_REQUIREMENTS.legalBases zostaje bez zmian — zawiera już neutralne OPERATIONAL_ERROR i OTHER.",
    statement: 'Każde usunięcie rekordu w panelu B2B tworzy wpis w audit_log w TEJ SAMEJ transakcji co samo usunięcie, z obowiązkowym uzasadnieniem operatora i podstawą prawną z zamkniętej listy. Nie istnieje ścieżka kodu kasująca rekord bez śladu audytowego. Wymaganie dotyczy WYŁĄCZNIE operacji delete — pozostałe operacje z AUDIT_REQUIREMENTS.mustLog mają własne wpisy potomne.',
    acceptance: [
      'AC1 — Transakcyjność: gdy zapis do audit_log zawiedzie, rekord NIE jest usunięty (rollback całej transakcji). Dla każdego z ośmiu wejść nie istnieje ścieżka kodu wywołująca delete poza transakcją zawierającą auditLog.create',
      'AC2 — Odwrotny kierunek: gdy delete zawiedzie (np. klucz obcy BLOCK), w audit_log nie przybywa wiersz — brak wpisów o operacjach, które się nie wydarzyły',
      "AC3 — Kompletność wpisu: operation = 'delete', resource zgodne z tabelą mapowania w WO (leads, installations, incidents, services, authorized_users, auditors, crews; logistyka zapisuje leads), record_id równe identyfikatorowi usuwanego rekordu, actor_email zalogowanego użytkownika (nie null, nie stała), actor_role Z CHWILI operacji — wartość z getCurrentActorRole() utrwalona jako tekst, nie odczytana później przez relację",
      'AC4 — Fail-closed przy braku roli: brak sesji, e-mail spoza authorized_users, rola nierozpoznana przez ROLES oraz wyjątek samego zapytania o rolę dają odmowę uprawnień, a nie generyczny błąd zapisu i nie usunięcie. Test dowodzi, że powstało ZERO zapytań delete i zero auditLog.create',
      'AC5 — Fail-closed przy braku e-maila: sesja bez adresu e-mail daje odmowę przed jakimkolwiek zapytaniem — actor_email jest NOT NULL i nie wolno go obejść pustym stringiem ani wartością zastępczą',
      "AC6 — Uzasadnienie obowiązkowe: justification krótsze niż 10 znaków po trim albo legalBasis spoza AUDIT_REQUIREMENTS.legalBases jest odrzucone BEZ żadnego zapisu i bez usunięcia. Sprawdzone dla wartości pustej, samych spacji, 'krótkie' (9 znaków) i 'NIEISTNIEJACA_PODSTAWA'",
      "AC7 — Jedno usunięcie = jeden wpis: udane usunięcie przez deleteLogisticsOrderAction tworzy dokładnie JEDEN wiersz w audit_log z resource = 'leads', nie dwa",
      'AC8 — Blokada nie loguje: odmowa z BLOCK_UNTIL_REASSIGNED (audytor z wiszącymi leadami, ekipa z aktywnymi instalacjami) nie tworzy wpisu w audit_log — nic nie usunięto',
      "AC9 — Powtórzenie na nieistniejącym rekordzie: drugie wywołanie z tym samym id (rekord już usunięty) nie tworzy drugiego wpisu; liczba wpisów operation='delete' dla danego record_id po N wywołaniach wynosi 1",
      'AC10 — Jedno źródło progu i słownika (test statyczny): literał 10 jako próg długości uzasadnienia ani jakakolwiek lista podstaw prawnych nie występują w apps/b2b-web/src poza importem z AUDIT_REQUIREMENTS; druga kopia schematu Zod obok pliku współdzielonego jest naruszeniem',
      'AC11 — Brak ścieżki bez audytu (test statyczny nad apps/b2b-web/src): każde wystąpienie .delete({ na modelu Prismy w pliku actions.ts znajduje się w bloku $transaction, w którym występuje też auditLog.create. Nowa akcja delete dodana jutro bez wpisu jest wykrywana przez ten test, nie przez czyjąś czujność',
      'AC12 — UI wymusza uzasadnienie: w każdym z ośmiu miejsc usuwania nie da się zatwierdzić operacji bez wpisania uzasadnienia i wybrania podstawy prawnej; window.confirm nie jest już ścieżką usunięcia w żadnym z ośmiu komponentów',
      'AC13 — Wpisu nie da się poprawić: UPDATE i DELETE na wierszu utworzonym przez usunięcie są odrzucone przez wyzwalacz audit_log_append_only_trg również przez Prismę, która omija RLS. Kryterium dzielone z SEC-AUDIT-LOG-APPEND-ONLY — tu potwierdza, że nowe wpisy podlegają tej samej ochronie; obowiązuje ten sam poziom dowodu co tam (dziś statyczny nad tekstem migracji, bo suite nie ma połączenia z żywym Postgresem)',
    ],
  }),

  // ───────────────────────── Powiadomienia ─────────────────────────
  R('NTF-QUEUE-WINDOW', { source: 'b2b_app_requirements.md#epic-4', domain: 'notifications', statement: 'SMS wysyłane wyłącznie w oknie 8:00–18:00; poza oknem kolejkowane na najbliższe okno.', acceptance: ['Strefa Europe/Warsaw', 'Przesunięcie nie gubi wiadomości'] }),
  R('NTF-POLY', { source: 'ADR-007', domain: 'notifications', statement: 'Kolejka obsługuje powiadomienia niezwiązane z leadem: serwisowe i usterkowe.', acceptance: ['Dokładnie jedno z lead_id/installation_id/service_id/incident_id jest niepuste — wymuszone przez CHECK w bazie', 'N10-N14 kolejkują się z service_id', 'N15-N18 kolejkują się z incident_id', 'Próba wstawienia rekordu z dwoma powiązaniami jest odrzucana przez bazę'], risk: 'HIGH' }),
  R('NTF-HISTORY', { source: 'b2b_app_requirements.md#epic-0', domain: 'notifications', statement: 'Karta 360 pokazuje pełną historię komunikacji z klientem.', acceptance: ['recipient_address zapisuje adres z chwili wysyłki i nie zmienia się po edycji danych klienta', 'Historia obejmuje wszystkie cztery typy powiązań', 'Powiadomienia wewnętrzne (I1-I7) nie pojawiają się w historii klienta'], risk: 'MEDIUM' }),
  R('NTF-RETRY', { source: 'b2b_app_requirements.md#epic-0', domain: 'notifications', statement: 'Centrum Powiadomień pozwala ponowić wysyłkę błędnych powiadomień.', acceptance: ['Ponowienie inkrementuje attempts na istniejącym rekordzie, nie tworzy nowego', 'Dwa ponowienia z tym samym idempotency_key skutkują jedną wysyłką — test uruchamia je równolegle', 'Po deadLetterAfterAttempts status zmienia się na DEAD_LETTER i rekord znika z kolejki roboczej', 'next_attempt_at rośnie wykładniczo od backoffBaseSeconds', 'Wiadomość w statusie DEAD_LETTER nie jest ponawiana automatycznie, tylko ręcznie z Centrum Powiadomień'], status: 'TODO', risk: 'HIGH' }),
  R('NTF-CATALOG-PARITY', { source: 'notification_definitions.md', domain: 'notifications', statement: 'Każde powiadomienie z katalogu ma szablon w bazie i odwrotnie — brak sierot w obie strony.', acceptance: ['Test kontraktowy porównuje katalog z tabelą message_templates', 'Brak duplikatów trigger_event'], risk: 'HIGH' }),

  // ───────────────────────── B2C (aplikacja kliencka) ─────────────────────────
  // Lejek zaczyna się od NEW_LEAD, ale jego jedynym producentem jest Triage B2C.
  // Dopóki ta sekcja nie istniała, wejście do maszyny stanów było poza kontraktem.

  // ── Styk B2C → lejek B2B ──
  R('B2C-LEAD-ENTRY', { source: 'b2c_app_requirements.md#1', domain: 'b2c', statement: 'Ukończony Triage tworzy leada w stanie NEW_LEAD — to jedyne legalne wejście do maszyny stanów lejka.', acceptance: ['Utworzony lead ma leads.status ustawiony na START_STATE z kontraktu lejka, a nie na osobne pole tekstowe poza maszyną stanów (dziś zapisywane jest status_triage z wartością spoza STATES)', 'Istnieje dokładnie jedna Server Action tworząca leada z B2C — test statyczny wykrywa drugą ścieżkę zapisu (dziś istnieją dwie: apps/b2c-web/app/actions/leads.ts i apps/b2c-web/app/actions/saveLead.ts)', 'Lead nie może być anonimowy: utworzenie wymaga kompletu — imię i nazwisko, adres, telefon, e-mail oraz odpowiedzi z triage (decyzja człowieka 2026-08-19)', 'Brak któregokolwiek z pięciu elementów odrzuca operację PO STRONIE SERWERA, nie tylko w UI — test pomija po kolei każdy z nich i oczekuje odrzucenia za każdym razem, pięć osobnych przypadków, nie jeden zbiorczy', 'Kompletność egzekwowana dwuwarstwowo w duchu CRM-DELETE-ADMIN-ONLY: schemat Zod w Server Action ORAZ ograniczenia NOT NULL w bazie — test sprawdza obie warstwy osobno, bo walidację aplikacyjną da się ominąć zapisem z pominięciem Server Action', 'Odpowiedzi Triage zapisane w leads.triage_answers w strukturze pozwalającej odtworzyć typ budynku, liczbę pomieszczeń i pasmo metrażu każdego pomieszczenia — wyłącznie identyfikatory ze słowników contracts/triage.contract.mjs, nie etykiety PL', 'leads.declared_property_condition zapisywane przy tworzeniu leada, w tej samej transakcji co lead, klient, adres i rezerwacja (B2C-LEAD-ATOMIC)', 'Wartość pochodzi wyłącznie ze słownika PROPERTY_CONDITIONS — wartość spoza słownika jest odrzucana po stronie serwera, nie tylko niedostępna w UI', 'Przesłanka montażu dwuetapowego odczytywana jest z pola suggestsTwoPhase w contracts/triage.contract.mjs, a nie z warunku porównującego stan lokalu w kodzie aplikacji', 'Deklaracja jest przesłanką dla audytora, nie rozstrzygnięciem: nie ustawia quotes.installation_type i nie tworzy installation_phases — test tworzy leada dla każdej wartości niosącej przesłankę (RENOVATION, DEVELOPER_SHELL) i sprawdza, że tryb montażu pozostaje nieustalony (FNL-2PHASE)', 'Deklaracja jest widoczna dla audytora w zleceniu audytu', 'Próba utworzenia leada w stanie innym niż START_STATE jest odrzucana po stronie serwera'], risk: 'HIGH' }),
  R('B2C-LEAD-ATOMIC', { source: 'b2c_app_requirements.md#1', domain: 'b2c', statement: 'Lead, klient, adres i rezerwacja terminu audytu powstają w jednej transakcji albo nie powstaje żaden z tych rekordów.', acceptance: ['Zapisy do leads, clients, addresses i bookings wykonują się w jednej transakcji bazodanowej', 'Test wstrzykuje błąd osobno na każdym z czterech kroków — po każdym z nich sprawdza, że nie powstał ani lead, ani klient, ani adres, ani rezerwacja', 'Powiązanie klienta z leadem powstaje wewnątrz tej samej transakcji, a nie osobnym UPDATE po zatwierdzeniu (dziś jest to czwarty, niezależny zapis)', 'Awaria integracji zewnętrznej z kalendarzem nie wycofuje transakcji ani nie zostawia rezerwacji bez leada — wpis do kalendarza jest kolejkowany po zatwierdzeniu, nie wykonywany w transakcji', 'To wymaganie odpowiada wyłącznie za niepodzielność zapisu; komplet danych wymaganych do utworzenia leada definiuje B2C-LEAD-ENTRY i nie jest tu powtarzany'], risk: 'HIGH' }),
  R('B2C-BOOKING-SLOT', { source: 'docs/prompts/figma_triage_ui_prompt.md#ekran-rezerwacji', domain: 'b2c', statement: 'Rezerwacja terminu audytu przez klienta jest atomowa — ten sam slot może zostać zajęty tylko raz.', acceptance: ['Atomowość gwarantuje ograniczenie w bazie (unikalny indeks częściowy na bookings), nie sprawdzenie w kodzie', 'Test uruchamia dwa równoległe żądania na ten sam slot i oczekuje dokładnie jednego sukcesu', 'Przegrane żądanie dostaje błąd domenowy „slot zajęty" wraz z listą alternatyw, nie odpowiedź 500', 'Lista wolnych slotów jest wyliczana z bookings i absences — dzisiejsze apps/b2c-web/app/api/calendar/slots/route.ts zwraca zahardkodowaną tablicę mockową i wymaganie opisuje stan docelowy, nie obecny', 'Rezerwacja audytu tworzy rekord bookings tą samą ścieżką co rezerwacja montażu z FNL-E3-E4 — jedna implementacja rezerwacji, dwa typy wizyty'], risk: 'HIGH' }),
  R('B2C-CONSENT-RODO', { source: 'database_model.md#4', domain: 'b2c', statement: 'Zgoda na regulamin i politykę prywatności jest zapisywana razem z leadem.', acceptance: ['Zapisywany jest moment udzielenia zgody oraz wersja zaakceptowanego dokumentu — sama flaga logiczna nie wystarcza', 'Brak zgody powoduje odrzucenie żądania w Server Action — test wysyła żądanie z pominięciem UI, bez znacznika zgody, i oczekuje błędu walidacji zamiast zapisu', 'Zgoda zapisuje się w tej samej transakcji co lead (B2C-LEAD-ATOMIC) — nie istnieje lead bez zgody ani zgoda bez leada', 'Późniejsza zmiana treści regulaminu nie modyfikuje wersji zapisanej przy istniejących leadach'], risk: 'HIGH' }),
  R('B2C-RLS-PUBLIC', { source: 'system_architecture.md#3', domain: 'b2c', statement: 'Aplikacja B2C czyta dane kluczem anonimowym z aktywnym RLS, a klucz serwisowy występuje wyłącznie w Server Actions.', acceptance: ['Skan zbudowanego bundla klienckiego apps/b2c-web nie zawiera klucza serwisowego — sprawdzenie samego kodu źródłowego nie wystarcza', 'Odczyt cudzego leada klientem anonimowym zwraca pusty zbiór — test wykonuje realne zapytanie, nie sprawdza konfiguracji polityk', 'Tabele katalogu produktów i treści są czytelne anonimowo, tabele lejka (leads, clients, addresses, bookings) nie', 'Zakaz wycieku klucza serwisowego jako taki opisuje SEC-SERVICE-KEY-SERVER-ONLY — tutaj sprawdzana jest wyłącznie strona B2C: że ścieżka publiczna faktycznie działa na RLS, a nie omija ją kluczem serwisowym'], risk: 'HIGH' }),

  // ── Triage ──
  R('B2C-TRIAGE-STEPS', { source: 'docs/prompts/figma_triage_ui_prompt.md', domain: 'b2c', statement: 'Triage prowadzi klienta przez siedem kroków, po jednym pytaniu na ekran, z paskiem postępu i działającym cofaniem.', acceptance: ['Każdy ekran kreatora prezentuje dokładnie jedno pytanie', 'Pasek postępu pokazuje numer bieżącego kroku i ich łączną liczbę', 'Test wypełnia kroki 1-4, cofa się przyciskiem „Wstecz" do kroku 1 i sprawdza, że zaznaczenia z kroków 2-4 są nadal ustawione', 'Cofnięcie i ponowne przejście naprzód nie duplikuje ani nie czyści odpowiedzi', 'Krok 4 (stan lokalu) prezentuje dokładnie wartości ze słownika PROPERTY_CONDITIONS wraz z ich etykietami PL — kafelek bez odpowiednika w słowniku jest błędem, tak samo jak brakujący kafelek', 'Opuszczenie kreatora przed ostatnim krokiem nie tworzy rekordu w leads'], risk: 'MEDIUM' }),
  R('B2C-TRIAGE-DISQUALIFY', { status: 'DONE', source: 'contracts/triage.contract.mjs', domain: 'b2c', statement: 'Konfiguracja spełniająca którąkolwiek regułę z DISQUALIFICATION_RULES kieruje klienta na ekran Eksperta zamiast na wycenę.', acceptance: ['Egzekwowane są obie reguły z DISQUALIFICATION_RULES — dziś store B2C sprawdza wyłącznie typ budynku (apps/b2c-web/store/triageStore.ts: isExpertScreen destrukturyzuje roomCount i nigdzie go nie używa)', 'Próg liczby pomieszczeń pochodzi z ROOM_COUNT_EXPERT_THRESHOLD w contracts/triage.contract.mjs — w komponencie, w store ani w teście nie występuje literał', 'Ekran Eksperta nie prezentuje ceny, ceny szacunkowej ani przycisku rezerwacji z wyceną', 'Dyskwalifikacja jest rozstrzygana także po stronie serwera — żądanie wyceny dla konfiguracji dyskwalifikującej, wysłane z pominięciem UI, jest odrzucane', 'Test przechodzi obie reguły osobno oraz ich koniunkcję', 'Istniejący apps/b2c-web/e2e/triage.spec.ts prowadzi kombinację z lokalem komercyjnym aż do ekranu wyceny i oczekuje ceny — ten test jest błędny wobec kontraktu i podlega przepisaniu; kod nie jest naginany do jego oczekiwań'], risk: 'MEDIUM' }),
  R('B2C-TRIAGE-CONDITIONAL', { source: 'docs/prompts/figma_triage_ui_prompt.md', domain: 'b2c', statement: 'Pytania zależne pojawiają się wyłącznie w kontekście, w którym mają sens.', acceptance: ['Pytanie o balkon i pytanie o piętro pojawiają się wyłącznie dla BUILDING_TYPES.APARTMENT — dla HOUSE i COMMERCIAL kreator je pomija', 'Wejście do Triage z karty produktu pomija kroki 2 i 3, a cofanie z kroku 4 wraca wtedy do kroku 1, nie do kroku 3', 'Pominięty krok nie zostawia w odpowiedziach wartości domyślnej udającej deklarację klienta — test sprawdza, że pasmo metrażu jest puste, a nie ustawione na pierwszy element ROOM_SIZE_BANDS (dziś store inicjalizuje je pierwszym pasmem)'], risk: 'LOW' }),
  R('B2C-SOFT-LEAD', { source: 'b2c_app_requirements.md#1', domain: 'b2c', statement: 'Exit Intent zapisuje kontakt cząstkowy do soft_leads i nie wprowadza nikogo do lejka.', acceptance: ['Modal nie pojawia się, gdy otwarty jest konfigurator urządzenia — test otwiera modal urządzenia i wyzwala Exit Intent, oczekując braku okna', 'Zapis tworzy rekord wyłącznie w soft_leads — test sprawdza, że liczba rekordów w leads nie zmienia się', 'Soft lead i lead wykluczają się rozłącznie: soft lead przechowuje WYŁĄCZNIE kontakt cząstkowy i NIGDY nie tworzy wiersza w leads, a lead nie powstaje bez kompletu wymaganego przez B2C-LEAD-ENTRY — test wykazuje, że dane soft leada nie wystarczają do utworzenia leada', 'Soft lead nie ma statusu z maszyny stanów lejka', 'Ponowne wyzwolenie Exit Intent w tej samej sesji nie tworzy drugiego rekordu'], risk: 'MEDIUM' }),
  R('B2C-BOOKING-VALIDATION', { source: 'ui_ux_guidelines.md#8', domain: 'b2c', statement: 'Formularz kontaktowy rezerwacji działa na react-hook-form z zodResolver, a ten sam schemat Zod waliduje dane powtórnie po stronie serwera.', acceptance: ['Schemat Zod jest jeden i importowany zarówno przez formularz, jak i przez Server Action — nie istnieją dwie definicje tych samych pól', 'Komunikaty walidacyjne po polsku', 'Numer telefonu i kod pocztowy walidowane wg formatu polskiego — test podaje wartość poprawną i niepoprawną dla każdego z tych pól', 'Żądanie wysłane bezpośrednio do Server Action z pominięciem UI, z niepoprawnym adresem e-mail, jest odrzucane', 'Pola obsługiwane przez react-hook-form, bez useState na pojedyncze pole tekstowe (UI-FORMS-RHF-ZOD)'], risk: 'MEDIUM' }),

  // ── Katalog i cena ──
  R('B2C-PRICE-FROM', { source: 'b2c_app_requirements.md#3', domain: 'b2c', statement: 'Cena „od" jest wyliczana w locie jako suma jednostki wewnętrznej, dedykowanego agregatu i usługi montażu, powiększona o VAT.', acceptance: ['Żaden komponent nie zawiera ceny zapisanej na sztywno — wszystkie składniki pochodzą z bazy produktów', 'Stawka VAT jest zdefiniowana w jednym miejscu i importowana przez wszystkie widoki — test zmienia ją w tym jednym miejscu i sprawdza, że zmieniła się każda prezentowana cena', 'Brak dostępnej kombinacji jednostka + agregat powoduje ukrycie ceny wraz z komunikatem, a nie wyliczenie ceny z niepełnych składników', 'Prezentowana cena jest brutto i jawnie oznaczona jako szacunkowa', 'Test porównuje wynik z ceną najtańszej dostępnej kombinacji w bazie — wynikiem ma być minimum, nie dowolny wariant', 'Stan lokalu nie jest składnikiem ceny: dwa przebiegi Triage różniące się WYŁĄCZNIE wartością z PROPERTY_CONDITIONS dają identyczną kwotę — test przechodzi wszystkie trzy wartości słownika przy niezmienionej reszcie konfiguracji (deklaracja jest przesłanką dla audytora, nie parametrem wyceny)'], risk: 'HIGH' }),
  R('B2C-CATALOG-LIST', { source: 'docs/prompts/figma_landing_page_ui_prompt.md', domain: 'b2c', statement: 'Katalog prezentuje urządzenia w jednej sekcji z filtrami marki i koloru.', acceptance: ['Filtr marki i filtr koloru zawężają listę i dają się złożyć — test sprawdza obie kombinacje', 'Katalog jest jedną sekcją: dawne sekcje Multi Split i Agregaty nie istnieją od przebudowy katalogu z 2026-08-06 i nie wracają jako asercje w testach', 'Pusty wynik filtrowania pokazuje komunikat, a nie pustą siatkę', 'Karta produktu otwiera modal urządzenia (B2C-DEVICE-MODAL)'], risk: 'LOW' }),
  R('B2C-DEVICE-MODAL', { source: 'docs/prompts/figma_device_modal_prompt.md', domain: 'b2c', statement: 'Modal urządzenia prezentuje galerię, cechy i standardowy zakres montażu oraz oferuje dwa CTA.', acceptance: ['Modal zawiera galerię zdjęć, cechy urządzenia w postaci odznak oraz listę pozycji standardowego zakresu montażu', 'Dwa CTA: rezerwacja z wybranym urządzeniem (wejście do Triage pomijające kroki 2 i 3, zgodnie z B2C-TRIAGE-CONDITIONAL) oraz powrót do przeglądania', 'Modal zamyka się klawiszem Esc i przywraca fokus na element, który go otworzył', 'Ikony wyłącznie z lucide-react (UI-ICONS-LUCIDE-ONLY)', 'Otwarty modal wstrzymuje Exit Intent (B2C-SOFT-LEAD)'], risk: 'LOW' }),

  // ── Treść i nawigacja ──
  R('B2C-CONTENT-PAGES', { source: 'docs/prompts/figma_landing_page_ui_prompt.md', domain: 'b2c', statement: 'Strony Baza wiedzy, O nas, Polityka prywatności i Regulamin renderują się i są osiągalne z nawigacji.', acceptance: ['Test przechodzi po liście czterech tras i dla każdej sprawdza kod 200 oraz obecność nagłówka H1 — nie po jednej trasie przykładowej', 'Artykuł bazy wiedzy jest osiągalny kliknięciem z listy artykułów, nie tylko po wpisaniu adresu', 'Odnośniki do Polityki prywatności i Regulaminu ze stopki oraz z pola zgody (B2C-CONSENT-RODO) prowadzą do stron istniejących — żadna nie zwraca 404', 'Wersja dokumentu prawnego prezentowana na stronie jest tą samą wartością, którą zapisuje zgoda'], risk: 'LOW' }),
  R('B2C-NAV-STATE', { source: 'docs/prompts/figma_landing_page_ui_prompt.md', domain: 'b2c', statement: 'Powrót z podstrony przywraca pozycję przewijania strony głównej.', acceptance: ['Test przewija stronę główną do sekcji katalogu, wchodzi w kartę produktu, wraca przyciskiem wstecz przeglądarki i sprawdza, że pozycja przewijania różni się od zapamiętanej o mniej niż wysokość widocznego obszaru', 'Zamknięcie modala urządzenia przywraca adres sprzed jego otwarcia i nie przewija strony na górę'], risk: 'LOW' }),

  // ───────────────────────── FIELD APP (aplikacja terenowa) ─────────────────────────
  // Źródło: docs/architecture/field_app_requirements.md (dokument opisowy, nie kontrakt).
  // apps/field-app nie istnieje — te wymagania są kotwicą dla progów, migracji i testów fazy 3.
  // Status BLOCKED, nie TODO: wymaganie jest poprawne, ale niewykonalne przed powstaniem aplikacji
  // terenowej (albo przed decyzją odnotowaną w kryteriach). kk-trace wyłącza BLOCKED z ostrzeżenia
  // „HIGH RISK bez żadnego testu", więc raport pokrycia nie zaszumia się siedmioma stałymi alarmami.

  // ── Geofencing i GPS (rozdział 6, D4) ──
  R('FLD-GEO-COORDS', { source: 'field_app_requirements.md#6.3', domain: 'field', statement: 'Adres klienta przechowuje współrzędne geograficzne, a ścieżka tworzenia leada w B2C zapisuje je zamiast odrzucać po drodze.', acceptance: ['Tabela adresów ma kolumny latitude i longitude (zmiennoprzecinkowe, nullable) — bez nich żaden próg przestrzenny nie ma z czego czytać', 'Kolumny wchodzą jako nullable: NOT NULL bez wartości domyślnej na istniejącej tabeli jest zabronione, kolejność to nullable → backfill → ewentualne ograniczenie', 'Server Action tworząca leada z B2C deklaruje współrzędne w typie danych wejściowych i zapisuje je w tej samej transakcji co lead, klient i adres (B2C-LEAD-ATOMIC)', 'Test przechodzi Triage z adresem geokodowanym i sprawdza, że rekord adresu ma niepuste współrzędne — dziś apps/b2c-web/app/actions/saveLead.ts odrzuca je dla każdego leada, bo interfejs SaveLeadData ich nie deklaruje', 'Obie równoległe ścieżki tworzenia leada traktują współrzędne tak samo albo jedna z nich zostaje jawnie zlikwidowana (B2C-LEAD-ENTRY)', 'Adres bez współrzędnych jest stanem jawnym, a nie cichym zerem — konsument progu przestrzennego odróżnia „brak danych" od „odległość 0"'], risk: 'HIGH' }),
  R('FLD-GEO-UNLOCK', { status: 'BLOCKED', source: 'field_app_requirements.md#6.1', domain: 'field', statement: 'Field App odblokowuje rozpoczęcie i zakończenie zlecenia dopiero wtedy, gdy pozycja GPS pracownika mieści się w promieniu SLA.GEOFENCE_UNLOCK_RADIUS od punktu docelowego.', acceptance: ['Promień pochodzi wyłącznie z SLA.GEOFENCE_UNLOCK_RADIUS (meters) — w kodzie aplikacji terenowej nie występuje literał liczbowy ani przeliczanie jednostek', 'Poza promieniem przyciski „Rozpocznij" i „Zakończ" są nieaktywne, a żądanie wysłane z pominięciem UI jest odrzucane po stronie serwera', 'Odblokowanie jest lokalnym zdarzeniem urządzenia, nie wiadomością z katalogu powiadomień — nie trafia do kolejki i nie ma odbiorcy z listy RECIPIENTS', 'Zlecenie pod adresem bez współrzędnych (FLD-GEO-COORDS) nie odblokowuje się automatycznie: brak danych to odmowa, nie przepuszczenie (fail-closed)', 'Test przechodzi trzy przypadki: wewnątrz promienia, dokładnie na granicy i poza nim — wartość graniczna pobierana z kontraktu, nie wpisana w teście'], risk: 'HIGH' }),
  R('FLD-GEO-EN-ROUTE', { status: 'BLOCKED', source: 'field_app_requirements.md#6.2', domain: 'field', statement: 'Przecięcie promienia SLA.GEOFENCE_EN_ROUTE_RADIUS w oknie dnia wizyty wyzwala klientowi SMS „w drodze"; Field App jest producentem zdarzenia, nie nowym typem powiadomienia.', acceptance: ['Próg pochodzi wyłącznie z SLA.GEOFENCE_EN_ROUTE_RADIUS (meters) — bez literału i bez przeliczania kilometrów na metry w kodzie', 'Zdarzenie konsumują wyłącznie cztery istniejące powiadomienia o bind.kind = GEO: N3 (audytor w drodze), N7 (ekipa w drodze), N13 (serwis w drodze) i N17 (usterka — technik w drodze). Field App nie dodaje piątego', 'Typ wizyty wybiera powiadomienie: audyt → N3, montaż → N7, serwis → N13, usterka → N17 — test sprawdza wszystkie cztery ścieżki osobno', 'Przecięcie promienia poza oknem czasowym dnia wizyty nie kolejkuje niczego', 'Powtórne przecięcie tego samego promienia w tym samym zleceniu nie wysyła drugiego SMS-a (idempotencja wg QUEUE_POLICY)', 'Katalog powiadomień nie ma pola req, więc walidator NIE sprawdza związku progu z N3/N7/N13/N17 — jedynym dowodem tego związku jest test wymieniający wszystkie cztery identyfikatory'], risk: 'HIGH' }),
  R('FLD-GPS-RODO', { status: 'BLOCKED', source: 'field_app_requirements.md#6.4', domain: 'field', statement: 'GPS pracownika jest zbierany wyłącznie w oknie aktywnego, przypisanego zlecenia i zapisywany jako zdarzenia punktowe, nigdy jako ciągły ślad trasy.', acceptance: ['Poza oknem aktywnego zlecenia aplikacja nie odczytuje pozycji — test sprawdza brak zapisu dla zlecenia zakończonego oraz dla zlecenia nieprzypisanego temu pracownikowi', 'Zapisywane są wyłącznie trzy zdarzenia punktowe: przecięcie promienia alertu, odblokowanie w promieniu pracy, zamknięcie zlecenia', 'Nie istnieje tabela ani mechanizm logujący pozycję cyklicznie co N sekund — brak śladu trasy jest wymaganiem, nie brakiem implementacji', 'Zdarzenie zapisuje moment (_at) i identyfikator zlecenia; poza tymi trzema momentami pozycja pracownika nie jest przechowywana', 'Podjęcie zlecenia bez wcześniejszej akceptacji zgód (FLD-CONSENT-ACCEPT) nie jest możliwe, więc GPS nie jest zbierany bez podstawy'], risk: 'HIGH' }),

  // ── Konto pracownika, zgody i dokumenty (rozdział 4.1, D4) ──
  R('FLD-AUTH-BLOCKED', { status: 'BLOCKED', source: 'field_app_requirements.md#4.1', domain: 'field', statement: 'Konto pracownika z odebranym dostępem (is_active = false) nie przechodzi bramki logowania do aplikacji terenowej.', acceptance: ['Zablokowane konto nie loguje się do Field App mimo poprawnych danych logowania — kryterium wyjęte z CRM-AUDYT-AC1 decyzją człowieka z 2026-08-20, bo tamto wymaganie zamknięto na bramce panelu B2B', 'Sprawdzenie blokady wykonuje serwer przy każdym żądaniu, nie tylko przy logowaniu — odebranie dostępu w trakcie aktywnej sesji kończy ją, a nie czeka na jej wygaśnięcie', 'Sprawdzenie jest zawężone do ról pracowniczych aplikacji terenowej i nie uruchamia się dla kont, których nie dotyczy (wzorem zawężenia bramki panelu B2B do roli audytor)', 'Błąd zapytania o blokadę kończy się odmową dostępu (fail-closed), nie cichym przepuszczeniem', 'Blokada jest odwracalna: przywrócenie dostępu przywraca logowanie', 'Zablokowany pracownik nie występuje w puli przypisania zleceń', 'Blokada administracyjna (is_active) to co innego niż zadeklarowany przez pracownika status dostępności (field_app_requirements.md#4.2) — odblokowanie nie zmienia deklaracji dostępności i nie odwrotnie'], risk: 'HIGH' }),
  // FLD-CONSENT-DOCS (2026-08-21): status BLOCKED -> TODO. Nośnik istnieje: tabele
  // legal_document_versions i employee_consents (migracja 20260821130000) plus zasoby w macierzy RBAC.
  //
  // USUNIĘTE KRYTERIUM: „Brak akceptacji blokuje podjęcie zlecenia po stronie serwera — żądanie
  // wysłane z pominięciem UI jest odrzucane". Opisywało dokładnie mechanizm, który decyzja D-D
  // (2026-08-21) z tego WO WYKLUCZYŁA: żadnego guarda na T01/T05, żadnego filtra puli przypisania.
  // Jako kryterium akceptacji było nietestowalne — „pominięcie interfejsu" dotyczyło interfejsu
  // Field App, który nie istnieje. Egzekwowanie blokady jest osobnym wymaganiem fazy 3; na poziomie
  // całego systemu docelowo obowiązuje nadal, ale TO wymaganie go nie dowozi i nie udaje, że dowozi.
  // Warunkiem wejściowym dla tamtego przyszłego wymagania jest ostatnie kryterium poniżej
  // (odpytywalność) — tak samo jak FLD-AUTH-BLOCKED przejęło bramkę logowania z CRM-AUDYT-AC1.
  // ── Punkt 23 WO BATCH-MEDIUM-LOW-CLEANUP (2026-09-01, contract-steward) ──
  // POKRYCIE STATYCZNE, BRAK INTEGRACYJNEGO. apps/b2b-web/tests/fld-consent-docs-migration-static.test.ts
  // zamraża wyłącznie TREŚĆ pliku migracji (obecność czterech mechanizmów: freeze opublikowanej wersji,
  // append-only na zgodach, version-must-be-current, częściowy indeks unikalny). Nie dowodzi, że te
  // triggery DZIAŁAJĄ na żywym Postgresie — dowodzi, że nie zniknęły z migracji. Ten test NIE
  // uprawnia FLD-CONSENT-ACCEPT ani FLD-LEGAL-DOC-VERSION do statusu DONE.
  // Uwaga o ID: „FLD-CONSENT-DOCS" z treści WO nie istnieje w tym rejestrze — zostało rozdzielone
  // na FLD-CONSENT-ACCEPT i FLD-LEGAL-DOC-VERSION jeszcze 2026-08-21 (decyzja D-C). Tagowanie
  // testu na te dwa ID jest poprawne i zweryfikowane przez kk-trace.
  R('FLD-CONSENT-TRIGGERS-INTEGRATION', { status: 'BLOCKED', source: 'WO BATCH-MEDIUM-LOW-CLEANUP punkt 23 (2026-09-01) — dług zarejestrowany jawnie, żeby pokrycie statyczne nie było mylone z pokryciem integracyjnym. Migracja 20260821130000 jest uruchomiona na produkcji od 2026-08-27, więc mechanizmy DZIAŁAJĄ na żywej bazie — nikt tego jednak nie weryfikuje automatycznie.', domain: 'field', statement: 'Cztery mechanizmy bazodanowe chroniące zgody pracownicze (freeze opublikowanej wersji dokumentu, append-only na employee_consents, version-must-be-current przy akceptacji, częściowy indeks unikalny na aktualnej wersji) są zweryfikowane testem uruchamianym na żywym Postgresie, nie asercją nad tekstem pliku migracji.', acceptance: ['Test uruchamia migrację na czystej instancji Postgresa i próbuje UPDATE treści opublikowanej wersji dokumentu — baza odrzuca', 'Test próbuje UPDATE i DELETE na istniejącym wierszu employee_consents — baza odrzuca oba', 'Test próbuje wstawić zgodę wskazującą wersję inną niż obowiązująca — baza odrzuca', 'Test próbuje wstawić drugą obowiązującą wersję tego samego dokumentu — częściowy indeks unikalny odrzuca', 'Każdy z czterech przypadków ma osobną asercję na kod błędu bazy, nie na sam fakt wyjątku'], risk: 'HIGH', note: 'BLOKADA: brak środowiska Postgres (brak docker/psql/supabase CLI) w CI i lokalnie. To wąskie gardło CAŁEGO projektu, nie drobiazg tego wymagania — blokuje także wszystkie przyszłe testy RLS i triggerów (patrz SEC-RLS-AUDITOR-SCOPE, CRM-DELETE-ADMIN-ONLY-*, gdzie warstwa RLS jest z tego samego powodu niepokryta). Odblokowanie = jedna decyzja infrastrukturalna, która zdejmuje blokadę z kilkunastu kryteriów naraz.' }),
  R('FLD-CONSENT-ACCEPT', { status: 'TODO', source: 'field_app_requirements.md#4.1', domain: 'field', statement: 'System rejestruje akceptację dokumentów prawnych przez pracownika terenowego: kto, którą wersję i kiedy — w sposób nieodwracalny i odporny na późniejszą zmianę treści.', acceptance: ['Zapis obejmuje moment (_at, timestamptz) oraz wersję wskazaną kluczem obcym do legal_document_versions — sama flaga logiczna nie wystarcza, numer wersji jako tekst też nie (wzorem B2C-CONSENT-RODO)', 'Akceptacja wskazująca nieistniejącą wersję jest odrzucona przez bazę, nie przez walidację aplikacyjną', 'Akceptacja wskazująca wersję, która nie jest w tym momencie obowiązująca (szkic albo wersja zastąpiona w trakcie akceptowania), jest odrzucona przez bazę — pracownik nie może po cichu zaakceptować treści, której nie widział, a Server Action musi z tego powodu przyjmować identyfikator wersji jawnie z frontendu, zamiast doklejać „najnowszą" po stronie serwera', 'Powtórna akceptacja tej samej wersji przez tego samego pracownika nie tworzy drugiego wpisu — dwa równoległe żądania dają dokładnie jeden wiersz, bo decyduje ograniczenie unikalności w bazie, a nie sprawdzenie „czy istnieje" przed zapisem', 'Publikacja nowej wersji nie modyfikuje ani nie unieważnia wpisów już istniejących: po publikacji odczyt wpisu sprzed niej pokazuje starą wersję i niezmieniony znacznik czasu', 'Wpisu w rejestrze nie da się zmienić — UPDATE jest odrzucany przez bazę dla każdej roli, łącznie z administratorem (macierz RBAC nie daje `update` nikomu, profil audit_log)', 'Usunięcie konta pracownika, który cokolwiek zaakceptował, jest odrzucone przez klucz obcy — dowód zgody nie znika razem z kontem i nie zostaje osierocony (R2 w WO). Analogicznej do SEC-RODO-DELETE ścieżki anonimizacji pracownika system dziś NIE MA; to znana luka do osobnej decyzji, a nie sytuacja do obejścia kaskadą', 'Ta sama osoba występująca jako audytor i jako ekipa akceptuje dwa razy, bo klucz obcy wskazuje jedną z dwóch niepowiązanych encji pracowniczych (dokładnie jedną, CHECK w bazie) — odpowiedź „raz" wymaga encji osoby, której w systemie nie ma', 'Stan „czy pracownik ma akceptację obowiązującej wersji" jest odpytywalny bez dodatkowej kolumny stanu (złączenie akceptacji z wersją obowiązującą) — to jest warunek wejściowy dla egzekwowania blokady, ale samo egzekwowanie NIE należy do tego wymagania: decyzja D-D (2026-08-21) umieściła je w Field App (faza 3) i będzie miało własne ID, tak jak FLD-AUTH-BLOCKED przejęło bramkę logowania z CRM-AUDYT-AC1'], risk: 'HIGH' }),
  // FLD-CONSENT-DOCS (2026-08-21): status BLOCKED -> TODO. Otwarte pytanie 6 rozstrzygnięte
  // decyzją D-C: nowa tabela objęta RESOURCES (legal_document_versions), a NIE rozszerzenie
  // mechanizmu treści statycznych B2C ani zasobu `documents`. Ostatnie kryterium („nierozstrzygnięte")
  // zniknęło, bo przestało być prawdziwe — kryterium opisujące otwartą decyzję nie jest kryterium.
  R('FLD-LEGAL-DOC-VERSION', { status: 'TODO', source: 'field_app_requirements.md#4.1', domain: 'field', statement: 'Administrator ma miejsce, w którym wgrywa i wersjonuje treść zgód RODO oraz regulaminu pracowniczego; wersja opublikowana jest niezmienna, a obowiązująca jest zawsze dokładnie jedna na rodzaj dokumentu.', acceptance: ['Wgranie nowej treści tworzy NOWĄ wersję zamiast nadpisywać poprzednią; odczyt starej wersji po publikacji nowej zwraca treść niezmienioną bajt w bajt', 'Zmiana treści wersji już opublikowanej jest odrzucana przez bazę, nie tylko pomijana przez Server Action — na opublikowanym wierszu wolno przestawić wyłącznie znacznik „obowiązująca"', 'Szkic (wersja nieopublikowana) pozostaje edytowalny i nie może być wersją obowiązującą — wersjonowanie zaczyna się od publikacji, nie od pierwszego zapisu roboczego', 'W danym momencie dokładnie jedna wersja danego rodzaju dokumentu jest obowiązująca; próba oznaczenia drugiej jest odrzucona przez częściowy indeks unikalny w bazie, także przy dwóch równoległych publikacjach', 'Wersja jest jawnym identyfikatorem (numer rosnący w obrębie rodzaju dokumentu plus klucz główny, którym wskazuje ją akceptacja), nie datą modyfikacji pliku', 'Wersji, do której odwołuje się jakakolwiek akceptacja, nie da się usunąć — próba kończy się odmową klucza obcego, nie kaskadą i nie osieroconą akceptacją', 'Prawo tworzenia i publikowania ma wyłącznie rola admin, sprawdzana po stronie serwera — ukrycie przycisku w UI nie jest zabezpieczeniem, bo Prisma omija RLS', 'Rodzaj dokumentu pochodzi z zamkniętego słownika w schemacie (enum), a nie z rekordów wprowadzanych w panelu: warunek „komplet zgód" w fazie 3 potrzebuje stabilnego identyfikatora, którego wiersz w tabeli słownikowej by nie dał', 'Treścią jest tekst przechowywany w bazie, nie plik w Storage — obiekt w buckecie jest mutowalny w miejscu, więc nadpisanie pliku po cichu zmieniłoby treść wersji już opublikowanej i wywróciło pierwsze kryterium'], risk: 'HIGH' }),

  // ── Dostępność pracownika (rozdział 4.2, D3; WO FLD-AVAILABILITY-SPLIT, decyzja D-A) ──
  // Status TODO, nie BLOCKED, mimo braku apps/field-app: schemat i macierz uprawnień już istnieją
  // (migracja 20260821120000), więc obie ścieżki zapisu da się napisać i przetestować po stronie
  // serwera, niezależnie od tego, że interfejsu terenowego jeszcze nie ma. To rozróżnia te dwa
  // wymagania od pozostałych FLD-*, które czekają na samą aplikację.
  R('FLD-AVAIL-SELF', { status: 'TODO', source: 'field_app_requirements.md#4.2', domain: 'field', statement: 'Pracownik terenowy sam deklaruje własną niedostępność, a deklaracja jest rozłączna z blokadą administracyjną (is_active) i ze statusem kadrowym (leave_status), których właścicielem pozostaje administrator.', acceptance: ['Deklaracja mieszka w osobnej tabeli availability_declarations (jeden wiersz na pracownika, wskazywany dokładnie jednym z kluczy auditor_id / crew_id), a nie w kolumnie na rekordzie pracownika — dzięki temu prawo zapisu z macierzy RBAC nie sięga is_active ani leave_status', 'Ustawienie się jako niedostępny nie zmienia audytorzy.is_active ani zespoly_monterskie.aktywny — test odczytuje obie wartości przed zmianą i po niej (AC1)', 'Konto zablokowane administracyjnie, które ustawi się jako dostępne, nadal nie przechodzi bramki logowania i nadal nie występuje w puli przypisania (AC2) — to jest scenariusz, dla którego pola zostały rozdzielone', 'Próba zapisu is_active, aktywny albo leave_status przez konto o roli audytor lub monter jest odrzucona po stronie serwera, nie tylko ukryta w interfejsie (AC3); auditors.update i crews.update pozostają wyłącznie dla roli admin', 'Pracownik niedostępny przy aktywnym koncie nie występuje w puli wyboru — filtr rozszerza istniejące zapytania getAuditors() i getCrews(), zamiast tworzyć drugi, równoległy mechanizm (AC4)', 'Brak wiersza deklaracji znaczy „dostępny" (fail-open), odwrotnie niż przy is_active, gdzie brak dostępu znaczy odmowa (fail-closed) — inaczej pracownik dodany po migracji nie trafiłby do puli', 'Wyścig blokady administracyjnej z deklaracją pracownika kończy się deterministycznie: konto zablokowane pozostaje zablokowane niezależnie od kolejności zapisów, bo oba fakty leżą w różnych tabelach i żaden zapis nie nadpisuje drugiego', 'Obie encje objęte symetrycznie, również zespoly_monterskie — mimo że dziś żadna bramka autoryzacyjna nie czyta pola aktywny (R3 w WO), test nie może pokrywać wyłącznie audytorów'], risk: 'HIGH' }),
  R('FLD-AVAIL-RESTORE', { status: 'TODO', source: 'field_app_requirements.md#4.2', domain: 'field', statement: 'Powrót pracownika do statusu dostępnego przywraca wcześniej wprowadzoną dostępność bez ponownego jej wprowadzania (D3).', acceptance: ['Test ustawia dostępność, przełącza pracownika na niedostępnego, wraca i sprawdza, że odczytana dostępność jest identyczna z wprowadzoną (AC5)', 'Deklaracja niedostępności jest przesłonięciem, a nie edycją danych źródłowych: zapis is_available = false nie kasuje i nie modyfikuje żadnego innego zapisu dostępności', 'Schemat świadomie NIE przechowuje kopii „poprzedniej wartości" w osobnej kolumnie — przywrócenie wynika z nienaruszalności danych źródłowych, bo kopia rozjeżdża się z oryginałem przy każdej zmianie wykonanej w czasie nieobecności', 'Gdy powstanie godzinowa dostępność tygodniowa (availability_rules, faza 4), przełącznik z tego wymagania nadal jej nie modyfikuje — to jest warunek do utrzymania przy dodawaniu tamtej tabeli, a nie jednorazowe kryterium', 'Moment ostatniej zmiany deklaracji jest znacznikiem technicznym (updated_at, timestamptz) i nie wyznacza granicy doby roboczej pracownika'], risk: 'MEDIUM' }),

  // ── Dokumentacja zdjęciowa (rozdział 8) ──
  R('FLD-PHOTO-SET', { status: 'BLOCKED', source: 'field_app_requirements.md#8', domain: 'field', statement: 'Zamknięcie montażu wymaga kompletu dokładnie czterech zdjęć: jednostka wewnętrzna, jednostka zewnętrzna, budynek z oddali, odpływ skroplin.', acceptance: ['Brak któregokolwiek z czterech zdjęć odrzuca zamknięcie montażu po stronie serwera, nie tylko wyszarza przycisk w UI', 'Zdjęcia są rozróżnialne rodzajem ze słownika — cztery pliki tego samego rodzaju nie spełniają wymogu', 'Zdjęcia trafiają do dedykowanego bucketu z własnymi politykami dostępu, nie do bucketów awatarów pracowników', 'Model danych przewiduje wiele zdjęć na montaż (installation_photos); pojedyncze pole protocol_url na jeden plik nie wystarcza', 'Nierozstrzygnięte (otwarte pytanie 7, field_app_requirements.md#12): czy komplet obowiązuje przy zamknięciu etapu I montażu dwuetapowego (T17), czy wyłącznie przy T09 — to kryterium uzupełnia się po decyzji, przed implementacją'] }),
];
